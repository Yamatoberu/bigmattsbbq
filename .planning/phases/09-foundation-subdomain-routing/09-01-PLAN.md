---
phase: 09-foundation-subdomain-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-sca-schema.mjs
  - package.json
autonomous: false
requirements: [INFRA-01]

must_haves:
  truths:
    - "Running `npm run check:sca` prints an explicit PASS or FAIL result for sca-schema reachability"
    - "When the sca schema is not exposed, the script exits non-zero and names the exact Supabase dashboard setting to change"
    - "The sca schema is reachable via PostgREST with the service-role key"
  artifacts:
    - path: "scripts/check-sca-schema.mjs"
      provides: "One-command PGRST106 / exposed-schema preflight check"
      min_lines: 30
    - path: "package.json"
      provides: "check:sca npm script"
      contains: "check:sca"
  key_links:
    - from: "scripts/check-sca-schema.mjs"
      to: "SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY"
      via: "fetch against /rest/v1/ with Accept-Profile: sca"
      pattern: "Accept-Profile"
---

<objective>
Remove the confirmed hard blocker that prevents every `sca` schema read in this phase: the `sca` Postgres schema is not in Supabase's PostgREST "Exposed schemas" allowlist.

Purpose: A live probe run against project `wpziabhigztyjrmjpmbw` on 2026-08-23 returned `PGRST106 — Invalid schema: sca. Only the following schemas are exposed: public, graphql_public`. Until this is fixed in the Supabase dashboard, INFRA-01 and INFRA-02 cannot be satisfied and every later plan in this phase would fail with a confusing generic error. This plan ships a permanent one-command preflight check and gates the phase on the dashboard change actually being made.

Output: `scripts/check-sca-schema.mjs`, a `check:sca` npm script, and a verified-reachable `sca` schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation-subdomain-routing/09-CONTEXT.md
@.planning/phases/09-foundation-subdomain-routing/09-RESEARCH.md

<interfaces>
<!-- Existing env var names this script must reuse verbatim. No new secrets (D-05). -->

From lib/supabase.ts (existing service-role client):
  const url = process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL;
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY;

From .env.example (existing keys):
  NEXT_PUBLIC_SUPABASE_URL=
  NEXT_PUBLIC_SUPABASE_ANON_KEY=
  SUPABASE_SERVICE_ROLE_KEY=

Supabase project ref (from supabase/.temp/project-ref): wpziabhigztyjrmjpmbw

Known-failing response body as of 2026-08-23 (HTTP 406):
  {"code":"PGRST106","details":null,"hint":"Only the following schemas are exposed: public, graphql_public","message":"Invalid schema: sca"}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sca schema reachability preflight script</name>
  <files>scripts/check-sca-schema.mjs, package.json</files>
  <read_first>
    - package.json (current scripts block; Node engine and existing script naming style)
    - lib/supabase.ts (exact env var names and fallback order to mirror)
    - .env.example (confirm which Supabase keys exist today)
    - .planning/phases/09-foundation-subdomain-routing/09-RESEARCH.md (Common Pitfalls section 1 — PGRST106 explanation to reuse in the failure message)
  </read_first>
  <action>
    Create `scripts/check-sca-schema.mjs` as a plain Node ESM script (no TypeScript, no bundler, no new dependencies). It must never be imported by application code — it lives in `scripts/` only.

    Behavior:
    - Resolve `url` from `process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL` and `key` from `process.env.SUPABASE_SERVICE_ROLE_KEY`, mirroring the resolution order in `lib/supabase.ts`.
    - If either is missing, print a message naming both variables and `.env.local`, then `process.exit(1)`.
    - Issue a single `fetch` to `${url}/rest/v1/` with headers `apikey: <key>`, `Authorization: Bearer <key>`, and `Accept-Profile: sca`.
    - Read the response body as text and attempt `JSON.parse` inside a try/catch.
    - FAIL path — response body has `code === "PGRST106"` (or the raw text contains `PGRST106`): print a multi-line, unmissable failure message that includes the literal string `PGRST106`, the phrase `sca schema is NOT exposed`, and these exact remediation steps: open the Supabase dashboard for project `wpziabhigztyjrmjpmbw`, go to Project Settings -> Data API -> "Exposed schemas", add `sca` to the list, save, then re-run `npm run check:sca`. Then `process.exit(1)`.
    - FAIL path — any other non-2xx status: print the status code and the first 500 chars of the body, then `process.exit(1)`.
    - PASS path — 2xx: print a success line containing the literal string `sca schema is reachable`, then `process.exit(0)`.
    - Wrap the fetch in try/catch; on a network/DNS error print the error message and `process.exit(1)`.
    - Never print the value of `SUPABASE_SERVICE_ROLE_KEY` or any substring of it, and never echo the full `Authorization` header. Print only the Supabase project URL host.

    In `package.json`, add to the existing `scripts` block: `"check:sca": "node --env-file-if-exists=.env.local scripts/check-sca-schema.mjs"`. Node 24.8.0 is the project runtime and supports `--env-file-if-exists`, so no dotenv dependency is needed. Place the entry after `"lint"` and before `"test"` so the ordering stays readable. Do not modify any other part of `package.json`.
  </action>
  <verify>
    <automated>npm run check:sca; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-sca-schema.mjs` exists and contains the literal header name `Accept-Profile`.
    - `grep -c '"check:sca"' package.json` returns 1.
    - `node -e "const p=require('./package.json'); if(!p.scripts['check:sca']) process.exit(1)"` exits 0.
    - Running `npm run check:sca` produces output containing either `sca schema is reachable` (exit 0) or `PGRST106` plus `Exposed schemas` (exit 1) — never a bare stack trace or an empty error object.
    - `grep -rn "SUPABASE_SERVICE_ROLE_KEY" scripts/check-sca-schema.mjs` shows the variable is only read, never passed to `console.log`/`console.error` as a value.
    - `grep -rn "check-sca-schema" app lib components` returns no matches (script is not imported by application code).
  </acceptance_criteria>
  <done>`npm run check:sca` runs end to end and reports a clear, actionable PASS or FAIL without leaking the service-role key.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Expose the sca schema in the Supabase dashboard</name>
  <what-built>
    A preflight script (`npm run check:sca`) that proves whether the `sca` Postgres schema is reachable through Supabase's Data API using the service-role key.

    As of 2026-08-23 this check FAILS. A live probe returned:
    `PGRST106 — Invalid schema: sca. Only the following schemas are exposed: public, graphql_public`

    This is a dashboard configuration change, not a code change. `service_role` bypasses Row Level Security but does NOT bypass PostgREST's schema routing allowlist. Nothing else in Phase 9 that touches Supabase can work until this is done.
  </what-built>
  <how-to-verify>
    1. Open https://supabase.com/dashboard/project/wpziabhigztyjrmjpmbw/settings/api (project ref `wpziabhigztyjrmjpmbw`, taken from `supabase/.temp/project-ref`).
    2. Find the Data API settings section containing "Exposed schemas".
    3. Add `sca` to the list alongside the existing `public` and `graphql_public` entries. Save.
    4. If the Postgres role grants were never applied, also run this in the SQL editor:
       `GRANT USAGE ON SCHEMA sca TO service_role;`
       `GRANT SELECT ON ALL TABLES IN SCHEMA sca TO service_role;`
       `ALTER DEFAULT PRIVILEGES IN SCHEMA sca GRANT SELECT ON TABLES TO service_role;`
    5. Back in the terminal, run: `npm run check:sca`
    6. Expected: output contains `sca schema is reachable` and the command exits 0.
    7. If it still prints `PGRST106`, the dashboard change did not save or is still propagating — wait ~30 seconds and re-run before reporting a problem.
  </how-to-verify>
  <action>
    Pause execution and present the dashboard steps in the how-to-verify block to the user. Do not attempt to
    change the Supabase project configuration programmatically — the "Exposed schemas" setting is a dashboard-only
    control and the Management API path is not configured for this project. After the user reports the change is
    saved, run `npm run check:sca` and report its exact output. If it still returns PGRST106, wait roughly 30
    seconds for PostgREST to reload its schema cache and re-run once before escalating back to the user.
  </action>
  <resume-signal>Type "approved" once `npm run check:sca` exits 0, or paste the failing output.</resume-signal>
  <verify>
    <automated>npm run check:sca</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:sca` exits 0.
    - Its stdout contains the literal string `sca schema is reachable`.
    - Its stdout does NOT contain `PGRST106`.
  </acceptance_criteria>
  <done>The `sca` schema answers service-role PostgREST requests, unblocking plans 09-03, 09-05, and 09-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell -> Supabase Data API | The service-role key crosses this boundary; it is a full-database credential with RLS bypass |
| developer terminal / CI logs | Anything the script prints may be captured in scrollback, CI logs, or a pasted bug report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | scripts/check-sca-schema.mjs | mitigate | Script reads `SUPABASE_SERVICE_ROLE_KEY` but never logs it or the `Authorization` header; only the URL host is printed. Enforced by the grep acceptance criterion. |
| T-09-02 | Elevation of Privilege | scripts/ -> app bundle | mitigate | Script lives in `scripts/`, is plain `.mjs`, is invoked only via the `check:sca` npm script, and is never imported by `app/`, `lib/`, or `components/`. Enforced by the grep acceptance criterion. |
| T-09-03 | Elevation of Privilege | Supabase `sca` schema grants | mitigate | Grants issued to `service_role` only (`GRANT SELECT`, not `ALL`). The `anon` and `authenticated` roles are deliberately not granted, so exposing the schema does not make `sca` browser-readable. |
| T-09-04 | Tampering | npm/pip/cargo installs | mitigate | Not applicable this plan — no new packages are installed. `--env-file-if-exists` is a Node 24 built-in flag. |
</threat_model>

<verification>
1. `npm run check:sca` exits 0 and prints `sca schema is reachable`.
2. `npm run test` still passes (no regressions from the `package.json` edit).
3. `grep -rn "check-sca-schema" app lib components` returns nothing.
</verification>

<success_criteria>
- A single command (`npm run check:sca`) answers "is the sca schema readable?" with a clear pass/fail and actionable remediation text.
- The `sca` schema is confirmed reachable from the service-role client, so plan 09-03 can generate types and build the client against real data.
- No service-role key material appears anywhere in script output.
</success_criteria>

<output>
Create `.planning/phases/09-foundation-subdomain-routing/09-01-SUMMARY.md` when done.
</output>
