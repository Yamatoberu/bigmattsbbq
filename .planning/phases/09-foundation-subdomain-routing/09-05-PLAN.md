---
phase: 09-foundation-subdomain-routing
plan: 05
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - lib/sca/scoring.ts
  - tests/sca-scoring.test.ts
autonomous: true
requirements: [INFRA-05]

must_haves:
  truths:
    - "One shared function computes distance_from_winning and distance_from_perfect"
    - "The function accepts a real sca score row from the generated types with no field mapping at the call site"
    - "The perfect-score constant 254.5 appears in exactly one place in the codebase"
    - "A score row with an unknown first-place score yields a null distance_from_winning rather than NaN"
  artifacts:
    - path: "lib/sca/scoring.ts"
      provides: "deriveScoreMetrics + PERFECT_SCORE"
      exports: ["deriveScoreMetrics", "PERFECT_SCORE"]
      min_lines: 20
    - path: "tests/sca-scoring.test.ts"
      provides: "Formula, null-handling, and real-row compatibility coverage"
      min_lines: 40
  key_links:
    - from: "tests/sca-scoring.test.ts"
      to: "lib/database-sca.types.ts"
      via: "compile-time assertion that a real Row satisfies the input type"
      pattern: "database-sca.types"
---

<objective>
Ship the single source of truth for SCA derived score math.

Purpose: INFRA-05 and CONTEXT.md D-07. The Dashboard comparison table (Phase 10), the competition detail page (Phase 10), and every analytics trend (Phase 11) all display `Distance From Winning` and `Distance From Perfect Score`. If each page recomputes the formula inline, they will drift. This plan makes recomputation unnecessary and makes the constant impossible to duplicate.

Output: `lib/sca/scoring.ts` and `tests/sca-scoring.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-foundation-subdomain-routing/09-CONTEXT.md
@.planning/phases/09-foundation-subdomain-routing/09-RESEARCH.md
@.planning/phases/09-foundation-subdomain-routing/09-03-SUMMARY.md

<interfaces>
<!-- The formulas, verbatim from REQUIREMENTS.md INFRA-05: -->

  distance_from_winning = first_place_score - total_score
  distance_from_perfect = 254.5 - total_score

<!-- Existing lib/ conventions to follow (from lib/normalizers.ts and lib/cart.ts): -->
  - named exports only, no default export
  - `export function name(...)` declarations, not arrow consts
  - interfaces declared and exported alongside the function in the same file
  - no JSDoc, no inline comments unless explaining a non-obvious WHY
  - 2-space indent, double-quoted strings

<!-- Existing test conventions (tests/packageMapping.test.ts, tests/drops.test.ts): -->
  - Vitest, node environment, `describe` / `it` / `expect`
  - named imports from the module under test using a relative `../lib/...` path

<!-- The real column names live in lib/database-sca.types.ts, generated by plan 09-03.
     Read them from there and from 09-03-SUMMARY.md — do not assume. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deriveScoreMetrics with the two derived formulas</name>
  <files>lib/sca/scoring.ts, tests/sca-scoring.test.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md (INFRA-05 — the two formulas, verbatim)
    - .planning/phases/09-foundation-subdomain-routing/09-CONTEXT.md (D-07 — one function, imported everywhere, no inline recomputation)
    - lib/database-sca.types.ts (generated in plan 09-03 — the REAL score-row field names and their nullability; do not guess `total_score` / `first_place_score` if the generated names differ)
    - .planning/phases/09-foundation-subdomain-routing/09-03-SUMMARY.md (the table and column names recorded by the previous plan)
    - lib/normalizers.ts (the small-pure-function style in this repo to match exactly)
    - tests/packageMapping.test.ts (Vitest style to match)
  </read_first>
  <behavior>
    Using the field names actually present in `lib/database-sca.types.ts` (referred to below by their
    REQUIREMENTS.md names `total_score` and `first_place_score`):

    - ({ total_score: 220, first_place_score: 240 }) -> { distance_from_winning: 20, distance_from_perfect: 34.5 }
    - ({ total_score: 240, first_place_score: 240 }) -> { distance_from_winning: 0, distance_from_perfect: 14.5 }
    - ({ total_score: 254.5, first_place_score: 254.5 }) -> { distance_from_winning: 0, distance_from_perfect: 0 }
    - ({ total_score: 200.5, first_place_score: 233.25 }) -> { distance_from_winning: 32.75, distance_from_perfect: 54 }
      (float result must be exact to 2 decimals; use toBeCloseTo with precision 2 to avoid IEEE-754 noise)
    - ({ total_score: 260, first_place_score: 240 }) -> { distance_from_winning: -20, distance_from_perfect: -5.5 }
      (values above the caps are returned as negatives, not clamped — clamping would hide bad data)
    - ({ total_score: 220, first_place_score: null }) -> { distance_from_winning: null, distance_from_perfect: 34.5 }
    - ({ total_score: 220, first_place_score: undefined }) -> { distance_from_winning: null, distance_from_perfect: 34.5 }
    - ({ total_score: null, first_place_score: 240 }) -> { distance_from_winning: null, distance_from_perfect: null }
    - PERFECT_SCORE === 254.5
  </behavior>
  <action>
    Create `lib/sca/scoring.ts` with named exports only:

    - `export const PERFECT_SCORE = 254.5;` — the single definition of the perfect-score constant for the whole
      repo. Every consumer imports it rather than retyping the number.
    - `export interface ScoreMetricsInput` with the two source fields, typed as `number | null | undefined` so a
      raw generated Row can be passed straight through without narrowing at the call site. Use whatever the
      generated column names actually are.
    - `export interface ScoreMetrics { distance_from_winning: number | null; distance_from_perfect: number | null }`
      — keep snake_case on the output keys so they match the requirement text and the comparison-table row labels
      Phase 10 renders.
    - `export function deriveScoreMetrics(score: ScoreMetricsInput): ScoreMetrics` implementing:
      - `distance_from_perfect` is `null` when total score is null or undefined, otherwise `PERFECT_SCORE - total`.
      - `distance_from_winning` is `null` when either total score or first-place score is null or undefined,
        otherwise `firstPlace - total`.
      - Do not clamp, round, or `Math.abs` either result. Callers format for display; this function only computes.
      - Use explicit `== null` checks (which catch both null and undefined) rather than falsy checks, so a
        legitimate score of `0` is not treated as missing.

    Create `tests/sca-scoring.test.ts` covering every case in the behavior block, plus an assertion that
    `PERFECT_SCORE` equals `254.5`. Use `toBeCloseTo(value, 2)` for any non-integer expectation.
  </action>
  <verify>
    <automated>npx vitest run tests/sca-scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sca-scoring.test.ts` passes with at least 9 cases.
    - `grep -c 'export function deriveScoreMetrics' lib/sca/scoring.ts` returns 1.
    - `grep -c 'export const PERFECT_SCORE' lib/sca/scoring.ts` returns 1.
    - `grep -rn --include=*.ts --include=*.tsx '254.5' lib app components | grep -v 'lib/sca/scoring.ts' | wc -l` returns 0 (the constant is defined exactly once outside tests).
    - `grep -v '^\s*//' lib/sca/scoring.ts | grep -cE 'Math\.(abs|max|min|round)'` returns 0 (no clamping or rounding).
    - `grep -c 'distance_from_winning' lib/sca/scoring.ts` returns at least 2 and `grep -c 'distance_from_perfect' lib/sca/scoring.ts` returns at least 2.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both derived values are computed by one exported function with proven null handling and no rounding.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the input type accepts a real sca score row</name>
  <files>lib/sca/scoring.ts, tests/sca-scoring.test.ts</files>
  <read_first>
    - lib/database-sca.types.ts (the exact `Database["sca"]["Tables"][...]["Row"]` type for the score table)
    - lib/sca/scoring.ts (as written in Task 1)
    - tests/sca-scoring.test.ts (as written in Task 1)
  </read_first>
  <action>
    Guarantee that a row fetched via `getScaSupabaseClient()` can be handed to `deriveScoreMetrics` with no
    mapping, casting, or narrowing at the call site. Without this, Phase 10 pages will each invent their own
    adapter, which is exactly the duplication INFRA-05 forbids.

    In `tests/sca-scoring.test.ts`, add a compile-time assertion block:
    - `import type { Database } from "../lib/database-sca.types";`
    - Alias the real score row: `type ScaScoreRow = Database["sca"]["Tables"][<the actual score table name>]["Row"];`
    - Add a type-level assignability assertion that a `ScaScoreRow` is assignable to `ScoreMetricsInput` — declare
      a typed variable of the row type built from a literal object and pass it to `deriveScoreMetrics` inside a
      real `it(...)` case that asserts the numeric result. A runtime case is preferred over a bare `satisfies`
      because it fails loudly in `npm run test` as well as in `npx tsc --noEmit`.

    If the assertion does not compile because the generated Row's field types are narrower or wider than
    `ScoreMetricsInput` (for example the columns are `number` and non-nullable, or are named differently),
    adjust `lib/sca/scoring.ts`'s `ScoreMetricsInput` field names and types to match the generated Row exactly.
    The generated types are the source of truth — never edit `lib/database-sca.types.ts` to fit the function.

    If the `sca` score table does not carry a first-place score column at all (for example first place lives on a
    competition or placement table), keep `ScoreMetricsInput` as a structural two-field interface, add the
    assignability assertion only for the total-score field, and record the actual location of the first-place
    value in the plan summary so Phase 10 wires it correctly. Do not invent a column that does not exist.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run tests/sca-scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'database-sca.types' tests/sca-scoring.test.ts` returns 1.
    - `grep -c 'Database\["sca"\]\["Tables"\]' tests/sca-scoring.test.ts` returns at least 1.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/sca-scoring.test.ts` passes, including the real-row case.
    - `git diff --stat lib/database-sca.types.ts` produces no output (generated types were not edited to fit the function).
    - `npm run test` exits 0.
  </acceptance_criteria>
  <done>A row straight out of `getScaSupabaseClient()` type-checks as an argument to `deriveScoreMetrics`, verified by the compiler and by a passing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| database row -> derived display value | Untrusted-in-shape data (nullable columns) crosses into arithmetic that is rendered to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-51 | Tampering | derived score display | mitigate | Null and undefined inputs return `null` rather than `NaN`, so a missing first-place score can never render as a plausible-looking wrong number. Covered by three explicit test cases. |
| T-09-52 | Tampering | duplicated formula drift | mitigate | `PERFECT_SCORE` is defined once; a repo-wide grep acceptance criterion fails the task if `254.5` appears anywhere else under `lib`, `app`, or `components`. |
| T-09-53 | Information Disclosure | scoring module | accept | Pure arithmetic with no I/O, no secrets, and no network access. Safe to import from a client component in later phases if needed. |
| T-09-54 | Tampering | generated types edited to fit code | mitigate | A `git diff --stat` acceptance criterion fails the task if `lib/database-sca.types.ts` was modified. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — no packages installed. |
</threat_model>

<verification>
1. `npx vitest run tests/sca-scoring.test.ts` passes.
2. `npm run test` exits 0.
3. `npx tsc --noEmit` exits 0.
4. `grep -rn --include=*.ts --include=*.tsx '254.5' lib app components | grep -v 'lib/sca/scoring.ts' | wc -l` returns 0.
5. `git diff --stat lib/database-sca.types.ts` produces no output.
</verification>

<success_criteria>
- ROADMAP success criterion 3 is satisfied: one shared lib function computes both derived values with no duplicated derivation logic.
- The function accepts real `sca` rows directly, so Phase 10 and 11 pages need no adapter code.
- Missing data produces `null`, never `NaN` or a silently wrong number.
</success_criteria>

<output>
Create `.planning/phases/09-foundation-subdomain-routing/09-05-SUMMARY.md` when done. Record the exact score table
name, the exact column names used, and where the first-place score actually lives — Phase 10 depends on it.
</output>
