---
phase: 09-foundation-subdomain-routing
plan: 04
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - docs/sca-subdomain-deployment.md
  - README.md
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "Project documentation lists the exact remaining manual steps to activate sca.bigmattsbbq.com"
    - "The documentation states plainly that the DNS cutover has NOT been performed"
    - "A reader can find the checklist from README.md without prior knowledge of the phase"
  artifacts:
    - path: "docs/sca-subdomain-deployment.md"
      provides: "Vercel domain + Hostinger CNAME activation checklist"
      contains: "cname.vercel-dns.com"
      min_lines: 40
    - path: "README.md"
      provides: "Pointer to the deployment checklist"
      contains: "sca-subdomain-deployment"
  key_links:
    - from: "README.md"
      to: "docs/sca-subdomain-deployment.md"
      via: "relative markdown link"
      pattern: "docs/sca-subdomain-deployment.md"
---

<objective>
Write down the exact manual steps needed to activate `sca.bigmattsbbq.com`, and be explicit that they have not been done.

Purpose: CONTEXT.md D-11 and ROADMAP success criterion 5. DNS lives at Hostinger, outside this repo, so no code task can complete the cutover. The deliverable is a precise checklist the user can execute in a few minutes when ready — not a claim that routing is live.

Output: `docs/sca-subdomain-deployment.md` plus a discoverable pointer in `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-foundation-subdomain-routing/09-CONTEXT.md
@.planning/phases/09-foundation-subdomain-routing/09-RESEARCH.md
@.planning/phases/09-foundation-subdomain-routing/09-02-SUMMARY.md

<interfaces>
<!-- Facts this doc must state accurately. Sourced from RESEARCH.md "Deployment Checklist" and user memory. -->

DNS registrar/host:  Hostinger.com (bigmattsbbq.com zone)
Target subdomain:    sca.bigmattsbbq.com
Vercel CNAME target: cname.vercel-dns.com (copy the exact value Vercel displays)
Wildcard needed:     no — a single named subdomain, so a plain CNAME is sufficient; no nameserver delegation
vercel.json:         does not exist in this repo and is not needed
App-side routing:    proxy.ts (created in plan 09-02) matches hostname === process.env.SCA_HOSTNAME
                     (default "sca.bigmattsbbq.com") OR hostname.startsWith("sca.")
Env var:             SCA_HOSTNAME — optional, documented in .env.example, only needed to override the default
Preview behavior:    *.vercel.app URLs deliberately do NOT match; use the plain /sca path there
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the subdomain activation checklist</name>
  <files>docs/sca-subdomain-deployment.md</files>
  <read_first>
    - .planning/phases/09-foundation-subdomain-routing/09-RESEARCH.md ("Deployment Checklist (for D-11)" section — the four verified steps and their citations)
    - .planning/phases/09-foundation-subdomain-routing/09-CONTEXT.md (D-11 and D-03 — what is in and out of scope)
    - proxy.ts (created in plan 09-02 — read the actual matching rule and matcher so the doc describes shipped behavior, not planned behavior)
    - lib/sca/routing.ts (the real host-matching implementation)
    - .env.example (the SCA_HOSTNAME entry as it was actually written)
  </read_first>
  <action>
    Create the `docs/` directory (it does not exist yet) and write `docs/sca-subdomain-deployment.md`.

    Required sections, in this order:

    1. Status — Open with an unambiguous status line stating that `sca.bigmattsbbq.com` is NOT yet live and that
       the steps below have not been carried out. Include the literal phrase `NOT yet performed`. State what IS
       done: the application-side routing (`proxy.ts`) ships in this phase, and the tracker is already reachable
       at the `/sca` path on any host.

    2. What already works — bullet list: `/sca` is a real path in dev (`http://localhost:3000/sca`), on Vercel
       previews, and on `https://bigmattsbbq.com/sca`. State explicitly that `*.vercel.app` preview URLs do not
       and should not match the sca host rule (D-03), so previews are exercised via `/sca`.

    3. Step 1 — Add the domain in Vercel. Project Settings, then Domains, then Add `sca.bigmattsbbq.com`. Note
       that this is a single named subdomain and not a wildcard, so Vercel displays a CNAME target (typically
       `cname.vercel-dns.com`) and no nameserver delegation is required.

    4. Step 2 — Add the CNAME at Hostinger. Hostinger DNS zone editor for `bigmattsbbq.com`. Record type `CNAME`,
       Name/Host `sca`, Value/Points-to set to the exact target Vercel displayed (copy verbatim, including a
       trailing period if one is shown), TTL default. Warn the reader not to guess the target value.

    5. Step 3 — Verify. Concrete checks, each with a copy-pasteable command where one exists:
       - `dig +short sca.bigmattsbbq.com` resolves to a Vercel target
       - Vercel's Domains panel shows "Valid Configuration"
       - `curl -sI https://sca.bigmattsbbq.com/` returns HTTP 200
       - `curl -s https://sca.bigmattsbbq.com/` output contains `SCA Tracker`, proving `proxy.ts` fired against a
         real Host header rather than merely the `/sca` path
       - `curl -sI https://bigmattsbbq.com/` still returns HTTP 200 and the storefront is unchanged

    6. No config changes required — state that no `vercel.json` is needed (none exists in this repo) and that
       `SCA_HOSTNAME` only needs to be set in Vercel environment variables if a hostname other than
       `sca.bigmattsbbq.com` is used; the `startsWith("sca.")` rule already covers a future
       `sca.staging.bigmattsbbq.com` with no code change.

    7. Rollback — Removing the CNAME at Hostinger and the domain in Vercel fully reverts activation. Because
       `proxy.ts` only branches on the Host header, no storefront route or behavior changes when the subdomain
       is removed.

    Every actionable item in sections 3 through 5 must be written as an unchecked markdown checkbox. Do not mark
    any item as complete.
  </action>
  <verify>
    <automated>test -f docs/sca-subdomain-deployment.md &amp;&amp; grep -q "NOT yet performed" docs/sca-subdomain-deployment.md &amp;&amp; grep -q "cname.vercel-dns.com" docs/sca-subdomain-deployment.md &amp;&amp; echo DOC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/sca-subdomain-deployment.md` succeeds.
    - `grep -c "NOT yet performed" docs/sca-subdomain-deployment.md` returns at least 1.
    - `grep -c "cname.vercel-dns.com" docs/sca-subdomain-deployment.md` returns at least 1.
    - `grep -c "Hostinger" docs/sca-subdomain-deployment.md` returns at least 1.
    - `grep -c "sca.bigmattsbbq.com" docs/sca-subdomain-deployment.md` returns at least 3.
    - `grep -c "vercel.json" docs/sca-subdomain-deployment.md` returns at least 1.
    - `grep -cE '^\s*- \[x\]' docs/sca-subdomain-deployment.md` returns 0 (nothing is pre-checked).
    - `grep -cE '^\s*- \[ \]' docs/sca-subdomain-deployment.md` returns at least 6.
    - `wc -l < docs/sca-subdomain-deployment.md` returns at least 40.
  </acceptance_criteria>
  <done>A reader with dashboard access can activate the subdomain from this file alone, and no reader can mistake it for a completed action.</done>
</task>

<task type="auto">
  <name>Task 2: Link the checklist from README</name>
  <files>README.md</files>
  <read_first>
    - README.md (current heading structure: Setup, Required env vars, Square version header, How to get Square IDs, API routes, Tests, Notes — match its heading level and tone)
    - docs/sca-subdomain-deployment.md (created in Task 1 — link to the exact filename)
  </read_first>
  <action>
    Add a new `## SCA Tracker subdomain` section to `README.md`, placed after the existing `## API routes` section
    and before `## Tests`, matching the surrounding `##` heading level and terse writing style.

    The section must contain, in three or four lines:
    - The SCA Tracker lives under `app/sca` and is reachable at `/sca` on any host.
    - Host-based routing lives in `proxy.ts` at the repo root (Next.js 16 renamed `middleware.ts` to `proxy.ts`).
    - `SCA_HOSTNAME` is an optional env var, defaulting to `sca.bigmattsbbq.com`.
    - A relative markdown link reading `docs/sca-subdomain-deployment.md` for the remaining manual DNS steps,
      with the words "not yet performed" so the README does not imply the subdomain is live.

    Also add `SCA_HOSTNAME` to the existing `## Required env vars` section, marked as optional with its default
    value, following the formatting already used for the other optional entries in that section.

    Do not reorder, reword, or delete any existing README content.
  </action>
  <verify>
    <automated>grep -q "docs/sca-subdomain-deployment.md" README.md &amp;&amp; grep -q "SCA_HOSTNAME" README.md &amp;&amp; echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "docs/sca-subdomain-deployment.md" README.md` returns at least 1.
    - `grep -c "SCA_HOSTNAME" README.md` returns at least 2 (env var section plus the SCA section).
    - `grep -c "proxy.ts" README.md` returns at least 1 and `grep -c "middleware.ts" README.md` returns at most 1 (only as the historical name, never as an instruction).
    - `grep -c "^## " README.md` returns exactly one more than it did before this task (verify with `git diff README.md`).
    - `git diff README.md` shows only additions — zero deleted lines.
  </acceptance_criteria>
  <done>Someone opening README.md discovers the SCA subdomain situation and the activation checklist without reading any planning artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> operator action | Instructions in this file drive changes to public DNS and a production TLS certificate binding |
| Hostinger DNS zone -> public internet | A wrong record here can hijack or break traffic for the whole apex domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-41 | Tampering | Hostinger DNS zone | mitigate | The checklist scopes the change to a single `CNAME` record named `sca` and explicitly tells the operator to copy the exact target Vercel displays rather than guessing, so the apex `A`/`ALIAS` records are never touched. |
| T-09-42 | Repudiation | "is the subdomain live?" | mitigate | The doc leads with the literal phrase `NOT yet performed` and every action item is an unchecked box, so status cannot be misread. Enforced by acceptance criteria on `- [x]` count being zero. |
| T-09-43 | Spoofing | subdomain takeover | mitigate | The rollback section instructs removing the Vercel domain binding and the Hostinger CNAME together. A CNAME left pointing at Vercel after the project binding is removed is the classic dangling-subdomain takeover vector; documenting them as a pair prevents it. |
| T-09-44 | Information Disclosure | docs content | accept | The file contains only public infrastructure facts (hostname, CNAME target, dashboard navigation). No credentials, project IDs with secret value, or tokens appear. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — documentation only, no packages installed. |
</threat_model>

<verification>
1. `test -f docs/sca-subdomain-deployment.md`
2. `grep -c "NOT yet performed" docs/sca-subdomain-deployment.md` returns at least 1.
3. `grep -cE '^\s*- \[x\]' docs/sca-subdomain-deployment.md` returns 0.
4. `grep -c "docs/sca-subdomain-deployment.md" README.md` returns at least 1.
5. `git diff README.md` shows additions only.
6. `npm run build` exits 0 (documentation changes must not affect the build).
</verification>

<success_criteria>
- ROADMAP success criterion 5 is satisfied: project documentation lists the exact remaining manual DNS steps at Hostinger.
- The checklist is executable start to finish without further research.
- Nothing in the repo claims the subdomain is live when it is not.
- The phase summary surfaces this checklist rather than burying it in a planning-only file (D-11).
</success_criteria>

<output>
Create `.planning/phases/09-foundation-subdomain-routing/09-04-SUMMARY.md` when done. The summary MUST inline the
full activation checklist (not just a link), because D-11 requires it to be visible in the final phase summary.
</output>
