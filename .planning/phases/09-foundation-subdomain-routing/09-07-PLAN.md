---
phase: 09-foundation-subdomain-routing
plan: 07
type: execute
wave: 4
depends_on: ["09-06"]
files_modified: []
autonomous: false
requirements: [INFRA-03, INFRA-04]

must_haves:
  truths:
    - "A human has seen /sca render and confirmed it looks like part of bigmattsbbq.com"
    - "A human has confirmed the storefront is visually and functionally unchanged"
    - "The host-based rewrite has been exercised against a real Host header, not just the /sca path"
  artifacts: []
  key_links:
    - from: "browser"
      to: "app/sca/page.tsx"
      via: "http://localhost:3000/sca and a spoofed sca.bigmattsbbq.com Host header"
      pattern: "SCA Tracker"
---

<objective>
Human verification that the SCA shell looks right and that nothing on the storefront regressed.

Purpose: Phase 9's two visual/behavioral requirements (INFRA-03 "without changing existing routes" and INFRA-04 "reuse the existing theme") cannot be fully proven by greps and type-checks. This checkpoint closes them with eyes on the screen before Phase 10 builds five more pages on this shell.

Output: No code. A pass/fail signal plus any styling corrections needed before Phase 10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-foundation-subdomain-routing/09-CONTEXT.md
@.planning/phases/09-foundation-subdomain-routing/09-02-SUMMARY.md
@.planning/phases/09-foundation-subdomain-routing/09-04-SUMMARY.md
@.planning/phases/09-foundation-subdomain-routing/09-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify the SCA shell and storefront non-regression</name>
  <what-built>
    Everything automatable in Phase 9 is done and green:
    - `proxy.ts` at the repo root rewrites `sca.*` Host traffic into `app/sca` (unit tested across 15+ host/path cases)
    - `app/layout.tsx` drops the storefront NavBar and Footer for SCA requests, driven by a non-spoofable `x-sca-area` header
    - `lib/supabase-sca.ts` provides a typed, server-only, sca-scoped service-role client
    - `lib/sca/scoring.ts` computes both derived score values
    - `/sca` renders the tracker shell with a live competition count read from the `sca` schema
    - `docs/sca-subdomain-deployment.md` documents the remaining manual DNS steps

    What automation cannot confirm: whether the SCA shell actually reads as part of bigmattsbbq.com, and whether
    the storefront still looks and behaves exactly as it did.
  </what-built>
  <how-to-verify>
    Start the dev server: `npm run dev`

    A. SCA shell appearance (INFRA-04)
    1. Open http://localhost:3000/sca
    2. Confirm the dark smoke background, the ember/gold accents, and the Playfair display heading match the rest
       of the site — it should feel like a sibling area, not a different product.
    3. Confirm the header shows: the small Big Matt's BBQ logo with its glow treatment, an "SCA Tracker" wordmark,
       and a single Dashboard nav link.
    4. Confirm the header does NOT show Home, Catering, About, Contact, or a Cart button.
    5. Click the logo. It must navigate to https://bigmattsbbq.com (the marketing site), not back to /sca.
    6. Confirm a real competition count is displayed inside a glass card. If it reads 0, confirm the sparse-state
       wording appears rather than a bare zero.
    7. Resize the browser to a narrow mobile width. Confirm the header does not overflow horizontally and the
       page remains readable.

    B. Host-based rewrite (INFRA-03)
    8. In a terminal: `curl -s -H 'Host: sca.bigmattsbbq.com' http://localhost:3000/ | head -40`
       Confirm the output contains "SCA Tracker" and does not contain "Catering".
    9. `curl -s -H 'Host: sca.bigmattsbbq.com' http://localhost:3000/sca | head -5`
       Confirm this also returns the SCA page and NOT a 404 (this is the double-prefix guard working).

    C. Storefront non-regression (INFRA-03)
    10. Open http://localhost:3000/ — confirm the storefront NavBar, hero, and Footer render exactly as before.
    11. Open http://localhost:3000/catering, /about, and /contact — confirm each renders normally with the full nav.
    12. Add an item to the cart and open /checkout. Confirm the cart badge count and the checkout page still work.
    13. Confirm no console errors appear in the browser devtools on the storefront pages.

    If anything in section A looks off-brand, describe it rather than approving — styling corrections are far
    cheaper now than after Phase 10 builds five more pages on this shell.
  </how-to-verify>
  <action>
    Pause execution and hand the verification steps in the how-to-verify block to the user. Before pausing, run
    the four automated commands (`npm run build`, `npm run test`, `npx tsc --noEmit`, `npm run check:sca`) plus the
    three curl checks in the acceptance criteria, and report each result so the user only has to perform the visual
    review. Start `npm run dev` in the background so the user can open the URLs immediately. Write no code in this
    task; if the user reports a styling or behavior defect, record it and route the fix back through the owning
    plan (09-02 for routing, 09-06 for the shell) rather than patching files from this checkpoint.
  </action>
  <resume-signal>Type "approved" if A, B, and C all pass, or describe exactly what looked or behaved wrong.</resume-signal>
  <verify>
    <automated>npm run build &amp;&amp; npm run test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:sca</automated>
    <human-check>Sections A, B, and C above confirmed in a browser and terminal</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `npm run test` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `npm run check:sca` exits 0.
    - `test -f proxy.ts` succeeds and `test -f middleware.ts` fails.
    - `curl -s -H 'Host: sca.bigmattsbbq.com' http://localhost:3000/` output contains `SCA Tracker` and not `Catering`.
    - `curl -s http://localhost:3000/` output contains `Catering` and not `SCA Tracker`.
    - `curl -s -o /dev/null -w '%{http_code}' -H 'Host: sca.bigmattsbbq.com' http://localhost:3000/sca` returns 200.
    - The user has explicitly typed "approved" for sections A, B, and C.
  </acceptance_criteria>
  <done>A human has confirmed the SCA shell is on-brand and the storefront is unchanged, so Phase 10 can build on this foundation without re-litigating the visual system.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev server -> reviewer's browser | The verification surface; a spoofed Host header is used deliberately here to simulate production routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-71 | Repudiation | phase completion claim | mitigate | The checkpoint requires an explicit "approved" signal covering all three sections plus four green commands, so the phase cannot be recorded as complete on an assumption. |
| T-09-72 | Spoofing | curl Host header in step 8 | accept | Host spoofing against a local dev server is the intended test technique and the only way to exercise the production rewrite path before DNS exists. It grants no privilege — the SCA tracker is public read-only content. |
| T-09-73 | Information Disclosure | dev server on localhost | accept | `npm run dev` binds to localhost with development credentials from `.env.local`. Standard existing practice for this repo; no change introduced by this phase. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — verification only, no packages installed. |
</threat_model>

<verification>
1. `npm run build && npm run test && npx tsc --noEmit && npm run check:sca` all exit 0.
2. Human approval recorded for sections A, B, and C.
3. The deployment checklist from `docs/sca-subdomain-deployment.md` is surfaced in the phase summary per D-11.
</verification>

<success_criteria>
- All five ROADMAP Phase 9 success criteria are demonstrably true, with criteria 1 and 4 confirmed by a human.
- No storefront regression was introduced by the root-layout change or the proxy.
- The user knows exactly what remains manual (Vercel domain + Hostinger CNAME) and that it is not done.
</success_criteria>

<output>
Create `.planning/phases/09-foundation-subdomain-routing/09-07-SUMMARY.md` when done. This is the final plan of the
phase — the summary MUST inline the full Vercel + Hostinger activation checklist from
`docs/sca-subdomain-deployment.md` per D-11, so the remaining manual steps are visible without opening another file.
</output>
