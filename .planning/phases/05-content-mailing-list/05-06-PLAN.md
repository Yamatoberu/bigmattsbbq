---
phase: 05-content-mailing-list
plan: 06
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/catering/page.tsx
  - app/about/page.tsx
  - app/contact/page.tsx
  - components/CateringSection.tsx
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
tags: [content, static-pages, catering, about, contact]

must_haves:
  truths:
    - "Visiting /catering renders a page with 'Catering by Big Matt's BBQ' heading, three tier cards (Basic, Plus, Ultra), expanded included-items list, booking details, and the 'Email for Catering' mailto CTA"
    - "Visiting /about renders a page with 'About Big Matt's BBQ' heading and 2–3 paragraphs of body copy"
    - "Visiting /contact renders a page with 'Get in Touch' heading, general email, catering email, and service area"
    - "The home page CateringSection shows a 'See full catering menu →' link pointing to /catering"
    - "All three new pages render NavBar and Footer via layout.tsx (inherited from Plan 02)"
  artifacts:
    - path: app/catering/page.tsx
      provides: "PAGE-01 — /catering route; reuses CateringSection plus extended detail"
      exports: ["default"]
    - path: app/about/page.tsx
      provides: "PAGE-02 — /about route; static copy"
      exports: ["default"]
    - path: app/contact/page.tsx
      provides: "PAGE-03 — /contact route; static copy with mailto links"
      exports: ["default"]
    - path: components/CateringSection.tsx
      provides: "Home-page teaser with added 'See full catering menu →' link per D-20"
      contains: "See full catering menu"
  key_links:
    - from: components/NavBar.tsx
      to: app/catering/page.tsx
      via: "Link href=/catering"
      pattern: "/catering"
    - from: components/NavBar.tsx
      to: app/about/page.tsx
      via: "Link href=/about"
      pattern: "/about"
    - from: components/NavBar.tsx
      to: app/contact/page.tsx
      via: "Link href=/contact"
      pattern: "/contact"
    - from: components/CateringSection.tsx
      to: app/catering/page.tsx
      via: "home-page teaser link"
      pattern: "/catering"
---

<objective>
Create the three static content pages (PAGE-01 Catering, PAGE-02 About, PAGE-03 Contact)
required to complete the site. Extend the existing `CateringSection` component to
serve both as the home-page teaser (with a new "See full catering menu →" link per
D-20) and as the shared tier-cards body on `/catering` (D-19).

Purpose: NAV-01's 5 nav links need real destinations. Without these pages, the nav
links in Plan 02 go to 404s. The three pages are static, draftable by Claude, and
reviewable by Matt before launch per D-21 / D-22.

Output: Three new pages under `app/` that render with NavBar + Footer (inherited
from Plan 02's layout lift), and an enhanced CateringSection with a teaser link on
the home page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-content-mailing-list/05-CONTEXT.md
@.planning/phases/05-content-mailing-list/05-UI-SPEC.md

@components/CateringSection.tsx
@app/globals.css
@app/page.tsx
@app/layout.tsx
</context>

<interfaces>
Contracts this plan CREATES:

```typescript
// app/catering/page.tsx — new file, Next.js page (default export)
export default function CateringPage(): JSX.Element;

// app/about/page.tsx — new file
export default function AboutPage(): JSX.Element;

// app/contact/page.tsx — new file
export default function ContactPage(): JSX.Element;
```

Contracts this plan MODIFIES:

```typescript
// components/CateringSection.tsx — existing, gains a prop to control the teaser link
export function CateringSection(props?: { showFullMenuLink?: boolean }): JSX.Element;
// When showFullMenuLink is true, renders a "See full catering menu →" link below the CTA (for home page use)
// When false/undefined, the link is hidden (so /catering doesn't link to itself)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend CateringSection with included-items list, booking details, and optional home-page teaser link</name>
  <files>components/CateringSection.tsx</files>
  <read_first>
    - components/CateringSection.tsx (current contents — simple 3-tier card + mailto CTA)
    - components/OrderLanding.tsx (to see how it's currently rendered — wraps CateringSection in a `<section className="section-spacing bg-[#0f0b08]">` with a SectionHeader)
    - app/globals.css (class vocabulary: .glass-card, .button-primary, .button-secondary)
    - .planning/phases/05-content-mailing-list/05-UI-SPEC.md (/catering page spec, CateringSection modifications per D-19 / D-20)
    - .planning/phases/05-content-mailing-list/05-CONTEXT.md (D-19, D-20)
  </read_first>
  <action>
    REWRITE components/CateringSection.tsx to accept a `showFullMenuLink` prop and display tier details. New content:

    ```tsx
    import Link from "next/link";

    type CateringSectionProps = {
      showFullMenuLink?: boolean;
    };

    const TIERS = [
      {
        name: "Basic",
        price: "$22 / person",
        included: [
          "Choice of 1 meat (pulled pork or brisket)",
          "2 classic sides",
          "Buns and house BBQ sauce"
        ]
      },
      {
        name: "Plus",
        price: "$30 / person",
        included: [
          "Choice of 2 meats",
          "3 classic sides",
          "Buns, pickles, house BBQ sauce",
          "Disposable serveware"
        ]
      },
      {
        name: "Ultra",
        price: "$38 / person",
        included: [
          "All 3 meats (pulled pork, brisket, smoked chicken)",
          "4 classic sides",
          "Buns, pickles, house + hot BBQ sauce",
          "Disposable serveware and utensils",
          "On-site setup"
        ]
      }
    ] as const;

    export function CateringSection({ showFullMenuLink = false }: CateringSectionProps = {}) {
      return (
        <div className="glass-card p-6">
          <div className="flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
            <div>
              <h3
                className="text-xl font-semibold text-smoke-900"
                style={{ fontFamily: "var(--font-display)" }}
              >
                Catering, simplified
              </h3>
              <p className="mt-2 text-sm text-smoke-600">
                Need a full spread? We keep catering straightforward and premium.
              </p>
            </div>
            <a href="mailto:catering@bigmattsbbq.com" className="button-primary">
              Email for Catering
            </a>
          </div>

          <div className="mt-6 grid gap-4 md:grid-cols-3">
            {TIERS.map((tier) => (
              <div key={tier.name} className="rounded-md border border-[#3a2a20] bg-[#1c140f] p-4">
                <p className="text-xs font-semibold uppercase tracking-[0.3em] text-[#f0c16a]">
                  {tier.name}
                </p>
                <p className="mt-2 text-xl font-semibold text-smoke-900" style={{ fontFamily: "var(--font-display)" }}>
                  {tier.price}
                </p>
                <ul className="mt-2 space-y-1 text-sm text-smoke-800">
                  {tier.included.map((item) => (
                    <li key={item}>• {item}</li>
                  ))}
                </ul>
              </div>
            ))}
          </div>

          {showFullMenuLink && (
            <div className="mt-6 text-center">
              <Link
                href="/catering"
                className="button-secondary text-sm"
              >
                See full catering menu →
              </Link>
            </div>
          )}
        </div>
      );
    }
    ```

    Constraints:
    - Named export only. No default export.
    - `showFullMenuLink` defaults to `false` — existing callers (OrderLanding currently) continue to render without the teaser link until explicitly opted in
    - CHANGE the mailto in this file from `bigmattsbarbecue@gmail.com` to `catering@bigmattsbbq.com` per UI-SPEC (/catering page) and CONTEXT.md D-19 ("same email CTA — catering@bigmattsbbq.com"). This is a deliberate change — the old email was the general inbox; catering gets a dedicated alias.
    - Use ONLY typography sizes from the declared scale: `text-sm`, `text-xs`, `text-xl`. Do NOT use `text-2xl`, `text-3xl`, `text-4xl` (headline belongs to page files), or `text-5xl`
    - Reuse `.glass-card`, `.button-primary`, `.button-secondary` global classes
    - Color values: only `#3a2a20`, `#1c140f`, `#f0c16a` hex + `text-smoke-600`, `text-smoke-800`, `text-smoke-900` — all already in this file or UI-SPEC
    - `Link` from `next/link` for client-side routing to `/catering`

    Now update the caller in `components/OrderLanding.tsx` (if needed): The home page CateringSection should render with `showFullMenuLink={true}` so the teaser link appears on the home page only. Search for the existing `<CateringSection />` JSX (around line 224 in the active-drop return) and change it to `<CateringSection showFullMenuLink />`.
  </action>
  <verify>
    <automated>grep -q "showFullMenuLink" components/CateringSection.tsx && grep -q "See full catering menu" components/CateringSection.tsx && grep -q "catering@bigmattsbbq.com" components/CateringSection.tsx && grep -q "Basic" components/CateringSection.tsx && grep -q "Plus" components/CateringSection.tsx && grep -q "Ultra" components/CateringSection.tsx && grep -q "<CateringSection showFullMenuLink" components/OrderLanding.tsx && ! grep -q "bigmattsbarbecue@gmail.com" components/CateringSection.tsx && ! grep -qE "text-(2xl|3xl|4xl|5xl)" components/CateringSection.tsx && npm run build 2>&1 | tail -10 | grep -qE "(Compiled|Route \(app\))"</automated>
  </verify>
  <acceptance_criteria>
    - `components/CateringSection.tsx` contains literal `showFullMenuLink` (prop name)
    - Contains literal `"See full catering menu →"` (with the arrow character)
    - Contains literal `"catering@bigmattsbbq.com"` (new dedicated catering email)
    - Does NOT contain `"bigmattsbarbecue@gmail.com"` (old general email — replaced per D-19)
    - Contains a `TIERS` array (or equivalent) with three entries: Basic, Plus, Ultra, each with an `included` array
    - Contains `import Link from "next/link";` (for the /catering link)
    - Does NOT contain `text-2xl`, `text-3xl`, `text-4xl`, or `text-5xl`
    - Does NOT contain `export default`
    - `components/OrderLanding.tsx` contains the JSX `<CateringSection showFullMenuLink` (grep-checkable; matches `<CateringSection showFullMenuLink />` or `<CateringSection showFullMenuLink={true} />`)
    - `npm run build` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>
    CateringSection now shows tier-included-items lists under each price. When `showFullMenuLink` is true, a "See full catering menu →" link appears. Home page passes the prop, /catering page (next task) omits it. Catering mailto updated to `catering@bigmattsbbq.com`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create /catering page</name>
  <files>app/catering/page.tsx</files>
  <read_first>
    - components/CateringSection.tsx (just modified — the component this page consumes without the teaser link)
    - app/globals.css (.section-spacing, .glass-card)
    - .planning/phases/05-content-mailing-list/05-UI-SPEC.md (/catering page spec — headline, subheading, layout, booking details block)
    - .planning/phases/05-content-mailing-list/05-CONTEXT.md (D-19)
    - app/page.tsx (existing page pattern: async default export fetching server data, but /catering is purely static so no async needed)
  </read_first>
  <action>
    Create directory `app/catering/` and file `app/catering/page.tsx` with:

    ```tsx
    import { CateringSection } from "../../components/CateringSection";

    export const metadata = {
      title: "Catering by Big Matt's BBQ",
      description: "Event-ready BBQ catering with three simple tiers. Serving Cache Valley and Utah County."
    };

    export default function CateringPage() {
      return (
        <section className="section-spacing mx-auto max-w-4xl">
          <header className="text-center">
            <h1
              className="text-4xl font-semibold text-smoke-900"
              style={{ fontFamily: "var(--font-display)" }}
            >
              Catering by Big Matt&apos;s BBQ
            </h1>
            <p className="mt-4 text-base text-smoke-700">
              Simple pricing, premium quality, no surprises.
            </p>
          </header>

          <div className="mt-10">
            <CateringSection />
          </div>

          <div className="glass-card p-6 mt-8">
            <h2
              className="text-xl font-semibold text-smoke-900"
              style={{ fontFamily: "var(--font-display)" }}
            >
              How far in advance?
            </h2>
            <p className="mt-3 text-sm text-smoke-700">
              We ask for at least two weeks&apos; notice for catering orders so we can plan the smoke schedule and source the right quantities. Larger events (75+ guests) are best booked a month out.
            </p>
          </div>

          <div className="glass-card p-6 mt-6">
            <h2
              className="text-xl font-semibold text-smoke-900"
              style={{ fontFamily: "var(--font-display)" }}
            >
              Where we cater
            </h2>
            <p className="mt-3 text-sm text-smoke-700">
              Cache Valley and Utah County. If your event is outside this radius, email us — we may be able to make it work for the right event.
            </p>
          </div>

          <div className="mt-10 text-center">
            <a href="mailto:catering@bigmattsbbq.com" className="button-primary">
              Email for Catering
            </a>
          </div>
        </section>
      );
    }
    ```

    Constraints:
    - DEFAULT export (Next.js page convention): `export default function CateringPage()`
    - `metadata` export for page title/description (SEO)
    - `<CateringSection />` rendered WITHOUT `showFullMenuLink` — /catering is the destination, so the "See full catering menu" link would be self-referential
    - Headline: "Catering by Big Matt's BBQ" (use `&apos;` for apostrophe)
    - Subheading: "Simple pricing, premium quality, no surprises."
    - Page uses `section-spacing mx-auto max-w-4xl` per UI-SPEC
    - Booking details block and service area block in separate `.glass-card` boxes
    - CTA at bottom: `mailto:catering@bigmattsbbq.com` with label "Email for Catering"
    - Use ONLY typography sizes `text-sm`, `text-base`, `text-xl`, `text-4xl` (no 2xl/3xl/5xl)
    - Do NOT add `"use client"` — this is a Server Component (static page; no interactive elements at the page level; CateringSection itself is a Server Component)
    - Do NOT create a loading.tsx or error.tsx in this directory — keep minimal for MVP
  </action>
  <verify>
    <automated>test -f app/catering/page.tsx && grep -q "export default function CateringPage" app/catering/page.tsx && grep -q "Catering by Big Matt&apos;s BBQ" app/catering/page.tsx && grep -q "Simple pricing, premium quality, no surprises" app/catering/page.tsx && grep -q "catering@bigmattsbbq.com" app/catering/page.tsx && grep -q "CateringSection" app/catering/page.tsx && ! grep -q "showFullMenuLink" app/catering/page.tsx && ! grep -qE "text-(2xl|3xl|5xl)" app/catering/page.tsx && ! grep -q "^\"use client\"" app/catering/page.tsx && npm run build 2>&1 | tail -10 | grep -qE "(Compiled|Route \(app\))"</automated>
  </verify>
  <acceptance_criteria>
    - `app/catering/page.tsx` exists
    - Contains `export default function CateringPage(`
    - Contains `export const metadata =` (SEO metadata)
    - Contains literal `"Catering by Big Matt&apos;s BBQ"` (page headline)
    - Contains literal `"Simple pricing, premium quality, no surprises."` (subheading)
    - Contains literal `"catering@bigmattsbbq.com"` (bottom CTA mailto)
    - Contains literal `"Email for Catering"` (button label)
    - Contains import of `CateringSection` from `"../../components/CateringSection"`
    - Contains `<CateringSection />` WITHOUT the `showFullMenuLink` prop (so /catering doesn't link to itself)
    - Contains literal `"How far in advance?"` heading
    - Contains literal `"Where we cater"` heading
    - Contains literal `"Cache Valley and Utah County"` (service area text)
    - Does NOT contain `"use client"` (server component by default)
    - Does NOT contain `text-2xl`, `text-3xl`, or `text-5xl`
    - `npm run build` exits 0 and the route table includes `/catering`
  </acceptance_criteria>
  <done>
    /catering page renders with the expected headline, subheading, CateringSection body, two additional info blocks (booking advance, service area), and a bottom CTA mailto button. Metadata export set for SEO. Page is a Server Component.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create /about and /contact pages</name>
  <files>app/about/page.tsx, app/contact/page.tsx</files>
  <read_first>
    - app/catering/page.tsx (just created — same page shape/pattern to mirror)
    - app/globals.css (.section-spacing, .glass-card, .button-primary)
    - .planning/phases/05-content-mailing-list/05-UI-SPEC.md (/about page spec, /contact page spec)
    - .planning/phases/05-content-mailing-list/05-CONTEXT.md (D-21 for About copy guidance, D-22 for Contact)
  </read_first>
  <action>
    PART A — Create directory `app/about/` and file `app/about/page.tsx`:

    ```tsx
    export const metadata = {
      title: "About Big Matt's BBQ",
      description: "The story behind Big Matt's BBQ — pit-smoked, small-batch, Utah-raised."
    };

    export default function AboutPage() {
      return (
        <section className="section-spacing mx-auto max-w-2xl">
          <header>
            <h1
              className="text-4xl font-semibold text-smoke-900"
              style={{ fontFamily: "var(--font-display)" }}
            >
              About Big Matt&apos;s BBQ
            </h1>
          </header>
          <div className="mt-6 space-y-4 text-base text-smoke-800 leading-relaxed">
            <p>
              Big Matt&apos;s BBQ started in a backyard smoker and a weekend obsession with getting pulled pork right. What started as feeding friends and family at barbecues grew into small-batch frozen drops that let folks across Utah stock their freezers with real pit-smoked BBQ.
            </p>
            <p>
              We keep it simple: small batches, one low-and-slow smoke per drop, and no shortcuts. Every brisket and pork shoulder gets 12+ hours over post oak and cherry wood, rested, sliced, vacuum-sealed, and flash-frozen. Heat it at home, slice it, serve it — the smoke is already there.
            </p>
            <p>
              We believe great BBQ isn&apos;t a restaurant — it&apos;s a craft. That&apos;s why we work in drops instead of running a storefront. When a drop opens, we&apos;ve planned the capacity, the pickup windows, and the sourcing. When it closes, we&apos;re already prepping the next one.
            </p>
          </div>
          <p className="mt-8 text-sm text-smoke-600 italic">
            Draft copy — Matt will revise before launch.
          </p>
        </section>
      );
    }
    ```

    PART B — Create directory `app/contact/` and file `app/contact/page.tsx`:

    ```tsx
    export const metadata = {
      title: "Contact Big Matt's BBQ",
      description: "Reach out about drops, catering, or anything else. We answer every email."
    };

    export default function ContactPage() {
      return (
        <section className="section-spacing mx-auto max-w-2xl">
          <header>
            <h1
              className="text-4xl font-semibold text-smoke-900"
              style={{ fontFamily: "var(--font-display)" }}
            >
              Get in Touch
            </h1>
          </header>

          <div className="mt-6 text-base text-smoke-800 leading-relaxed">
            <p>
              We answer every email. Reach out about upcoming drops, catering, or anything else BBQ-adjacent.
            </p>
          </div>

          <div className="glass-card p-6 mt-8 space-y-6">
            <div>
              <p className="text-xs font-semibold uppercase tracking-[0.3em] text-[#f0c16a]">
                General Inquiries
              </p>
              <a
                href="mailto:bigmattsbarbecue@gmail.com"
                className="mt-2 inline-block text-base text-smoke-900 underline hover:text-[#f0c16a]"
              >
                bigmattsbarbecue@gmail.com
              </a>
            </div>

            <div>
              <p className="text-xs font-semibold uppercase tracking-[0.3em] text-[#f0c16a]">
                Catering
              </p>
              <p className="mt-2 text-sm text-smoke-700">
                For event quotes, tier details, and booking.
              </p>
              <a
                href="mailto:catering@bigmattsbbq.com"
                className="button-primary mt-3 inline-block"
              >
                catering@bigmattsbbq.com
              </a>
            </div>

            <div>
              <p className="text-xs font-semibold uppercase tracking-[0.3em] text-[#f0c16a]">
                Service Area
              </p>
              <p className="mt-2 text-base text-smoke-800">
                Cache Valley and Utah County.
              </p>
            </div>
          </div>
        </section>
      );
    }
    ```

    Constraints (BOTH pages):
    - DEFAULT export (Next.js page convention)
    - `metadata` export for SEO
    - Page layout: `section-spacing mx-auto max-w-2xl` per UI-SPEC
    - Page heading uses `text-4xl font-semibold` in Playfair Display
    - Typography scale: ONLY `text-xs`, `text-sm`, `text-base`, `text-xl`, `text-4xl`. No 2xl/3xl/5xl.
    - Color classes from UI-SPEC: `text-smoke-600`, `text-smoke-700`, `text-smoke-800`, `text-smoke-900`, `text-[#f0c16a]`
    - No `"use client"` on either page — both are Server Components (no interactivity at page level)
    - Do NOT include a contact form on /contact (explicitly excluded by D-22 — email CTAs only)
    - Apostrophes in JSX text use `&apos;` (e.g., `Big Matt&apos;s BBQ`)
    - About page copy: 3 paragraphs per UI-SPEC spec (2-3 paragraphs range); subject matter covers who Matt is, BBQ philosophy, and drop model explanation per D-21
    - Contact page entries: general email (`bigmattsbarbecue@gmail.com` — KEEP the existing general email; it's only CateringSection's mailto that was changed to the dedicated catering@ alias), catering email (`catering@bigmattsbbq.com`), service area ("Cache Valley and Utah County")
    - About page includes a footer line "Draft copy — Matt will revise before launch." per D-21 ("Matt reviews and edits before launch")
  </action>
  <verify>
    <automated>test -f app/about/page.tsx && test -f app/contact/page.tsx && grep -q "export default function AboutPage" app/about/page.tsx && grep -q "export default function ContactPage" app/contact/page.tsx && grep -q "About Big Matt&apos;s BBQ" app/about/page.tsx && grep -q "Get in Touch" app/contact/page.tsx && grep -q "bigmattsbarbecue@gmail.com" app/contact/page.tsx && grep -q "catering@bigmattsbbq.com" app/contact/page.tsx && grep -q "Cache Valley and Utah County" app/contact/page.tsx && grep -q "Draft copy" app/about/page.tsx && ! grep -q "^\"use client\"" app/about/page.tsx && ! grep -q "^\"use client\"" app/contact/page.tsx && ! grep -qE "text-(2xl|3xl|5xl)" app/about/page.tsx && ! grep -qE "text-(2xl|3xl|5xl)" app/contact/page.tsx && ! grep -q "<form" app/contact/page.tsx && npm run build 2>&1 | tail -10 | grep -qE "(Compiled|Route \(app\))"</automated>
  </verify>
  <acceptance_criteria>
    - `app/about/page.tsx` exists with `export default function AboutPage(`
    - About page contains literal `"About Big Matt&apos;s BBQ"` (headline)
    - About page contains at least 3 `<p>` tags in the body
    - About page contains literal `"Draft copy — Matt will revise before launch."`
    - About page does NOT contain `"use client"`
    - About page does NOT use `text-2xl`, `text-3xl`, or `text-5xl`

    - `app/contact/page.tsx` exists with `export default function ContactPage(`
    - Contact page contains literal `"Get in Touch"` (headline)
    - Contact page contains literal `"bigmattsbarbecue@gmail.com"` (general email)
    - Contact page contains literal `"catering@bigmattsbbq.com"` (catering email)
    - Contact page contains literal `"Cache Valley and Utah County"` (service area)
    - Contact page does NOT contain `<form` anywhere (D-22 — no contact form)
    - Contact page does NOT contain `"use client"`
    - Contact page does NOT use `text-2xl`, `text-3xl`, or `text-5xl`

    - `npm run build` exits 0
    - Build output includes `/about` and `/contact` in the route table
    - Both pages have `metadata` export
  </acceptance_criteria>
  <done>
    /about and /contact pages exist, render correctly, and follow the typography/color/copy contract in UI-SPEC. About has 3 paragraphs of draft copy with a "Draft copy" disclaimer; Contact has general email, catering email, and service area in a glass-card layout with no form element. Both are Server Components.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — all three static pages render and link from nav</name>
  <what-built>
    - /catering page with expanded CateringSection (3 tiers with included-items list, booking details, service area, mailto CTA)
    - /about page with 3-paragraph draft copy (Matt to revise)
    - /contact page with general email, catering email, service area — no form
    - Home-page CateringSection now has "See full catering menu →" link pointing to /catering
    - CateringSection mailto updated to catering@bigmattsbbq.com
  </what-built>
  <how-to-verify>
    Run `npm run dev` and test at http://localhost:3000:

    NAV LINKS:
    1. Click "Catering" in the nav — /catering loads with "Catering by Big Matt's BBQ" headline
    2. Click "About" — /about loads with "About Big Matt's BBQ" headline
    3. Click "Contact" — /contact loads with "Get in Touch" headline
    4. On each page, verify the active-link highlight in nav: current-page link is gold (`#f0c16a`), other links are default

    /catering PAGE:
    1. Verify 3 tier cards show name, price, and bulleted "included" list (e.g., "• Choice of 1 meat")
    2. "Basic" tier shows 3 bullets; "Plus" shows 4; "Ultra" shows 5 — matches the copy in Task 1
    3. Booking advance block reads "How far in advance?" with "two weeks' notice" guidance
    4. Service area block reads "Where we cater" with "Cache Valley and Utah County"
    5. Bottom CTA "Email for Catering" opens mailto:catering@bigmattsbbq.com
    6. There is NO "See full catering menu →" link on /catering itself (would be self-referential)

    /about PAGE:
    1. Verify 3 paragraphs of body copy
    2. Verify italicized footer "Draft copy — Matt will revise before launch."

    /contact PAGE:
    1. Verify three entries in the glass-card: General Inquiries, Catering, Service Area
    2. "General Inquiries" link opens mailto:bigmattsbarbecue@gmail.com
    3. "Catering" link opens mailto:catering@bigmattsbbq.com (styled as button-primary)
    4. Service area reads "Cache Valley and Utah County."
    5. No form element anywhere on the page

    HOME PAGE REGRESSION:
    1. Visit `/` — CateringSection teaser appears below mailing list section (from Plan 03) with "See full catering menu →" link
    2. Click "See full catering menu →" — navigates to /catering
    3. CateringSection teaser now has "Email for Catering" button pointing to catering@bigmattsbbq.com (not bigmattsbarbecue@gmail.com)

    GLOBAL REGRESSION:
    1. All three new pages show NavBar + Footer (inherited from Plan 02 layout lift)
    2. All three new pages show the Footer mailing list form (from Plan 03) — try submitting from /about to confirm

    Matt specifically review:
    - About page copy — is it accurate / does it need edits?
    - Catering tier `included` lists — are the items correct?
    - Booking advance window (two weeks) — is that the right guidance?
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe copy/content edits needed (e.g., "change about paragraph 2 to...", "Ultra tier should include greens not greens").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static page → no server-side data | These pages are Server Components with no database reads, no user input, no auth |
| mailto: link click → external email client | User's local email app handles the mailto; no web form means no CSRF, no injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-06-01 | Information Disclosure | Exposing Matt's personal email | accept | `bigmattsbarbecue@gmail.com` is already a public business address; it's on /contact and in the footer today. No PII escalation. |
| T-5-06-02 | Tampering | Static page content | mitigate | Pages are Next.js Server Components compiled at build time; no runtime mutation vector. |
| T-5-06-03 | Spam / Abuse | mailto: addresses harvested by scrapers | accept | Standard risk for any public contact page. Alternative (obfuscation/JavaScript decoding) hurts accessibility. |

No security-sensitive flows in Plan 06. Pure static content.
</threat_model>

<verification>
- `npm run build` exits 0 and the route table shows `/catering`, `/about`, `/contact`
- All four `<Link>`s from the NavBar (Catering, About, Contact, and the home-teaser "See full catering menu →") route correctly
- `npm run test` exits 0 (no regressions to existing tests)
- Human sign-off in Task 4 checkpoint
</verification>

<success_criteria>
- PAGE-01 satisfied: /catering renders with expanded CateringSection, booking details, service area, and mailto CTA
- PAGE-02 satisfied: /about renders with 3 paragraphs of draft copy and a disclaimer
- PAGE-03 satisfied: /contact renders with general email, catering email, service area — no form per D-22
- D-19: /catering uses same tiers + same email CTA, plus added detail
- D-20: Home CateringSection has "See full catering menu →" link
- D-21: About copy is drafted by Claude, flagged as draft for Matt's review
- D-22: Contact copy includes general email + catering email + service area; no form
- No typography-scale violations (no text-2xl/3xl/5xl in any new file)
- No `"use client"` on static pages (Server Components only)
</success_criteria>

<output>
After completion, create .planning/phases/05-content-mailing-list/05-06-SUMMARY.md
</output>
