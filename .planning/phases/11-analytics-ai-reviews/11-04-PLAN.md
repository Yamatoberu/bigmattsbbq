---
phase: 11-analytics-ai-reviews
plan: 04
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - app/sca/ai-reviews/page.tsx
  - app/sca/ai-reviews/[id]/page.tsx
autonomous: true
requirements: [AIRV-01, AIRV-02]

must_haves:
  truths:
    - "User can visit /sca/ai-reviews and see every stored AI review, newest first, regardless of review_type"
    - "Each list row shows the review type, model, date, and the linked cook (with its competition folded into the label)"
    - "User can open a single review and read its full untruncated comments"
    - "User can see the review's prompt when one exists, and sees no prompt section at all when it does not"
    - "User can navigate from a review back to its cook and back to the review list"
    - "An invalid or unknown review id renders the SCA 404 page, not an error trace"
  artifacts:
    - path: "app/sca/ai-reviews/page.tsx"
      provides: "AIRV-01 list route"
      min_lines: 50
    - path: "app/sca/ai-reviews/[id]/page.tsx"
      provides: "AIRV-02 detail route"
      min_lines: 60
  key_links:
    - from: "app/sca/ai-reviews/page.tsx"
      to: "lib/sca/queries.ts"
      via: "getAllAiReviews()"
      pattern: "getAllAiReviews"
    - from: "app/sca/ai-reviews/page.tsx"
      to: "/sca/ai-reviews/[id]"
      via: "Read Full Review link per row"
      pattern: "/sca/ai-reviews/\\$\\{"
    - from: "app/sca/ai-reviews/[id]/page.tsx"
      to: "lib/sca/queries.ts"
      via: "parseScaId + getAiReviewById"
      pattern: "parseScaId"
    - from: "app/sca/ai-reviews/[id]/page.tsx"
      to: "/sca/cooks/[id]"
      via: "View Cook back-link"
      pattern: "/sca/cooks/\\$\\{"
---

<objective>
Ship the AI Reviews surface: a newest-first list of every stored `cook_ai_review` row and a drill-down detail page showing model, review type, prompt (only when present), full comments, and back-links to the cook and the list.

Purpose: Closes AIRV-01 and AIRV-02 (D-03 no `review_type` filtering, D-04 no prompt placeholder, D-06 flat newest-first list).
Output: `app/sca/ai-reviews/page.tsx` (new) and `app/sca/ai-reviews/[id]/page.tsx` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-analytics-ai-reviews/11-UI-SPEC.md
@.planning/phases/11-analytics-ai-reviews/11-PATTERNS.md
@app/sca/cooks/page.tsx
@app/sca/cooks/[id]/page.tsx
@lib/sca/format.ts

<interfaces>
<!-- Contracts the executor needs. Produced by plan 11-02 (wave 1). -->

From lib/sca/types.ts (extended in 11-02):
- `AiReviewCookSummary` = `Pick<ScaCookRow, "id" | "steak_label"> & { competition: CookCompetitionSummary | null }`
- `AiReviewWithCook` = the `cook_ai_review` row (`id: number`, `cook_id: number`, `comments: string`, `created_at: string`, `model: string | null`, `prompt: string | null`, `review_type: string | null`) intersected with `{ cook: AiReviewCookSummary | null }`
- `CookCompetitionSummary` = `Pick<ScaCompetitionRow, "id" | "name" | "event_date" | "city" | "state">`

From lib/sca/queries.ts (extended in 11-02):
- `export async function getAllAiReviews(): Promise<AiReviewWithCook[]>` — already ordered `created_at` descending; do NOT re-sort in the page.
- `export async function getAiReviewById(id: number): Promise<AiReviewWithCook | null>` — `null` means row not found.
- `export function parseScaId(raw: string | string[] | undefined): number | null` — existing, already unit-tested against injection-shaped input.

From lib/sca/format.ts (existing, unchanged):
- `cookColumnLabel(competitionName, steakLabel): string` — folds competition name and steak label into one display string, falling back to `"Untitled Cook"`.
- `formatCookDate(isoTimestamp): string` — America/Denver, for `created_at`.
- `formatEventDate(dateOnly): string` — UTC, for `competition.event_date`.
- `EM_DASH = "—"`.

From lib/logger.ts: `logError(message: string, error: unknown, requestId?: string): void`.

Shared class constant repeated verbatim in every `app/sca/**/page.tsx`:
`const LINK_CLASSES = "min-h-[44px] inline-flex items-center text-smoke-800 hover:text-gold-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ember-500";`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the /sca/ai-reviews list route</name>
  <files>app/sca/ai-reviews/page.tsx</files>
  <read_first>
    - app/sca/cooks/page.tsx (the near-identical list-page analog: imports, `dynamic = "force-dynamic"`, try/catch + logError, LINK_CLASSES, the error/empty/list ternary, `space-y-4` card rows)
    - .planning/phases/11-analytics-ai-reviews/11-UI-SPEC.md ("Page Layout: `/sca/ai-reviews` (list)" — the authoritative markup, badge treatment, and copy)
    - lib/sca/format.ts (cookColumnLabel, formatCookDate, EM_DASH semantics)
    - lib/sca/queries.ts (getAllAiReviews, added by plan 11-02)
    - app/globals.css (the `.badge` and `.glass-card` utility definitions being reused)
  </read_first>
  <action>
    Create `app/sca/ai-reviews/page.tsx` as an `async` default-exported Server Component named `ScaAiReviewsPage`, with `export const dynamic = "force-dynamic"` and a local `LINK_CLASSES` constant copied verbatim from `app/sca/cooks/page.tsx`.

    Fetch `getAllAiReviews()` in a try/catch. On failure call `logError("ScaAiReviewsPage query failed", error, "sca-ai-reviews-index-ssr")` and set `errorMessage` to the exact existing string `We couldn't load this page right now. Please try again in a moment.` Never interpolate the caught error into JSX (WR-02).

    Do NOT sort the result in the page — the query already returns `created_at` descending (D-06). Do NOT filter by `review_type` (D-03).

    Page body: `section-spacing` wrapper, h1 `AI Reviews` in `font-[var(--font-display)] text-4xl font-semibold text-[#f7f1e6]`, subtitle `<p className="mt-2 max-w-2xl text-sm text-smoke-800">` reading exactly `Every AI review Big Matt's cooks have received, newest first.`

    Three-branch ternary matching `app/sca/cooks/page.tsx`: error card, then empty card, then the list.
    - Empty card (`glass-card mt-8 p-6`): h2 `No AI reviews recorded yet.` plus a body paragraph `Check back after Big Matt's next AI review run.`
    - List: `<div className="mt-8 space-y-4">` with one `<div key={review.id} className="glass-card p-6">` per review containing, in order:
      1. A metadata row `<div className="flex flex-wrap items-center gap-3">` holding `<span className="badge">` with `review.review_type ?? EM_DASH` (the pill is reserved for review type only), then two `<span className="text-xs uppercase tracking-[0.25em] text-smoke-800">` elements for `review.model ?? EM_DASH` and `formatCookDate(review.created_at)`.
      2. A `Link` to `/sca/cooks/${review.cook.id}` whose text is `cookColumnLabel(review.cook?.competition?.name ?? null, review.cook?.steak_label ?? null)`, classed `"mt-3 " + LINK_CLASSES`. Render this link ONLY when `review.cook` is non-null (the embed is typed nullable per the codebase convention); when null, render the same label text as a plain `<p>` so the row never emits a link to an undefined id and no non-null assertion is needed.
      3. `<p className="mt-3 line-clamp-3 text-sm text-[#f7f1e6]">{review.comments}</p>` — `line-clamp-3` is built into Tailwind 3.4, no plugin needed.
      4. A `Link` to `/sca/ai-reviews/${review.id}` with text `Read Full Review`, classed `"mt-4 " + LINK_CLASSES`.

    Apostrophes in JSX text follow the existing brace-wrapped-string convention used in `app/sca/cooks/page.tsx`. No `"use client"`, no `any`, no non-null assertions, no comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0.
    - `grep -c "getAllAiReviews" app/sca/ai-reviews/page.tsx` returns exactly 2 (import plus one call site).
    - `grep -c 'export const dynamic = "force-dynamic"' app/sca/ai-reviews/page.tsx` returns 1.
    - `grep -c "sca-ai-reviews-index-ssr" app/sca/ai-reviews/page.tsx` returns 1.
    - `grep -c "error.message\|String(error)" app/sca/ai-reviews/page.tsx` returns 0.
    - `grep -c "review_type" app/sca/ai-reviews/page.tsx` is at least 1 and `grep -c "filter(\|=== \"appearance\"" app/sca/ai-reviews/page.tsx` returns 0 (D-03).
    - `grep -c "sort(" app/sca/ai-reviews/page.tsx` returns 0 (D-06 — ordering comes from the query).
    - `grep -c "line-clamp-3" app/sca/ai-reviews/page.tsx` returns 1.
    - `grep -c "Read Full Review" app/sca/ai-reviews/page.tsx` returns 1 and `grep -c "No AI reviews recorded yet." app/sca/ai-reviews/page.tsx` returns 1.
    - `grep -c 'className="badge"' app/sca/ai-reviews/page.tsx` returns 1.
    - `grep -c "cook!\|: any\|as any" app/sca/ai-reviews/page.tsx` returns 0.
  </acceptance_criteria>
  <done>`/sca/ai-reviews` builds and renders every stored review newest-first with its type badge, model, date, linked cook label, 3-line comment preview, and a link into the detail page.</done>
</task>

<task type="auto">
  <name>Task 2: Build the /sca/ai-reviews/[id] detail route</name>
  <files>app/sca/ai-reviews/[id]/page.tsx</files>
  <read_first>
    - app/sca/cooks/[id]/page.tsx (the detail-page analog: `params: Promise<{ id: string }>`, `parseScaId` + `notFound()`, try/catch + logError, LINK_CLASSES, and — at lines 149-177 — the already-proven conditional model/review_type/prompt AI-review rendering block)
    - .planning/phases/11-analytics-ai-reviews/11-UI-SPEC.md ("Page Layout: `/sca/ai-reviews/[id]` (detail)" — the authoritative markup, subtitle composition, and back-link copy)
    - lib/sca/queries.ts (getAiReviewById and parseScaId)
    - lib/sca/format.ts (cookColumnLabel, formatCookDate, formatEventDate, EM_DASH)
    - app/sca/not-found.tsx (the SCA 404 surface reached by notFound())
  </read_first>
  <action>
    Create `app/sca/ai-reviews/[id]/page.tsx` as an `async` default-exported Server Component named `AiReviewDetailPage` accepting `{ params }: { params: Promise<{ id: string }> }`, with `export const dynamic = "force-dynamic"` and a local `LINK_CLASSES` constant copied verbatim from the cook detail page.

    Control flow, mirroring `app/sca/cooks/[id]/page.tsx` exactly: `await params`, run `parseScaId(id)`, call `notFound()` when it returns `null`. Do not write a second id parser or a regex here (ASVS V5 — `parseScaId` is already tested against injection-shaped, exponential, and overflow input). Then fetch `getAiReviewById(reviewId)` inside try/catch; on error call `logError("AiReviewDetailPage query failed", error, "sca-ai-review-detail-ssr")`, set the exact generic string `We couldn't load this page right now. Please try again in a moment.`, and return the single `glass-card p-6` message inside a `section-spacing` wrapper. When the query resolves `null`, call `notFound()`.

    Page body inside `section-spacing`:
    - h1 = `review.model ?? EM_DASH` in `font-[var(--font-display)] text-4xl font-semibold text-[#f7f1e6]`.
    - Subtitle `<p className="mt-2 text-sm text-smoke-800">` composing, separated by a spaced middle dot: a `<span className="badge">` with `review.review_type ?? EM_DASH`, then `formatCookDate(review.created_at)`, then — only when `review.cook` is non-null — a `Link` to `/sca/cooks/${review.cook.id}` whose text is `cookColumnLabel(review.cook.competition?.name ?? null, review.cook.steak_label)`, then — only when `review.cook.competition` is non-null — `formatEventDate(review.cook.competition.event_date)`.
    - Comments section (`<div className="mt-8">`): h2 `Comments` in `font-[var(--font-display)] text-2xl font-semibold text-[#f7f1e6]`, then `<div className="glass-card mt-4 p-6">` containing `<p className="whitespace-pre-line text-sm text-[#f7f1e6]">{review.comments}</p>`. `comments` is NOT NULL in the schema so this section always renders, untruncated.
    - Prompt section: rendered ONLY when `review.prompt` is truthy — same h2 + `glass-card mt-4 p-6` + `whitespace-pre-line` structure with heading `Prompt`. When `prompt` is null the heading and card must not exist in the DOM at all; never emit a `Prompt: —` placeholder (D-04).
    - Footer `<div className="mt-8 flex flex-wrap gap-6">` with a `Link` to `/sca/ai-reviews` reading `Back to AI Reviews` (always), and — only when `review.cook` is non-null — a `Link` to `/sca/cooks/${review.cook.id}` reading `View Cook`. Both use `LINK_CLASSES`.

    Guard every `review.cook` access with a truthiness check rather than a non-null assertion (RESEARCH.md Pitfall 3 — the embed is typed nullable despite the FK). No `"use client"`, no `any`, no comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0, `npm run build` exits 0, `npm run test` exits 0.
    - `grep -c "parseScaId" app/sca/ai-reviews/\[id\]/page.tsx` returns 2 (import plus one call) and `grep -c "notFound()" app/sca/ai-reviews/\[id\]/page.tsx` returns 2 (invalid id and missing row).
    - `grep -c "getAiReviewById" app/sca/ai-reviews/\[id\]/page.tsx` returns 2.
    - `grep -c "/\^\\\\d\|RegExp\|test(" app/sca/ai-reviews/\[id\]/page.tsx` returns 0 (no second id parser).
    - `grep -c "sca-ai-review-detail-ssr" app/sca/ai-reviews/\[id\]/page.tsx` returns 1 and `grep -c "error.message\|String(error)" app/sca/ai-reviews/\[id\]/page.tsx` returns 0.
    - `grep -c "review.prompt &&\|review.prompt ?" app/sca/ai-reviews/\[id\]/page.tsx` is at least 1 and `grep -c "Prompt: " app/sca/ai-reviews/\[id\]/page.tsx` returns 0 (D-04).
    - `grep -c "whitespace-pre-line" app/sca/ai-reviews/\[id\]/page.tsx` returns 2 (comments and prompt).
    - `grep -c "Back to AI Reviews" app/sca/ai-reviews/\[id\]/page.tsx` returns 1 and `grep -c "View Cook" app/sca/ai-reviews/\[id\]/page.tsx` returns 1.
    - `grep -c "cook!\|: any\|as any" app/sca/ai-reviews/\[id\]/page.tsx` returns 0.
    - `grep -c "line-clamp" app/sca/ai-reviews/\[id\]/page.tsx` returns 0 (detail shows untruncated text).
  </acceptance_criteria>
  <done>`/sca/ai-reviews/[id]` builds, 404s on invalid or unknown ids, shows full comments, conditionally shows the prompt, and links back to both the cook and the review list without any non-null assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL segment → server query filter | `/sca/ai-reviews/[id]` is the only user-controlled input this phase adds; it reaches `.eq("id", id)` in `getAiReviewById`. |
| Next.js server → Supabase (`sca`, service role) | Existing boundary; two new read paths added. |
| Server render → browser HTML | AI-generated `comments`/`prompt` text is rendered as React text children (auto-escaped), never via `dangerouslySetInnerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering | `[id]` route param reaching `.eq("id", id)` | mitigate | `parseScaId()` reused verbatim before any query call; `notFound()` on `null`. Acceptance criteria greps that no second regex/parser is introduced (ASVS V5). |
| T-11-10 | Information Disclosure | Raw Supabase error rendered on either new route | mitigate | try/catch + `logError` + fixed generic string on both routes; acceptance criteria greps that `error.message`/`String(error)` never appear. |
| T-11-11 | Information Disclosure | Enumeration of non-existent review ids revealing row counts | accept | All data is public read-only competition history; `notFound()` returns an identical 404 for both invalid and unknown ids, so no differential signal is exposed. |
| T-11-12 | Tampering (XSS) | Model-authored `comments`/`prompt` text rendered to the page | mitigate | Rendered as JSX text children only; no `dangerouslySetInnerHTML` anywhere. `whitespace-pre-line` is a CSS class, not raw HTML. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean
- `npm run build` — succeeds
- `npm run test` — full suite green
- `grep -rc "dangerouslySetInnerHTML" app/sca/ai-reviews/` returns 0 for both files
</verification>

<success_criteria>
- `/sca/ai-reviews` lists every stored review (including `review_type` values other than `appearance`) newest-first with a type badge and a link into the detail page.
- `/sca/ai-reviews/[id]` shows model, review type, created date, cook + competition links, full comments, and the prompt only when present.
- Invalid and unknown ids both render the SCA 404.
- No raw Supabase error text reaches the browser on either route.
</success_criteria>

<output>
Create `.planning/phases/11-analytics-ai-reviews/11-04-SUMMARY.md` when done
</output>
