---
phase: 11-analytics-ai-reviews
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sca/types.ts
  - tests/sca-queries.test.ts
  - lib/sca/queries.ts
autonomous: true
requirements: [AIRV-01, AIRV-02]

must_haves:
  truths:
    - "Server code can fetch every stored AI review, newest first, with its cook and that cook's competition attached"
    - "Server code can fetch a single AI review by id and receive null (not an exception) when the id does not exist"
    - "No AI review is excluded by review_type — photo_review rows come back alongside appearance rows (D-03: no review_type filtering)"
    - "The review list is ordered newest-first by created_at, matching D-06's flat most-recent-first ordering"
  artifacts:
    - path: "lib/sca/types.ts"
      provides: "AiReviewCookSummary and AiReviewWithCook view-model contracts"
      contains: "AiReviewWithCook"
    - path: "lib/sca/queries.ts"
      provides: "getAllAiReviews() and getAiReviewById() server-only queries"
      exports: ["getAllAiReviews", "getAiReviewById"]
    - path: "tests/sca-queries.test.ts"
      provides: "Unit coverage for AIRV-01 select/order and AIRV-02 PGRST116 handling"
      contains: "getAiReviewById"
  key_links:
    - from: "lib/sca/queries.ts"
      to: "sca.cook_ai_review"
      via: "supabase.from(\"cook_ai_review\").select(embed)"
      pattern: "cook_ai_review"
    - from: "lib/sca/queries.ts"
      to: "lib/sca/types.ts"
      via: "AiReviewWithCook type-only import"
      pattern: "AiReviewWithCook"
---

<objective>
Ship the data layer behind AI Reviews: two view-model types and two server-only Supabase queries that join `cook_ai_review` → `cook` → `competition`, following the exact embed/`PGRST116` conventions already proven by `getCompetitionWithCooks` and `getCookWithDetails` in the same file.

Purpose: AIRV-01 needs every stored review newest-first (unfiltered by `review_type`, per D-03); AIRV-02 needs one review by id with its cook and competition for back-links.
Output: `AiReviewCookSummary` + `AiReviewWithCook` in `lib/sca/types.ts`, `getAllAiReviews()` + `getAiReviewById()` in `lib/sca/queries.ts`, and two new `describe` blocks in `tests/sca-queries.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-analytics-ai-reviews/11-RESEARCH.md
@.planning/phases/11-analytics-ai-reviews/11-PATTERNS.md
@lib/sca/queries.ts
@lib/sca/types.ts
@tests/sca-queries.test.ts

<interfaces>
<!-- Contracts the executor needs. Read directly from the codebase during planning. -->

Generated row shape, `Database["sca"]["Tables"]["cook_ai_review"]["Row"]` (already aliased as `ScaCookAiReviewRow` in lib/sca/types.ts):
- `comments: string` (NOT NULL), `cook_id: number` (NOT NULL), `created_at: string` (NOT NULL), `id: number`, `model: string | null`, `prompt: string | null`, `review_type: string | null`.
- There is NO `updated_at` column on this table. `created_at` is the only timestamp.
- FK: `cook_ai_review_cook_id_fkey` on `columns: ["cook_id"]` → `referencedRelation: "cook"`.
- `cook` FK to competition is `cook_competition_id_fkey` → `competition.id` (the same FK `getAllCooksWithScores` already embeds through).

Existing type idioms in lib/sca/types.ts:
- `export type CookCompetitionSummary = Pick<ScaCompetitionRow, "id" | "name" | "event_date" | "city" | "state">;`
- `export type ScaCookRow = Database["sca"]["Tables"]["cook"]["Row"];`
- Every single-embed field in this file is typed nullable (`CookWithScore.competition`, `CookWithDetails.competition`).

Existing query idioms in lib/sca/queries.ts:
- List: `.from(t).select(embed).order(col, { ascending })` then `if (error) throw error; return (data ?? []) as unknown as T[];`
- Detail: `.from(t).select(embed).eq("id", id).single()` then `if (error?.code === "PGRST116") return null; if (error) throw error; return data as unknown as T;`
- `parseScaId(raw)` already exists and is already unit-tested — do not write a second id parser.

Existing test scaffolding in tests/sca-queries.test.ts (lines 1-65) already records `lastCall.table`, `lastCall.select`, `lastCall.order`, `lastCall.eq`, `lastCall.single` and supports `.select().order()`, `.select().eq().single()` chains. No test-infra changes are needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AiReviewCookSummary and AiReviewWithCook to lib/sca/types.ts</name>
  <files>lib/sca/types.ts</files>
  <read_first>
    - lib/sca/types.ts (current contents — `CookCompetitionSummary`, `CookWithScore`, `CompetitionWithCooks`, `CookWithDetails` are the idioms to mirror)
    - lib/database-sca.types.ts (the `cook_ai_review` Row block at lines 131-168 and the `cook` Row block, to confirm field nullability rather than assuming it)
    - .planning/phases/11-analytics-ai-reviews/11-PATTERNS.md (the `lib/sca/types.ts` section, which drafts both new types)
  </read_first>
  <action>
    Append two exported types to `lib/sca/types.ts`, placed alongside the other composed view-model types (after `CookWithDetails` is fine):

    1. `AiReviewCookSummary` = `Pick<ScaCookRow, "id" | "steak_label">` intersected with `{ competition: CookCompetitionSummary | null }`. Use `Pick` on the existing `ScaCookRow` alias rather than re-typing column names by hand, matching `CookCompetitionSummary`'s idiom.

    2. `AiReviewWithCook` = the `cook_ai_review` row fields plus `cook: AiReviewCookSummary | null`. Derive the row fields from the existing `ScaCookAiReviewRow` alias (already declared at line 7 of this file) rather than hand-writing `id`/`cook_id`/`model`/`review_type`/`prompt`/`comments`/`created_at` — e.g. an intersection of `ScaCookAiReviewRow` with `{ cook: AiReviewCookSummary | null }`, mirroring how `CompetitionWithCooks` intersects `ScaCompetitionRow` with its embed.

    `cook` MUST be nullable even though `cook_id` is NOT NULL in the schema — this matches every other single-embed field in the file and preserves the defensive null-check convention (RESEARCH.md Pitfall 3). Do not introduce `any`, non-null assertions, or comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type AiReviewCookSummary\|export interface AiReviewCookSummary" lib/sca/types.ts` returns 1.
    - `grep -c "export type AiReviewWithCook\|export interface AiReviewWithCook" lib/sca/types.ts` returns 1.
    - `grep -c "cook: AiReviewCookSummary | null" lib/sca/types.ts` returns 1.
    - `grep -c "ScaCookAiReviewRow" lib/sca/types.ts` is at least 2 (the existing alias plus the new reuse).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Both AI-review view-model types exist, reuse the generated row aliases instead of duplicating column names, and keep the embedded cook nullable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add getAllAiReviews and getAiReviewById describe blocks to tests/sca-queries.test.ts</name>
  <files>tests/sca-queries.test.ts</files>
  <read_first>
    - tests/sca-queries.test.ts (full file — the mock client/call recorder at lines 1-65, `describe("getAllCooksWithScores")` at lines 129-150, `describe("getCompetitionWithCooks")` at lines 174-195)
    - lib/sca/queries.ts (existing query signatures the new ones must match in shape)
    - .planning/phases/11-analytics-ai-reviews/11-VALIDATION.md (the `-t "getAllAiReviews"` / `-t "getAiReviewById"` filter strings this suite must match)
  </read_first>
  <behavior>
    - describe "getAllAiReviews": queries `lastCall.table === "cook_ai_review"`.
    - describe "getAllAiReviews": `lastCall.select` contains `cook:cook_id` and `competition:competition_id`.
    - describe "getAllAiReviews": `lastCall.select` does NOT contain `review_type` equality filtering, and `lastCall.eq` is `null` — the list is unfiltered by `review_type` (D-03).
    - describe "getAllAiReviews": `lastCall.order` equals `["created_at", { ascending: false }]` (D-06 newest-first).
    - describe "getAllAiReviews": returns `[]` when the client resolves `{ data: null, error: null }`.
    - describe "getAllAiReviews": rejects when the client resolves a non-null error.
    - describe "getAiReviewById": resolves to `null` when the client returns error code `PGRST116`.
    - describe "getAiReviewById": rejects for a non-`PGRST116` error.
    - describe "getAiReviewById": returns the row on success and passes `lastCall.eq === ["id", <id>]` with `lastCall.single === true`.
  </behavior>
  <action>
    Append two new `describe` blocks named exactly `"getAllAiReviews"` and `"getAiReviewById"` to `tests/sca-queries.test.ts`. Do not modify the existing mock client, recorder, `beforeEach`/`afterEach`, or any existing describe block — the scaffolding at lines 1-65 already supports the `.select().order()` and `.select().eq().single()` chains these functions will make.

    Mirror `describe("getAllCooksWithScores")` (lines 129-150) for the list block: set `mockResult`, dynamically `await import("../lib/sca/queries")` inside each `it`, assert against `lastCall`. Mirror the `describe("getCompetitionWithCooks")` PGRST116/reject/success triad (lines 174-195) for the detail block.

    Use a success-row fixture shaped like `{ id, cook_id, model, review_type, prompt, comments, created_at, cook: { id, steak_label, competition: { id, name, event_date, city, state } } }`.

    Cover every bullet in the behavior block. This task is expected to leave these two blocks RED (the functions do not exist yet) while all pre-existing blocks in the file stay green — Task 3 turns them GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/sca-queries.test.ts -t "getAiReviewById" 2>&1 | grep -qi "fail\|error" &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'describe("getAllAiReviews"' tests/sca-queries.test.ts` returns 1.
    - `grep -c 'describe("getAiReviewById"' tests/sca-queries.test.ts` returns 1.
    - `grep -c 'describe("parseScaId"\|describe("getAllCooksWithScores"\|describe("getCompetitions"\|describe("getCompetitionWithCooks"\|describe("getCookWithDetails"' tests/sca-queries.test.ts` still returns 5 (no existing block was removed or renamed).
    - The new blocks assert `["created_at", { ascending: false }]` and `"PGRST116"` literally.
    - `npx vitest run tests/sca-queries.test.ts` fails only inside the two new describe blocks at the end of this task.
  </acceptance_criteria>
  <done>Two new describe blocks encode the AIRV-01 select/order contract and the AIRV-02 PGRST116 contract, and currently fail because the queries are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement getAllAiReviews and getAiReviewById in lib/sca/queries.ts</name>
  <files>lib/sca/queries.ts</files>
  <read_first>
    - lib/sca/queries.ts (full file — `getAllCooksWithScores` lines 21-35 is the list analog, `getCompetitionWithCooks` lines 51-71 is the detail/PGRST116 analog)
    - lib/sca/types.ts (the `AiReviewWithCook` type added in Task 1)
    - lib/database-sca.types.ts (`cook_ai_review` Row + FK names, to build the embed select string against real columns)
    - tests/sca-queries.test.ts (the contract written in Task 2)
    - .planning/phases/11-analytics-ai-reviews/11-RESEARCH.md (Pattern 3 — the drafted select strings)
  </read_first>
  <action>
    Add `AiReviewWithCook` to the existing type-only import block at the top of `lib/sca/queries.ts` (keep the block alphabetized as it currently is). Do not add any new runtime import — `getScaSupabaseClient` and `import "server-only"` are already present.

    Add `getAllAiReviews(): Promise<AiReviewWithCook[]>`: `.from("cook_ai_review")`, select the embed string `id, cook_id, model, review_type, prompt, comments, created_at, cook:cook_id(id, steak_label, competition:competition_id(id, name, event_date, city, state))`, then `.order("created_at", { ascending: false })`. Do NOT add a `.eq("review_type", ...)` filter — D-03 requires every stored row, including the `photo_review` one. Error handling and the return cast follow `getAllCooksWithScores` exactly: `if (error) { throw error; }` then `return (data ?? []) as unknown as AiReviewWithCook[];`.

    Add `getAiReviewById(id: number): Promise<AiReviewWithCook | null>`: same `.from("cook_ai_review")` and the identical embed select string, then `.eq("id", id).single()`. Error handling follows `getCompetitionWithCooks` exactly: `if (error?.code === "PGRST116") { return null; }` then `if (error) { throw error; }` then `return data as unknown as AiReviewWithCook;`. Do not add id parsing inside the query — callers use the existing `parseScaId` before invoking.

    Extract the shared embed select string into a module-private `const` so the list and detail functions cannot drift apart, or inline it in both — either is acceptable, but the two strings must be byte-identical. No `any`, no non-null assertions, no comments.
  </action>
  <verify>
    <automated>npx vitest run tests/sca-queries.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/sca-queries.test.ts` exits 0 with all blocks passing, including the two added in Task 2.
    - `npx tsc --noEmit` exits 0.
    - `grep -c "export async function getAllAiReviews" lib/sca/queries.ts` returns 1 and `grep -c "export async function getAiReviewById" lib/sca/queries.ts` returns 1.
    - `grep -c 'eq("review_type"' lib/sca/queries.ts` returns 0 (D-03: no review_type filter).
    - `grep -c "PGRST116" lib/sca/queries.ts` returns 3 (the two pre-existing detail queries plus `getAiReviewById`).
    - `grep -c 'import "server-only"' lib/sca/queries.ts` returns 1 (the file stays server-only).
    - `npm run test` exits 0.
  </acceptance_criteria>
  <done>Both AI-review queries exist, are type-safe, return newest-first unfiltered list data and null-on-missing detail data, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js server → Supabase (`sca` schema, service role) | Service-role reads bypass RLS by design (existing INFRA-01 decision). This plan adds two new read paths across that existing boundary; no new credential or new boundary is introduced. |
| Route param → query filter | `/sca/ai-reviews/[id]` will pass a caller-supplied id into `.eq("id", id)`. Validated upstream, not in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | `getAiReviewById(id)` `.eq("id", id)` filter | mitigate | Signature accepts `number`, not `string`; callers must run the already-tested `parseScaId()` (regex `^\d+$` + `Number.isSafeInteger` + `> 0`) before invoking. No second id parser is written (ASVS V5). |
| T-11-04 | Information Disclosure | Thrown Postgrest error escaping to the page | mitigate | Queries throw the raw error; every calling page wraps in try/catch, calls `logError()` and renders the fixed generic string (WR-02). Enforced in plan 11-04's acceptance criteria. |
| T-11-05 | Elevation of Privilege | Service-role client leaking into client bundle | mitigate | `lib/sca/queries.ts` keeps its existing `import "server-only"` first line; acceptance criteria greps that it is still present. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this plan (RESEARCH.md Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx vitest run tests/sca-queries.test.ts` — green including the two new describe blocks
- `npm run test` — full suite green
- `npx tsc --noEmit` — clean
</verification>

<success_criteria>
- `getAllAiReviews()` returns every `cook_ai_review` row, ordered `created_at` descending, with cook and competition embedded, and no `review_type` filtering.
- `getAiReviewById(id)` returns the joined row, or `null` for a missing id, and rethrows any other Supabase error.
- `AiReviewWithCook.cook` is nullable and no non-null assertion is introduced anywhere.
- Zero new dependencies added to `package.json`.
</success_criteria>

<output>
Create `.planning/phases/11-analytics-ai-reviews/11-02-SUMMARY.md` when done
</output>
