---
phase: 11-analytics-ai-reviews
plan: 05
type: execute
wave: 3
depends_on: ["11-03", "11-04"]
files_modified:
  - components/sca/ScaNavBar.tsx
autonomous: false
requirements: [ANLY-01, ANLY-02, ANLY-03, AIRV-01, AIRV-02]

must_haves:
  truths:
    - "User can reach Analytics and AI Reviews from the SCA nav on every SCA page (D-07)"
    - "The SCA nav reads Dashboard, Competitions, Cooks, Analytics, AI Reviews in that order, completing the five-item information architecture (D-07)"
    - "The active-link highlight works for the two new routes without new logic"
    - "A human has confirmed all five Phase 11 success criteria against live Supabase data"
  artifacts:
    - path: "components/sca/ScaNavBar.tsx"
      provides: "Five-entry scaNavLinks completing the SCA information architecture"
      contains: "/sca/ai-reviews"
  key_links:
    - from: "components/sca/ScaNavBar.tsx"
      to: "/sca/analytics"
      via: "scaNavLinks entry"
      pattern: "/sca/analytics"
    - from: "components/sca/ScaNavBar.tsx"
      to: "/sca/ai-reviews"
      via: "scaNavLinks entry"
      pattern: "/sca/ai-reviews"
---

<objective>
Wire the two new surfaces into the SCA navigation and gate the phase on a human verification pass against live data.

Purpose: D-07 completes the five-item SCA information architecture; the checkpoint proves all five ROADMAP Phase 11 success criteria are actually true in the browser, since this repo has no jsdom/RTL rendering tests for any `app/sca` page.
Output: updated `components/sca/ScaNavBar.tsx` and a human-approved verification of Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-analytics-ai-reviews/11-UI-SPEC.md
@.planning/phases/11-analytics-ai-reviews/11-VALIDATION.md
@components/sca/ScaNavBar.tsx

<interfaces>
<!-- Current state of the edit point, read during planning. -->

components/sca/ScaNavBar.tsx (client component, `"use client"` at line 1) currently declares:
`const scaNavLinks = [{ label: "Dashboard", href: "/sca" }, { label: "Competitions", href: "/sca/competitions" }, { label: "Cooks", href: "/sca/cooks" }] as const;`

`isActive(href)` already handles any new href generically:
- `href === "/sca"` matches `pathname === "/"` or `pathname === "/sca"`
- otherwise matches `pathname === href || pathname.startsWith(href + "/")`

The `.map()` render is entirely driven by that array — no per-link markup exists to duplicate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Analytics and AI Reviews entries to scaNavLinks</name>
  <files>components/sca/ScaNavBar.tsx</files>
  <read_first>
    - components/sca/ScaNavBar.tsx (current 3-entry array, `isActive` logic, and the href-driven `.map()` render)
    - .planning/phases/11-analytics-ai-reviews/11-UI-SPEC.md ("Navigation" section — the exact final array and ordering)
  </read_first>
  <action>
    Append exactly two entries to the `scaNavLinks` array in `components/sca/ScaNavBar.tsx`, after the existing `Cooks` entry, producing the final order Dashboard, Competitions, Cooks, Analytics, AI Reviews:
    - `{ label: "Analytics", href: "/sca/analytics" }`
    - `{ label: "AI Reviews", href: "/sca/ai-reviews" }`

    Keep the `as const` assertion on the array. Change nothing else in the file — `isActive()` already matches new hrefs generically via `pathname === href || pathname.startsWith(href + "/")`, so `/sca/ai-reviews/7` correctly highlights the `AI Reviews` entry with no new logic. Do not add nav entries for `/sca/ai-reviews/[id]` or any other detail route; detail pages stay drill-down-only, matching the Cook Detail and Competition Detail precedent (D-07).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"/sca/analytics"' components/sca/ScaNavBar.tsx` returns 1 and `grep -c '"/sca/ai-reviews"' components/sca/ScaNavBar.tsx` returns 1.
    - `grep -c 'label: "' components/sca/ScaNavBar.tsx` returns 5.
    - `grep -c "as const" components/sca/ScaNavBar.tsx` returns 1.
    - `grep -n '"/sca/cooks"' components/sca/ScaNavBar.tsx` reports a lower line number than `grep -n '"/sca/analytics"'`, which in turn reports a lower line number than `grep -n '"/sca/ai-reviews"'` (order is Dashboard, Competitions, Cooks, Analytics, AI Reviews).
    - `git diff --stat components/sca/ScaNavBar.tsx` shows 2 inserted lines and 0 deleted lines beyond the trailing-comma/formatting of the previous last entry.
    - `npx tsc --noEmit`, `npm run build`, and `npm run test` all exit 0.
  </acceptance_criteria>
  <done>The SCA nav exposes all five top-level destinations in the specified order, with active highlighting working on both new routes and no other change to the component.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Analytics and AI Reviews against live data</name>
  <files>none</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 11 "Success Criteria" — the five statements being verified)
    - .planning/phases/11-analytics-ai-reviews/11-VALIDATION.md ("Manual-Only Verifications" table — why these checks cannot be automated in this repo)
  </read_first>
  <what-built>
    - `lib/sca/trends.ts` — `buildTrendSeries()` shared by all seven charts, unit-tested in `tests/sca-trends.test.ts`
    - `components/sca/TrendChart.tsx` — static SVG chart Server Component, no client JS, no new dependency
    - `app/sca/analytics/page.tsx` — `/sca/analytics` rendering Total Score, Gap to First, and five judging-category trends from one Supabase fetch
    - `lib/sca/queries.ts` — `getAllAiReviews()` / `getAiReviewById()`, unit-tested in `tests/sca-queries.test.ts`
    - `app/sca/ai-reviews/page.tsx` and `app/sca/ai-reviews/[id]/page.tsx` — AI review list and detail
    - `components/sca/ScaNavBar.tsx` — five-item nav
    Automated gates already green: `npm run test`, `npx tsc --noEmit`, `npm run build`.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the SCA area (via `sca.bigmattsbbq.com` host mapping or `http://localhost:3000/sca`). Live data as of planning: 21 cooks, 20 scores, 3 AI reviews (`appearance` x2, `photo_review` x1).

    1. Nav: confirm the SCA nav reads Dashboard, Competitions, Cooks, Analytics, AI Reviews in that order, and that each link lands on a real page.
    2. `/sca/analytics` — ANLY-01: the "Total Score" chart renders a line with a point per scored cook, first/last dates printed at the bottom corners, and value labels on the first, last, min, and max points.
    3. `/sca/analytics` — ANLY-02: the "Gap to First" chart renders and its values look like gaps to first place (small numbers), not total scores.
    4. `/sca/analytics` — ANLY-03: five separate charts appear under "Judging Categories" — Appearance, Doneness, Texture, Taste, Overall Impression — and each has its own vertical scale (category lines are NOT flattened against the total-score range).
    5. `/sca/analytics` at phone width (DevTools ~390px): charts stack one per row and stay readable; no horizontal scrolling; no console errors.
    6. `/sca/ai-reviews` — AIRV-01: all three reviews appear newest-first, including the `photo_review` one (it must NOT be filtered out). Each row shows a review-type badge, model, date, a clickable cook label, a 3-line comment preview, and "Read Full Review".
    7. Click a cook label on a list row: it opens that cook's detail page.
    8. `/sca/ai-reviews/{id}` — AIRV-02: open a review that HAS a prompt and one that does NOT. The one with a prompt shows a "Prompt" section; the one without shows no Prompt heading and no "Prompt: —" placeholder anywhere.
    9. On a detail page — AIRV-02 "linked back to its cook and competition": the subtitle shows a clickable cook label AND a clickable event date, and the footer shows "Back to AI Reviews", "View Cook", and "View Competition". Click each of the three footer links: "Back to AI Reviews" returns to the list, "View Cook" opens that cook's detail page, and "View Competition" opens that cook's competition detail page (`/sca/competitions/{id}`). Also confirm comments render untruncated with paragraph breaks preserved.
    10. Visit `/sca/ai-reviews/999999` and `/sca/ai-reviews/abc`: both show the SCA 404 page, not an error trace or raw Supabase message.
    11. Storefront non-regression: visit `http://localhost:3000/` and confirm the main Big Matt's BBQ site still renders with its own nav (no SCA chrome bleed).
  </how-to-verify>
  <action>
    Confirm the automated gates are green first (`npm run test`, `npx tsc --noEmit`, `npm run build`), start the dev server with `npm run dev`, then present the eleven verification steps above to the developer and PAUSE. Modify no file in this task. Record the developer's verdict verbatim in the plan summary; if any step fails, capture the step number and the observed behavior so it can be routed into gap-closure planning rather than patched inline.
  </action>
  <verify>
    <human-check>Developer walks the eleven steps above against live Supabase data and replies with "approved" or the failing step numbers.</human-check>
    <automated>npm run test &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>
  <resume-signal>Type "approved" to close Phase 11, or describe any check that failed with the step number.</resume-signal>
  <acceptance_criteria>
    - All eleven verification steps were performed by a human against live Supabase data.
    - All five ROADMAP Phase 11 success criteria are confirmed TRUE, or each failure is recorded with its step number for gap closure.
    - The `photo_review` row was observed present on `/sca/ai-reviews` (proves D-03 was honored).
    - A prompt-less review was observed with no Prompt section rendered (proves D-04 was honored).
    - On an AI Review detail page, "View Cook" and "View Competition" were both observed and both navigated to the correct `/sca/cooks/{id}` and `/sca/competitions/{id}` pages (proves AIRV-02's "linked back to its cook and competition").
    - Both `/sca/ai-reviews/999999` and `/sca/ai-reviews/abc` returned the SCA 404.
    - No raw Supabase or Postgrest error text was visible anywhere in the browser.
  </acceptance_criteria>
  <done>Human has typed "approved", or has listed the failing steps to be routed into gap-closure planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client component render → browser | `ScaNavBar` is the one client component under `app/sca`; this plan adds two static hrefs to its link array. No user input, no new data crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering | `scaNavLinks` hrefs | accept | Hrefs are compile-time string literals in an `as const` array; no runtime construction, no user input reaches them. |
| T-11-14 | Information Disclosure | Human verification performed against production Supabase data | accept | Data is public read-only competition history intended for a public subdomain; the verification is a browser read, no writes exist in this milestone. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean
- `npm run build` — succeeds
- `npm run test` — full suite green
- Human checkpoint approved (all five ROADMAP Phase 11 success criteria confirmed)
</verification>

<success_criteria>
- SCA nav shows five entries in the order Dashboard, Competitions, Cooks, Analytics, AI Reviews, with correct active highlighting on `/sca/analytics` and `/sca/ai-reviews` (including detail routes).
- A human has confirmed ANLY-01, ANLY-02, ANLY-03, AIRV-01, and AIRV-02 in the browser against live data.
- The storefront at `/` is unaffected.
</success_criteria>

<output>
Create `.planning/phases/11-analytics-ai-reviews/11-05-SUMMARY.md` when done
</output>
