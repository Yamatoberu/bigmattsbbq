---
phase: 10-core-browsing-dashboard-competitions-cook-detail
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sca/aggregates.ts
  - tests/sca-aggregates.test.ts
  - app/sca/cooks/page.tsx
  - components/sca/ScaNavBar.tsx
autonomous: true
gap_closure: true
closes_gaps: [G-10-1]
requirements: [COOK-01]

must_haves:
  truths:
    - "A visitor can reach any cook's detail page from the SCA nav bar without first landing on a comparison table"
    - "The Cooks index lists every recorded cook newest-first, each linking to /sca/cooks/{id}"
    - "A cook with no score row still appears in the index with em dashes instead of NaN or 0"
    - "The Cooks nav link is highlighted while on /sca/cooks and on /sca/cooks/{id}"
  artifacts:
    - path: "app/sca/cooks/page.tsx"
      provides: "Cooks index route (server component) listing all cooks"
      contains: "export const dynamic = \"force-dynamic\""
    - path: "lib/sca/aggregates.ts"
      provides: "sortCooksByRecencyDesc pure helper"
      exports: ["sortCooksByRecencyDesc"]
    - path: "tests/sca-aggregates.test.ts"
      provides: "unit coverage for sortCooksByRecencyDesc"
      contains: "sortCooksByRecencyDesc"
  key_links:
    - from: "components/sca/ScaNavBar.tsx"
      to: "/sca/cooks"
      via: "scaNavLinks array entry"
      pattern: "href: \"/sca/cooks\""
    - from: "app/sca/cooks/page.tsx"
      to: "/sca/cooks/{id}"
      via: "next/link per cook row"
      pattern: "/sca/cooks/\\$\\{cook.id\\}"
    - from: "app/sca/cooks/page.tsx"
      to: "lib/sca/queries.ts"
      via: "getAllCooksWithScores()"
      pattern: "getAllCooksWithScores"
---

<objective>
Close gap G-10-1: cook detail pages are currently reachable only by clicking a cook column header inside a `ComparisonTable`. Ship a `/sca/cooks` index page and a `Cooks` nav entry so cook detail is discoverable from anywhere in the SCA Tracker.

Purpose: COOK-01 says a user can view a single cook's detail page. Human UAT (10-HUMAN-UAT.md, test 1) found no discoverable path to that page. This plan adds the missing navigation surface without touching any already-shipped page body.
Output: `app/sca/cooks/page.tsx` (new route), a `Cooks` entry in `scaNavLinks`, and a tested `sortCooksByRecencyDesc` helper in `lib/sca/aggregates.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-HUMAN-UAT.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-UI-SPEC.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-PATTERNS.md
@app/sca/competitions/page.tsx
@lib/sca/queries.ts
@lib/sca/types.ts

<interfaces>
<!-- Contracts the executor needs. Do not re-derive these from the codebase. -->

From lib/sca/types.ts:
- `CookWithScore` = `{ id: number; steak_label: string | null; cooked_at: string; competition_id: number | null; competition: CookCompetitionSummary | null; score: ScaScoreRow | null }`
- `CookCompetitionSummary` = `Pick<ScaCompetitionRow, "id" | "name" | "event_date" | "city" | "state">`

From lib/sca/queries.ts (server-only):
- `getAllCooksWithScores(): Promise<CookWithScore[]>` — every cook with its embedded competition summary and `score` row, ordered `cooked_at` ASCENDING. Throws the raw Supabase error on failure.

From lib/sca/format.ts (pure):
- `EM_DASH` — the literal `—`
- `formatScoreValue(value: number | null | undefined): string` — returns `EM_DASH` for null/undefined/non-finite
- `cookColumnLabel(competitionName, steakLabel): string` — `"<comp> - <label>"`, falls back to one side, then `"Untitled Cook"`
- `formatCookDate(isoTimestamp: string): string` — `America/Denver`, e.g. `Nov 1, 2025`

From lib/logger.ts:
- `logError(message: string, error: unknown, requestId: string): void`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sortCooksByRecencyDesc to lib/sca/aggregates.ts with unit coverage</name>
  <files>lib/sca/aggregates.ts, tests/sca-aggregates.test.ts</files>
  <read_first>
    - lib/sca/aggregates.ts (current exports and the existing `getLatestCooks` sort style this must match)
    - tests/sca-aggregates.test.ts (the `makeScore` / `makeCook` fixture factories to reuse — do not create new fixture helpers)
    - lib/sca/comparison.ts (the `sortCooksAscending` helper — the new function is its descending mirror; keep the same copy-before-sort discipline)
  </read_first>
  <behavior>
    - `sortCooksByRecencyDesc([])` returns `[]`
    - Given cooks with `cooked_at` of `2025-11-01`, `2025-11-05`, `2025-11-03`, the result order is `2025-11-05`, `2025-11-03`, `2025-11-01`
    - Ties on identical `cooked_at` are broken by descending `id` (higher id first), so ordering is deterministic
    - The input array is not mutated — the caller's array order is unchanged after the call
    - Cooks with `score: null` are retained (this is a sort, not a filter)
  </behavior>
  <action>
Add a new named export `sortCooksByRecencyDesc(cooks: CookWithScore[]): CookWithScore[]` to `lib/sca/aggregates.ts`. It is a pure function: copy the input with a spread before sorting (mirroring `sortCooksAscending` in `lib/sca/comparison.ts`), compare `cooked_at` strings descending (later string first), and break exact ties with `b.id - a.id`. Do not filter out unscored cooks — this is the index-page ordering helper, not an aggregate input.

Append a `describe("sortCooksByRecencyDesc", ...)` block to `tests/sca-aggregates.test.ts` covering the five behaviors above. Reuse the file's existing `makeCook` factory with `id`/`cooked_at`/`score` overrides; do not add a second fixture factory. Import the new function by adding it to the existing named-import list from `../lib/sca/aggregates`.
  </action>
  <verify>
    <automated>npx vitest run tests/sca-aggregates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function sortCooksByRecencyDesc" lib/sca/aggregates.ts` returns one match
    - `npx vitest run tests/sca-aggregates.test.ts` exits 0 with at least 5 new assertions under a `sortCooksByRecencyDesc` describe block
    - A test asserts the input array is unmutated (compare the caller's array `.map(c => c.id)` before and after)
    - A test asserts descending-`id` tie-break for two cooks sharing one `cooked_at` value
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>`sortCooksByRecencyDesc` is exported, pure, non-mutating, and covered by passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create the /sca/cooks index route</name>
  <files>app/sca/cooks/page.tsx</files>
  <read_first>
    - app/sca/competitions/page.tsx (the list-page template this must mirror exactly: `dynamic = "force-dynamic"`, `LINK_CLASSES` constant, try/catch + `logError` + generic `errorMessage`, error / empty / list three-branch render)
    - app/sca/cooks/[id]/page.tsx (the detail page this links to, and its heading/label conventions)
    - lib/sca/queries.ts (`getAllCooksWithScores` return shape and ordering)
    - lib/sca/format.ts (`EM_DASH`, `formatScoreValue`, `cookColumnLabel`, `formatCookDate`)
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-UI-SPEC.md (locked copy strings and the Label/Body/Heading/Display type scale)
  </read_first>
  <action>
Create `app/sca/cooks/page.tsx` as an async Server Component named `ScaCooksPage`, structurally cloned from `app/sca/competitions/page.tsx`. Relative import depth from this file is three levels (`../../../lib/...`).

Required elements:
1. `export const dynamic = "force-dynamic";`
2. Reuse the same `LINK_CLASSES` string constant already used verbatim in `app/sca/competitions/page.tsx` and `app/sca/cooks/[id]/page.tsx` (`min-h-[44px] inline-flex items-center text-smoke-800 hover:text-gold-300 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-ember-500`) — this satisfies the UI-SPEC's 44px tap-target rule.
3. Data: `let cooks: CookWithScore[] = []` then `cooks = await getAllCooksWithScores()` inside a try/catch. On catch call `logError("ScaCooksPage query failed", error, "sca-cooks-index-ssr")` and set `errorMessage` to the locked string `We couldn't load this page right now. Please try again in a moment.` Never assign or render `error.message`.
4. Order for display with `sortCooksByRecencyDesc(cooks)` from `../../../lib/sca/aggregates` — newest first, matching the Competitions list's newest-first convention (COMP-01).
5. `<h1>` reads `Cooks` using the existing Display classes `font-[var(--font-display)] text-4xl font-semibold text-[#f7f1e6]`, followed by a `text-sm text-smoke-800` sub-line: `Every steak Big Matt's has turned in, newest first.`
6. Three render branches in the same order as the Competitions list: `errorMessage` → a `.glass-card mt-8 p-6` with the error paragraph; else zero cooks → a `.glass-card mt-8 p-6` with heading `No cooks recorded yet.` and body `Check back after Big Matt's next SCA cookoff.`; else the list.
7. Each list row is a `div.glass-card.p-6` keyed by `cook.id` inside a `mt-8 space-y-4` wrapper containing, in order:
   - an eyebrow `p` (`text-xs uppercase tracking-[0.25em] text-smoke-800`) with `formatCookDate(cook.cooked_at)`
   - an `h2` (`mt-2 font-[var(--font-display)] text-2xl font-semibold text-[#f7f1e6]`) with `cookColumnLabel(cook.competition?.name ?? null, cook.steak_label)`
   - a meta `p` (`mt-2 text-sm text-smoke-800`) reading `Total Score {formatScoreValue(cook.score?.total_score)} · Placement {formatScoreValue(cook.score?.placement)}` — `formatScoreValue` already yields the em dash for missing values per D-04, so render it unconditionally; never substitute `0` or blank
   - a `<Link href={`/sca/cooks/${cook.id}`} className={"mt-4 " + LINK_CLASSES}>` labelled `View Cook` (the UI-SPEC's locked CTA copy for cook drill-down)
8. Do not use `dangerouslySetInnerHTML` anywhere; all cook/competition text renders as JSX children so React escapes it.
  </action>
  <verify>
    <automated>MISSING — page rendering has no automated coverage (vitest runs in `environment: "node"` with no jsdom/RTL per 10-VALIDATION.md). Automated substitute: `npx tsc --noEmit && npm run build && grep -rn "dangerouslySetInnerHTML\|error\.message" app/sca/cooks/page.tsx` (the grep MUST return no matches)</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 and its route table lists `/sca/cooks` as a distinct route alongside `/sca/cooks/[id]`
    - `grep -rn "dangerouslySetInnerHTML" app/sca/cooks/page.tsx` returns no matches
    - `grep -rn "error\.message" app/sca/cooks/page.tsx` returns no matches
    - `grep -c 'force-dynamic' app/sca/cooks/page.tsx` returns 1
    - `grep -n 'sortCooksByRecencyDesc' app/sca/cooks/page.tsx` returns a match (the page uses the tested helper rather than an inline sort)
    - The three locked strings appear verbatim in the file: `We couldn't load this page right now. Please try again in a moment.`, `No cooks recorded yet.`, `Check back after Big Matt's next SCA cookoff.`
    - With the dev server running, `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/sca/cooks` returns `200`
  </acceptance_criteria>
  <done>`/sca/cooks` renders every recorded cook newest-first, each row linking to that cook's detail page, with em dashes for missing scores and the locked error/empty copy.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Cooks entry to the SCA nav bar</name>
  <files>components/sca/ScaNavBar.tsx</files>
  <read_first>
    - components/sca/ScaNavBar.tsx (the `scaNavLinks` const array at line 7 and the `isActive` helper at lines 15-18 — confirm the `startsWith(href + "/")` fallback before editing)
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-CONTEXT.md (D-11 — the original decision that Cook Detail stays drill-down-only; this plan supersedes only the nav half of D-11, per gap G-10-1)
  </read_first>
  <action>
Append exactly one entry to the `scaNavLinks` const array so it reads, in order: `{ label: "Dashboard", href: "/sca" }`, `{ label: "Competitions", href: "/sca/competitions" }`, `{ label: "Cooks", href: "/sca/cooks" }`. Keep the `as const` assertion.

Change nothing else in this file. `isActive` already returns true for `/sca/cooks` (exact match) and `/sca/cooks/123` (via the `pathname.startsWith(href + "/")` branch), so the active-state highlight needs no edit. Do not touch the `/sca` special case on line 16.

In the plan summary, note that this supersedes the nav half of D-11 ("Cook Detail stays drill-down-only") because human UAT rejected drill-down-only as undiscoverable (G-10-1).
  </action>
  <verify>
    <automated>MISSING — nav rendering has no automated coverage (no jsdom/RTL). Automated substitute: `npx tsc --noEmit && grep -n 'href: "/sca/cooks"' components/sca/ScaNavBar.tsx`</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'href: "/sca' components/sca/ScaNavBar.tsx` returns 3
    - `grep -n 'label: "Cooks", href: "/sca/cooks"' components/sca/ScaNavBar.tsx` returns exactly one match
    - `git diff --stat components/sca/ScaNavBar.tsx` shows at most 3 changed lines (array entry only — no changes to `isActive` or the rendered markup)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The SCA nav bar exposes Dashboard, Competitions, and Cooks, and the Cooks link highlights on both `/sca/cooks` and `/sca/cooks/{id}`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → `/sca/cooks` (SSR) | Unauthenticated request triggers a service-role Supabase read; only the response body may cross back |
| Supabase `sca` schema → rendered HTML | Operator-authored text (`steak_label`, competition `name`) is echoed into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-34 | Information Disclosure | `app/sca/cooks/page.tsx` catch block | mitigate | Assign only the locked generic string to `errorMessage`; raw errors go to `logError(..., "sca-cooks-index-ssr")` server-side. Enforced by the `grep -rn "error\.message"` gate in Task 2 |
| T-10-35 | Tampering (stored XSS) | cook `steak_label` / competition `name` rendered on the index | mitigate | Render every value as a JSX child so React escapes it; `dangerouslySetInnerHTML` grep gate must return no matches |
| T-10-36 | Information Disclosure | Cooks index enumerates every cook id publicly | accept | Per D-07 the entire cook set is already public on the Dashboard comparison table; the index exposes no field the Dashboard does not. Single-chef, read-only, non-sensitive competition scores |
| T-10-37 | Denial of Service | unbounded `getAllCooksWithScores()` on a `force-dynamic` public route | accept | Per D-02 the dataset is dozens of rows for the life of this milestone; the Dashboard already runs the identical unbounded query on a public route. Revisit with pagination if row count grows past the hundreds |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (10-RESEARCH.md Package Legitimacy Audit: not applicable — zero new dependencies) |
</threat_model>

<verification>
- `npm run test` exits 0 (full suite, including the new `sortCooksByRecencyDesc` cases)
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0 and lists both `/sca/cooks` and `/sca/cooks/[id]`
- `grep -rn "dangerouslySetInnerHTML\|error\.message" app/sca components/sca lib/sca` returns no matches
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/sca/cooks` returns 200
</verification>

<success_criteria>
- G-10-1 closed: a visitor can reach any cook detail page from the nav bar in two clicks (Cooks → View Cook), with no comparison table involved
- Every recorded cook appears on `/sca/cooks`, newest first
- Unscored cooks render em dashes, never `0`, `NaN`, or blank
- No existing page body, query, or test was modified beyond the additive nav entry and the new aggregates helper
</success_criteria>

<output>
Create `.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-10-SUMMARY.md` when done
</output>
