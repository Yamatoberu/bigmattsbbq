---
phase: 10
slug: core-browsing-dashboard-competitions-cook-detail
status: draft
shadcn_initialized: false
preset: none
created: 2026-08-23
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — hand-rolled Tailwind design system (no shadcn) |
| Preset | not applicable |
| Component library | none — plain Tailwind + hand-authored component classes (`.glass-card`, `.badge`, `.button-primary`, `.button-secondary`, `.input-field`) defined in `app/globals.css` |
| Icon library | none — inline SVG only (matches existing `NavBar`/`ScaNavBar` precedent); no new icons required for Phase 10 (drill-down links use text, not icon buttons) |
| Font | Playfair Display (`--font-display`, headings/display) + Nunito Sans (`--font-body`, body text) — both loaded once in `app/layout.tsx`, inherited by `app/sca/layout.tsx`; no new font weights or families this phase |

**shadcn gate resolution:** `components.json` is absent and this is a Next.js/React project, which would normally trigger the shadcn initialization prompt. This gate is intentionally **not** run for Phase 10. Rationale, pre-decided by upstream artifacts:
- `REQUIREMENTS.md` INFRA-04 (Phase 9, complete): "SCA Tracker pages reuse the site's existing Tailwind theme … rather than introducing a new visual system."
- `REQUIREMENTS.md` Out of Scope table: "A separate/second Next.js app, Vercel project, or design system for the tracker" is explicitly excluded.
- `10-CONTEXT.md` canonical refs: "reuse ember/smoke tokens … per Phase 9's D-09, no new visual system."
- Phase 9 already shipped and human-verified (`09-07-PLAN.md`) the on-brand shell (`ScaNavBar`, `ScaFooter`, `app/sca/layout.tsx`) that this phase's pages render inside.

Introducing shadcn now would directly contradict a locked, human-verified decision. Design System values above are pre-populated from the existing codebase, not from a shadcn preset.

---

## Visual Hierarchy (Focal Points)

One primary focal point per screen — the element that should draw the eye first, before any other content on the page:

| Screen | Primary Focal Point |
|--------|---------------------|
| Dashboard | The "closest gap to first" insight card (DASH-03/D-05) — its `ember`/`gold` accent numeric callout is the single visual anchor above the fold; the comparison table sits below it as supporting detail, not competing for first-glance attention |
| Competitions (list) | The most recent competition's row card, positioned first in the list — reverse-chronological order plus consistent `.glass-card` styling makes recency the implicit hierarchy signal, with no additional color/size emphasis needed |
| Competition Detail | The comparison table's "Best Cook" highlighted column — the one `accent`-colored column draws the eye first, framing every other column as a point of comparison against it |
| Cook Detail | The cook's steak label as the page `<h1>` (Display, 36px) — immediately followed by the Score Breakdown section directly beneath it; process variables and AI reviews are secondary, lower-emphasis sections further down the page |

---

## Spacing Scale

Declared values (must be multiples of 4), matching the scale already in use sitewide (`section-spacing`, `.glass-card` padding, `NavBar` touch targets):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-text gaps, badge internal padding |
| sm | 8px | Compact element spacing, dense table cell vertical padding |
| md | 16px | Default element spacing, card internal gaps |
| lg | 24px | Card padding (`.glass-card` / `p-6`), section horizontal padding (`section-spacing`) |
| xl | 32px | Layout gaps between stacked page sections (summary cards → comparison table → insights) |
| 2xl | 48px | Major section breaks (`section-spacing` vertical), matches existing `py-12` |
| 3xl | 64px | Page-level top/bottom spacing on long pages (Cook Detail with all sections populated) |

Exceptions:
- Comparison table cells (D-03's horizontally-scrolling wide table) use tighter **12px horizontal / 8px vertical** cell padding instead of the 24px card default — density is required to keep many cook columns legible without excessive per-cell whitespace; still multiples of 4.
- Any tappable row/column header cell that also acts as a drill-down link (e.g. cook column header → Cook Detail) keeps a **44px minimum tap target height** on mobile, matching the existing `NavBar` mobile-toggle convention (`min-h-[44px]`), even where the visual cell is shorter.

---

## Typography

Exactly one declared size per role (4 sizes total, matching existing sitewide usage in `SectionHeader.tsx` and `app/sca/page.tsx`):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Label | 12px (`text-xs`) | 600 (semibold) | 1.4 |
| Body | 14px (`text-sm`) | 400 (regular, Nunito Sans) | 1.5 |
| Heading | 24px (`text-2xl`) | 600 (semibold, Playfair Display) | 1.2 |
| Display | 36px (`text-4xl`) | 600 (semibold, Playfair Display) | 1.2 |

Usage notes (pre-populated from existing sitewide conventions, e.g. `SectionHeader.tsx`, `app/sca/page.tsx`):
- **Label (12px/600):** comparison table row labels (Competition, Cook, Placement, category names, Total Score, Distance From Winning, Distance From Perfect Score), summary card eyebrow text, badges — typically paired with `uppercase tracking-[0.25em]` per existing `.badge`/`ScaNavBar` link styling.
- **Body (14px/400):** comparison table data cells, process-variable values, AI review comments, general paragraph copy.
- **Heading (24px/600, `text-2xl`):** section titles within a page — "Comparison Table," "Process Variables," "Score Breakdown," "AI Reviews." This is the single size for `SectionHeader`'s `<h2>` this phase; do not apply the existing `md:text-3xl` responsive bump within Phase 10 markup — one declared size keeps the contract unambiguous.
- **Display (36px/600, `text-4xl`):** page-level `<h1>` — "Dashboard," "Competitions," a competition's name on its detail page, a cook's steak label on Cook Detail. Matches the existing `competitionCount` display number in `app/sca/page.tsx`.

Exactly 2 weights declared (400, 600) — matches the project's existing typographic discipline; `font-bold` (700, used only by `CountdownTimer`'s live-drop timer) is out of scope for this read-only, non-time-sensitive phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0b0806` (body background, `pit-surface` `#120c09`) | Page background — inherited automatically from `app/globals.css` body styles; no new background color needed |
| Secondary (30%) | `#16100c` (`pit-card`, via `.glass-card`), border `#3a2a20` | Summary cards, comparison table container, competition list row cards, Cook Detail section panels — reuse `.glass-card` as-is |
| Accent (10%) | `ember-500` (`#e64622`) / `gold-300` (`#f0c16a`) | See reserved-for list below — never applied broadly |
| Destructive | not applicable | Phase 10 ships zero destructive actions (read-only browsing per v2.0 milestone scope); if a negative/attention numeric value ever needs emphasis (e.g. a large gap-to-first), reuse the existing `ember-300` form-error tone already used sitewide rather than introducing a new red |

Accent reserved for:
- Active nav link state in `ScaNavBar` (existing pattern — `text-gold-300` on the active `Competitions`/`Dashboard` link)
- "Best Cook" aggregate column/card highlight on the Dashboard and Competition detail comparison table (single visual callout, not a general highlight color) — this is the Competition Detail focal point (see Visual Hierarchy above)
- The numeric callout inside the "closest gap to first" insight card (DASH-03/D-05) — the one data point the insight is built around, and the Dashboard's focal point (see Visual Hierarchy above)
- Hover/focus state on drill-down links (competition row → detail, cook column/row → Cook Detail) — matches existing `hover:text-gold-300` / `focus-visible:outline-ember-500` conventions already used in `ScaNavBar`
- `.badge` component background (existing `#7a1a0e`/ember-tinted) if a badge is used anywhere in this phase (e.g. an "AI Reviewed" indicator on Cook Detail)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | "View Competition" (competition list row → Competition Detail) and "View Cook" (comparison table cook column/row → Cook Detail) — this phase is navigational/browsing, not conversion-driven; no purchase or submit action exists |
| Empty state heading | "No competitions recorded yet." (zero-competitions case, matches existing `app/sca/page.tsx` precedent verbatim) |
| Empty state body | "Check back after Big Matt's next SCA cookoff." — paired with the heading above |
| Empty state (locked, D-09) | "No process detail recorded for this cook." — Cook Detail process-variables section when no `cook_detail` row exists |
| Empty state (locked, D-10) | "No AI reviews yet." — Cook Detail AI review section when zero `cook_ai_review` rows exist; section always renders per D-10, never hidden |
| Missing table value (locked, D-04) | `—` (em dash) — any comparison-table or score-breakdown cell backed by a nullable/missing score; never `0` or blank |
| Error state | "We couldn't load this page right now. Please try again in a moment." — generic, user-facing; paired with server-side `logError()`. Never render `error.message` to the visitor (fixes the WR-02 pattern flagged in `09-REVIEW.md`; distinct from a true 404, which uses Next.js `notFound()` per RESEARCH.md Pattern 3, not this error copy) |
| Destructive confirmation | not applicable — no destructive actions in this phase (read-only milestone; no create/edit/delete flows exist) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not used this project | not applicable |
| third-party | none declared | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
