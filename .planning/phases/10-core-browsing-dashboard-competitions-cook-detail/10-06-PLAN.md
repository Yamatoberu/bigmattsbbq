---
phase: 10-core-browsing-dashboard-competitions-cook-detail
plan: 06
type: execute
wave: 4
depends_on: ["10-04", "10-05"]
files_modified:
  - app/sca/page.tsx
  - components/sca/ScaNavBar.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Visiting /sca shows summary cards for latest cooks, best cook, worst cook, average total score, and average gap to first"
    - "Visiting /sca shows a comparison table with every recorded cook as a named column plus Worst Cook, Best Cook, and Cook Averages columns"
    - "Visiting /sca shows a data-driven what-stands-out section reflecting real score data"
    - "A failed Supabase read shows a generic message and never a raw database error"
    - "The SCA nav bar links to both Dashboard and Competitions"
  artifacts:
    - path: "app/sca/page.tsx"
      provides: "The real Dashboard (DASH-01, DASH-02, DASH-03), replacing the Phase 9 placeholder body"
      contains: "SummaryCards"
      min_lines: 60
    - path: "components/sca/ScaNavBar.tsx"
      provides: "scaNavLinks with the Competitions entry (D-11)"
      contains: "/sca/competitions"
  key_links:
    - from: "app/sca/page.tsx"
      to: "lib/sca/queries.ts"
      via: "getAllCooksWithScores() server-side call"
      pattern: "getAllCooksWithScores"
    - from: "app/sca/page.tsx"
      to: "components/sca/ComparisonTable.tsx"
      via: "buildComparisonTable(cooks, { aggregates: true }) passed as the model prop"
      pattern: "buildComparisonTable"
    - from: "app/sca/page.tsx"
      to: "lib/logger.ts"
      via: "logError on caught query failure, with generic user-facing copy"
      pattern: "logError"
---

<objective>
Replace the Phase 9 placeholder body of `/sca` with the real Dashboard, and add the Competitions nav entry.

Purpose: This is the phase's headline surface — all three DASH requirements land here, wired from the Wave 2 models and Wave 3 components. It also closes Phase 9's WR-02 finding on this exact file: the placeholder currently renders `error.message` straight to the visitor.
Output: rewritten `app/sca/page.tsx`, one-line edit to `components/sca/ScaNavBar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-CONTEXT.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-UI-SPEC.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-PATTERNS.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-04-SUMMARY.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-05-SUMMARY.md

<interfaces>
From `lib/sca/queries.ts` (Plan 10-04, server-only):
```typescript
export async function getAllCooksWithScores(): Promise<CookWithScore[]>;  // ordered by cooked_at ascending, throws on Supabase error
```

From `lib/sca/aggregates.ts` (Plan 10-02) and `lib/sca/insights.ts`:
```typescript
export function computeSummaryStats(cooks: CookWithScore[]): SummaryStats;
export function computeWhatStandsOut(cooks: CookWithScore[]): Insight[];
```

From `lib/sca/comparison.ts` (Plan 10-03):
```typescript
export function buildComparisonTable(cooks: CookWithScore[], options: { aggregates: boolean }): ComparisonTableModel;
```

From `components/sca/*` (Plan 10-05):
```typescript
export function ComparisonTable(props: { model: ComparisonTableModel; caption?: string }): JSX.Element;
export function SummaryCards(props: { stats: SummaryStats }): JSX.Element;
export function WhatStandsOut(props: { insights: Insight[] }): JSX.Element | null;
```

From `lib/logger.ts`:
```typescript
export function logError(message: string, error: unknown, requestId?: string): void;
```

Current `components/sca/ScaNavBar.tsx:7` (the exact line to change):
```typescript
const scaNavLinks = [{ label: "Dashboard", href: "/sca" }] as const;
```
`isActive()` already handles any href via its `pathname.startsWith(href + "/")` fallback, so nothing else in that file changes.
</interfaces>

<ui_contract>
Locked by `10-UI-SPEC.md`:
- Page `<h1>`: "Dashboard", Display size `text-4xl` / 600, `font-[var(--font-display)]`.
- Section `<h2>`s: Heading size `text-2xl` / 600, `font-[var(--font-display)]`. No `md:text-3xl` bump.
- Page section order (Visual Hierarchy — the closest-gap insight card is the above-the-fold focal point): h1 → What Stands Out → Summary Cards → Comparison Table.
- Vertical rhythm between stacked sections: 32px (`mt-8` / `space-y-8`). Page container: existing `section-spacing` class.
- Error copy, verbatim: "We couldn't load this page right now. Please try again in a moment."
- Empty copy when zero cooks exist, verbatim: heading "No competitions recorded yet." + body "Check back after Big Matt's next SCA cookoff."
</ui_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Competitions entry to ScaNavBar (D-11)</name>
  <files>components/sca/ScaNavBar.tsx</files>
  <read_first>
    - components/sca/ScaNavBar.tsx (current file — confirm the scaNavLinks const and the isActive fallback before editing)
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-CONTEXT.md (D-11 — the exact array entry shape)
  </read_first>
  <action>
Change the `scaNavLinks` const so it contains two entries, in this order: `{ label: "Dashboard", href: "/sca" }` then `{ label: "Competitions", href: "/sca/competitions" }`. Keep the `as const` assertion.

Change nothing else in this file. In particular do not touch `isActive` — its `pathname === href || pathname.startsWith(href + "/")` branch already highlights `Competitions` for both `/sca/competitions` and `/sca/competitions/12`, and its `href === "/sca"` special case already prevents the Dashboard link from staying active on competition routes.

Per D-11, Cook Detail remains drill-down-only — do NOT add a `/sca/cooks` nav entry.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"/sca/competitions"' components/sca/ScaNavBar.tsx` returns 1
    - `grep -c '"/sca/cooks"' components/sca/ScaNavBar.tsx` returns 0
    - `grep -c 'label: "Dashboard"' components/sca/ScaNavBar.tsx` returns 1
    - `git diff --stat components/sca/ScaNavBar.tsx` shows changes confined to this one file with fewer than 8 changed lines
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>`scaNavLinks` contains exactly the Dashboard and Competitions entries; no other line in the file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app/sca/page.tsx as the real Dashboard</name>
  <files>app/sca/page.tsx</files>
  <read_first>
    - app/sca/page.tsx (current placeholder — copy its `export const dynamic = "force-dynamic"`, async Server Component shape, and try/catch; do NOT copy its `errorMessage = error.message` line, which is WR-02)
    - lib/sca/queries.ts (getAllCooksWithScores signature and throw behavior)
    - lib/sca/aggregates.ts (computeSummaryStats)
    - lib/sca/insights.ts (computeWhatStandsOut)
    - lib/sca/comparison.ts (buildComparisonTable options)
    - components/sca/SummaryCards.tsx, components/sca/WhatStandsOut.tsx, components/sca/ComparisonTable.tsx (prop contracts)
    - lib/logger.ts (logError signature)
    - .planning/phases/09-foundation-subdomain-routing/09-REVIEW.md (WR-02 — the exact mistake this rewrite must not repeat)
  </read_first>
  <action>
Rewrite `app/sca/page.tsx` end to end. Keep `export const dynamic = "force-dynamic";` and the default-exported async Server Component (Next.js page convention — this is the one place a default export is correct). Use relative imports (`../../lib/...`, `../../components/...`) per project convention; there are no path aliases.

Structure:
1. Declare `let cooks: CookWithScore[] = []` and `let errorMessage: string | undefined`.
2. `try { cooks = await getAllCooksWithScores(); } catch (error) { logError("ScaDashboardPage cooks query failed", error, "sca-dashboard-ssr"); errorMessage = "We couldn't load this page right now. Please try again in a moment."; }`. The literal generic string is the locked UI-SPEC error copy. Never assign or render `error.message` — that is the WR-02 regression this task closes on this exact file.
3. Compute view models outside the try: `const stats = computeSummaryStats(cooks); const insights = computeWhatStandsOut(cooks); const model = buildComparisonTable(cooks, { aggregates: true });` — `aggregates: true` is what produces DASH-02's Worst Cook / Best Cook / Cook Averages columns.
4. Render inside `<div className="section-spacing">`:
   - `<h1>` "Dashboard" at Display size, plus one `text-sm text-smoke-800` sub-line describing the tracker.
   - If `errorMessage` is set, render a single `.glass-card p-6` containing the generic message and return early — do not also render empty tables.
   - Else if `cooks.length === 0`, render a single `.glass-card p-6` with the locked empty copy: heading "No competitions recorded yet." and body "Check back after Big Matt's next SCA cookoff."
   - Else render, in this order with 32px vertical rhythm: `<WhatStandsOut insights={insights} />`, then a "Summary" section with `<SummaryCards stats={stats} />`, then a "Comparison" section with an `<h2>` "Comparison Table" and `<ComparisonTable model={model} caption="All recorded cooks compared side by side" />`. What-stands-out comes first because the closest-gap card is the page's declared focal point.
5. Delete the placeholder's competition-count card and its "Dashboard, Competitions, Analytics, and AI Reviews arrive in the next phases." trailing paragraph.

Per D-02 the table shows ALL recorded cooks — do not slice, paginate, or cap the list. Per D-07 no `chef_id` filter is applied anywhere.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes successfully
    - `npx tsc --noEmit` exits 0
    - `grep -c "error.message\|errorMessage = error" app/sca/page.tsx` returns 0 (WR-02 closed)
    - `grep -c "We couldn't load this page right now. Please try again in a moment." app/sca/page.tsx` returns 1
    - `grep -c "No competitions recorded yet." app/sca/page.tsx` returns 1 and `grep -c "Check back after Big Matt's next SCA cookoff." app/sca/page.tsx` returns 1
    - `grep -c "logError" app/sca/page.tsx` returns 1
    - `grep -c "aggregates: true" app/sca/page.tsx` returns 1
    - `grep -c "SummaryCards\|WhatStandsOut\|ComparisonTable" app/sca/page.tsx` returns 6 (three imports, three usages)
    - `grep -c 'export const dynamic = "force-dynamic"' app/sca/page.tsx` returns 1
    - `grep -c "arrive in the next phases" app/sca/page.tsx` returns 0 (placeholder copy removed)
    - `grep -Ec "\.slice\(|\.splice\(|chef_id" app/sca/page.tsx` returns 0 (all cooks shown, no chef filter — D-02, D-07)
    - `grep -c "use client" app/sca/page.tsx` returns 0
  </acceptance_criteria>
  <done>`/sca` renders the what-stands-out section, five summary cards, and the full aggregate comparison table from live data, with generic error copy and locked empty-state copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → `/sca` SSR render | Anonymous request triggers a service-role Supabase read; the response body is the only channel back to the visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Information Disclosure | `app/sca/page.tsx` catch block | mitigate | Replace the Phase 9 WR-02 pattern: `logError()` server-side with the raw error, render only the locked generic string. Gated by `grep -c "error.message" == 0`. |
| T-10-18 | Tampering (stored XSS) | Dashboard render tree | mitigate | Every database-derived string reaches the DOM through JSX text interpolation via the Wave 3 components; no `dangerouslySetInnerHTML` anywhere in the page or its children |
| T-10-19 | Elevation of Privilege | server-only client leaking to browser | mitigate | The page has no `"use client"` directive and imports `lib/sca/queries.ts`, which carries `import "server-only"` — any client-boundary violation fails the build |
| T-10-20 | Denial of Service | Dashboard with zero or all-unscored cooks | mitigate | `computeSummaryStats`, `computeWhatStandsOut`, and `buildComparisonTable` are all empty-set safe (Plans 10-02/10-03), so an empty dataset renders the empty state rather than throwing during SSR |
| T-10-SC | Tampering | npm installs | accept | No package-manager installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npm run build` succeeds
- `npx tsc --noEmit` exits 0
- `npm run test` (full suite) green
- `grep -rn "error.message" app/sca/` returns nothing
- `npm run dev` and load `/sca`: summary cards, insight cards, and the comparison table all render with real values; nav shows Dashboard and Competitions
</verification>

<success_criteria>
- `/sca` satisfies DASH-01 (five summary cards), DASH-02 (all-cook comparison table with the three aggregate columns), and DASH-03 (data-driven insight cards)
- Query failure renders only the locked generic message; the raw error appears solely in server logs
- Zero-cook dataset renders the locked empty-state copy instead of an empty table
- `ScaNavBar` links to `/sca` and `/sca/competitions` only
</success_criteria>

<output>
Create `.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-06-SUMMARY.md` when done
</output>
