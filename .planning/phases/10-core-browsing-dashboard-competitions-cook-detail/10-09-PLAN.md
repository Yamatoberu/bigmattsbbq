---
phase: 10-core-browsing-dashboard-competitions-cook-detail
plan: 09
type: execute
wave: 5
depends_on: ["10-06", "10-07", "10-08"]
files_modified: []
autonomous: false
requirements: [DASH-01, DASH-02, DASH-03, COMP-01, COMP-02, COMP-03, COOK-01, COOK-02]

must_haves:
  truths:
    - "A human has confirmed all four SCA browsing pages render correctly against live Supabase data"
    - "The documented sparse-data cases (competition 4 / cook 7, and a cook with no cook_detail or AI reviews) render their fallback states rather than errors"
    - "The storefront at bigmattsbbq.com is unchanged by this phase"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the Phase 10 browsing surface against live data.

Purpose: The repo's Vitest config runs in `environment: "node"` with no jsdom or React Testing Library, so page rendering has zero automated coverage by design (10-VALIDATION.md, Manual-Only Verifications). Every `lib/sca/*` function is unit-tested; what remains unproven is that the pages wire those models together correctly and that the sparse-data paths — which are the MAJORITY case in the real data — actually render.
Output: human approval, or a defect list for gap-closure planning.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-VALIDATION.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-UI-SPEC.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-06-SUMMARY.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-07-SUMMARY.md
@.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full automated gate and collect the sparse-data ids</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-VALIDATION.md (the binding sampling contract and manual-only list)
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-RESEARCH.md (the live-data facts: competition 4 / cook 7 have no score; only cooks 19 and 20 have cook_detail and cook_ai_review rows)
  </read_first>
  <action>
Run the phase gate and prepare the exact URLs the human will click, so the checkpoint below contains real ids rather than placeholders.

1. Run `npm run test` (full suite) and record the pass count.
2. Run `npx tsc --noEmit` and `npm run build`, recording that both exit 0.
3. Run `grep -rn "dangerouslySetInnerHTML" app/sca components/sca lib/sca` and confirm no matches (the phase-wide XSS gate from every plan's threat model).
4. Run `grep -rn "error\.message" app/sca` and confirm no matches (the WR-02 gate).
5. Start the dev server (`npm run dev`) in the background.
6. Confirm the four HTTP status expectations with curl against `http://localhost:3000`: `/sca` → 200, `/sca/competitions` → 200, `/sca/competitions/abc` → 404, `/sca/cooks/999999` → 404.
7. Using the sca Supabase client (or the Supabase MCP tools), re-confirm the current sparse-data ids rather than trusting the research snapshot: which competition has cooks with no `score` row, and which cook ids have no `cook_detail` and no `cook_ai_review`. Record one competition id and one cook id for each sparse case, plus one fully-populated cook id.

Report all findings inline; do not write a report file.
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 with every `tests/sca-*.test.ts` file passing
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 and its route table lists `/sca`, `/sca/competitions`, `/sca/competitions/[id]`, and `/sca/cooks/[id]`
    - `grep -rn "dangerouslySetInnerHTML" app/sca components/sca lib/sca` produces no output
    - `grep -rn "error\.message" app/sca` produces no output
    - curl status codes are 200, 200, 404, 404 for the four URLs above
    - A concrete competition id (zero-scored cook) and two concrete cook ids (one sparse, one fully populated) are reported for use in Task 2
  </acceptance_criteria>
  <done>All automated gates pass and the sparse-data ids for the manual pass are confirmed against live data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the four SCA browsing pages</name>
  <files>(no files modified — human verification only)</files>
  <read_first>
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-UI-SPEC.md (the locked copy strings, focal points, and typography the human is checking against)
    - .planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-VALIDATION.md (Manual-Only Verifications table — the binding manual contract)
  </read_first>
  <action>
Present the `<what-built>` summary and the `<how-to-verify>` steps below to the developer, substituting the concrete competition and cook ids collected in Task 1 for every `{id}` placeholder. Keep the dev server running for the duration. Block until the developer responds with "approved" or a defect list; do not self-approve and do not proceed on `workflow.auto_advance`. Record the verdict verbatim in the plan summary.
  </action>
  <verify>
    <automated>MISSING — page rendering has no automated coverage (vitest environment is "node" with no jsdom/React Testing Library, per 10-VALIDATION.md); Task 1's `npm run test && npx tsc --noEmit && npm run build` gate is the automated substitute and human sign-off covers the render surface</automated>
    <human-check>Developer confirms all six verification groups below and replies "approved"</human-check>
  </verify>
  <what-built>
Phase 10 shipped the SCA Tracker's browsing surface, all rendered server-side from live Supabase `sca` data:
- `/sca` — Dashboard: what-stands-out insight cards, five summary cards (latest cooks, best cook, worst cook, average total score, average gap to first), and a comparison table with every recorded cook as a named column plus Worst Cook / Best Cook / Cook Averages
- `/sca/competitions` — all competitions, newest first, with city/state/organizer shown only when present
- `/sca/competitions/[id]` — event metadata plus the same comparison table scoped to that event's cooks
- `/sca/cooks/[id]` — steak label, competition link, full score breakdown, process variables, and AI review history
- `ScaNavBar` now links to Dashboard and Competitions
- `app/sca/not-found.tsx` renders an on-brand 404 inside the SCA shell

Automated coverage: 5 new `lib/sca` modules with unit tests (aggregates, insights, comparison, cook-detail fields, queries/id-validation). Page rendering has no automated coverage — the repo's Vitest runs in a node environment with no React rendering infrastructure — which is why this manual pass exists.
  </what-built>
  <how-to-verify>
Start the dev server with `npm run dev` and open `http://localhost:3000`. Use the ids reported by Task 1 where indicated.

**1. Dashboard — `/sca`**
   - The what-stands-out cards appear first and describe real numbers (a gap in points, a score swing, a placement change) — not generic sentences. The closest-gap card's number is gold.
   - Five summary cards show real values; nothing reads `NaN`, `Infinity`, `undefined`, or `0` where a value is genuinely missing (missing values must be an em dash `—`).
   - The comparison table has one column per recorded cook, headed `<competition name> - <steak label>`, plus Worst Cook, Best Cook, and Cook Averages at the right. Only the Best Cook column is accent-colored.
   - Rows read, top to bottom: Competition, Cook, Cook Placement, Appearance, Doneness, Texture, Taste, Overall Impression, Total Score, Distance From Winning, Distance From Perfect Score.
   - Narrow the browser to phone width: the table scrolls sideways in its own container; it does NOT stack into per-cook cards and does not push the page horizontally.
   - Click a cook column header — it navigates to that cook's detail page.

**2. Competitions list — `/sca/competitions`**
   - The most recent competition is first; ordering is by event date, newest to oldest.
   - Event dates are correct — spot-check one against the database. A competition dated the 1st must NOT display as the previous month's last day.
   - Rows show city/state when present. Nothing displays an empty "City:" label, a stray middot, or a dash placeholder for a missing field.
   - "View Competition" navigates to the detail page.

**3. Competition detail — `/sca/competitions/{id}`**
   - Open a normal competition: metadata shows only the fields that have values, and the comparison table lists every cook entered at that event.
   - Open the zero-scored-cook competition id from Task 1: the page loads, and its Worst Cook / Best Cook / Cook Averages columns show em dashes rather than `NaN`, `0`, or an error.

**4. Cook detail — `/sca/cooks/{id}`**
   - Open the SPARSE cook id from Task 1: the Process Variables section reads exactly "No process detail recorded for this cook." and the AI Reviews section is present and reads exactly "No AI reviews yet." Neither section is hidden.
   - Open the FULLY POPULATED cook id from Task 1: real process variables are listed, and at least one AI review renders with its model/type/date and full comment text. Line breaks inside the comment are preserved.
   - The competition name links back to that competition's detail page.

**5. Errors and 404s**
   - Visit `/sca/cooks/abc` and `/sca/competitions/999999`: both show the on-brand "Not found" page inside the SCA nav/footer shell — not a raw Next.js error, not a stack trace, not a Supabase error string.

**6. Storefront non-regression**
   - Visit `/`, `/catering`, and `/about`: the storefront renders exactly as before, with the storefront nav bar (not the SCA nav bar).
  </how-to-verify>
  <resume-signal>Type "approved" if every check passes, or list the specific failures (page, what you expected, what you saw).</resume-signal>
  <acceptance_criteria>
    - All six verification groups are explicitly confirmed by the human
    - No page displays `NaN`, `Infinity`, `undefined`, `[object Object]`, or a raw Supabase/Postgres error string
    - Both locked fallback strings render verbatim on the sparse cook: "No process detail recorded for this cook." and "No AI reviews yet."
    - The comparison table's eleven rows appear in the required order with the required labels
    - Storefront pages are visually unchanged
  </acceptance_criteria>
  <done>The human types "approved", or a defect list is captured for gap-closure planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → all four SCA pages | Final end-to-end check that no server-side detail escapes to the rendered response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-31 | Information Disclosure | all Phase 10 pages | mitigate | Verification step 5 explicitly requires that no raw Supabase/Postgres error text or stack trace is visible on any error or 404 path; Task 1's `grep -rn "error\.message" app/sca` gate proves it structurally |
| T-10-32 | Tampering (stored XSS) | AI review and note text | mitigate | Task 1's repo-wide `dangerouslySetInnerHTML` gate must return no matches; Task 2 confirms review text renders as literal escaped text with preserved line breaks |
| T-10-33 | Tampering | `[id]` route params | mitigate | Verification step 5 exercises a non-numeric and an out-of-range id on both dynamic routes, confirming `parseScaId` + `notFound()` behave in the running app, not just in unit tests |
| T-10-SC | Tampering | npm installs | accept | No package-manager installs in this phase (RESEARCH.md Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `npm run test`, `npx tsc --noEmit`, and `npm run build` all exit 0
- No `dangerouslySetInnerHTML` and no `error.message` under `app/sca`, `components/sca`, `lib/sca`
- Human approval recorded for all six verification groups
</verification>

<success_criteria>
- All eight Phase 10 requirements (DASH-01/02/03, COMP-01/02/03, COOK-01/02) confirmed true against live data by a human
- Sparse-data paths verified against real sparse records, not only the fully-populated ones
- Storefront non-regression confirmed
- Any defects captured as a concrete list for `/gsd:plan-phase 10 --gaps`
</success_criteria>

<output>
Create `.planning/phases/10-core-browsing-dashboard-competitions-cook-detail/10-09-SUMMARY.md` when done
</output>
