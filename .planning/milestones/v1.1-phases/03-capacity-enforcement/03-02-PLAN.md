---
phase: 03-capacity-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/database.types.ts
  - lib/env.ts
  - tests/supabase.test.ts
  - .planning/phases/02-drop-config-storefront/02-VERIFICATION.md
  - .planning/phases/02-drop-config-storefront/02-03-SUMMARY.md
  - .planning/phases/02-drop-config-storefront/02-05-SUMMARY.md
  - .planning/phases/01-foundation/01-VALIDATION.md
autonomous: true
requirements: [DATA-03, ORD-05]

must_haves:
  truths:
    - "place_preorder RPC type has p_drop_id and p_pickup_id typed as string (UUID), not number"
    - "getSupabaseEnv is no longer exported from lib/env.ts"
    - "tests/supabase.test.ts no longer imports or tests getSupabaseEnv — getSupabaseClient tests remain intact"
    - "Phase 2 VERIFICATION.md reflects plans 02-04 and 02-05 completion"
    - "Phase 2 summaries 02-03 and 02-05 have requirements-completed frontmatter"
    - "Phase 1 VALIDATION.md has nyquist_compliant: true and task statuses updated"
  artifacts:
    - path: "lib/database.types.ts"
      provides: "Corrected place_preorder Args types"
      contains: "p_drop_id: string"
    - path: "lib/env.ts"
      provides: "Clean env module without dead getSupabaseEnv export"
    - path: "tests/supabase.test.ts"
      provides: "getSupabaseClient tests only — getSupabaseEnv describe block removed"
  key_links:
    - from: "lib/database.types.ts"
      to: "supabase RPC calls"
      via: "TypeScript type inference"
      pattern: "p_drop_id: string"
---

<objective>
Fix the place_preorder type mismatch, remove dead getSupabaseEnv code, and update planning artifacts to close all remaining Phase 3 gap-closure items.

Purpose: Resolve blocking type errors, clean up technical debt, and bring planning documents to accurate state.
Output: Corrected types, clean env module, updated verification/validation/summary documents.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capacity-enforcement/03-CONTEXT.md

<interfaces>
<!-- Key types the executor needs to understand the type fix -->

From lib/database.types.ts (lines 215-226 — CURRENT, WRONG):
```typescript
place_preorder: {
  Args: {
    p_drop_id: number      // WRONG — must be string (UUID)
    p_email: string
    p_full_name: string
    p_items: Json
    p_opt_in?: boolean
    p_phone?: string
    p_pickup_id: number    // WRONG — must be string (UUID)
  }
  Returns: Json
}
```

From lib/database.types.ts (lines 236-244 — CORRECT reference):
```typescript
reserve_pickup_slot: {
  Args: {
    p_drop_id: string      // correct — UUID
    p_pickup_option_id: string
    p_product_name: string
    p_quantity: number
  }
  Returns: Json
}
```

From lib/env.ts (lines 39-50 — to be REMOVED):
```typescript
export function getSupabaseEnv(): SupabaseEnv {
  const url = process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!url || !serviceRoleKey) {
    throw new Error(
      "Missing Supabase environment variables. Check SUPABASE_URL (or NEXT_PUBLIC_SUPABASE_URL) and SUPABASE_SERVICE_ROLE_KEY."
    );
  }
  return { url, serviceRoleKey };
}
```

From tests/supabase.test.ts:
- Line 2: `import { getSupabaseEnv } from "../lib/env";` — REMOVE this import
- Lines 62-93: `describe("getSupabaseEnv", ...)` block — REMOVE entire block
- Lines 7-60: `describe("getSupabaseClient", ...)` block — KEEP intact
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix place_preorder types and remove dead getSupabaseEnv code</name>
  <files>lib/database.types.ts, lib/env.ts, tests/supabase.test.ts</files>
  <read_first>
    - lib/database.types.ts (full file — locate place_preorder Args block at lines 215-226)
    - lib/env.ts (full file — locate getSupabaseEnv function at lines 39-50 and SupabaseEnv interface at lines 34-37)
    - tests/supabase.test.ts (full file — locate getSupabaseEnv import at line 2 and describe block at lines 62-93)
    - lib/supabase.ts (confirm it does NOT import getSupabaseEnv — reads process.env directly)
  </read_first>
  <action>
**1. Fix `lib/database.types.ts` — place_preorder Args (lines 215-226):**

Change `p_drop_id: number` to `p_drop_id: string` (line 217).
Change `p_pickup_id: number` to `p_pickup_id: string` (line 223).

All other fields in place_preorder Args remain unchanged. The corrected block:
```typescript
place_preorder: {
  Args: {
    p_drop_id: string
    p_email: string
    p_full_name: string
    p_items: Json
    p_opt_in?: boolean
    p_phone?: string
    p_pickup_id: string
  }
  Returns: Json
}
```

**2. Remove dead code from `lib/env.ts`:**

Delete the `SupabaseEnv` interface (lines 34-37):
```typescript
export interface SupabaseEnv {
  url: string;
  serviceRoleKey: string;
}
```

Delete the `getSupabaseEnv()` function (lines 39-50):
```typescript
export function getSupabaseEnv(): SupabaseEnv {
  // ... entire function body
}
```

Keep `getSquareEnv()` and `SquareEnv` interface intact. The file should end after the `getSquareEnv()` function's closing brace.

**3. Update `tests/supabase.test.ts`:**

Remove the import of `getSupabaseEnv` from line 2:
```typescript
// DELETE: import { getSupabaseEnv } from "../lib/env";
```

Remove the entire `describe("getSupabaseEnv", ...)` block (lines 62-93).

Keep the `describe("getSupabaseClient", ...)` block (lines 7-60) completely intact — do not modify it.

Keep the vitest imports on line 1 unchanged: `import { afterEach, describe, expect, it, vi } from "vitest";`
  </action>
  <verify>
    <automated>npm run build && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - lib/database.types.ts contains `p_drop_id: string` inside place_preorder Args (not `number`)
    - lib/database.types.ts contains `p_pickup_id: string` inside place_preorder Args (not `number`)
    - lib/env.ts does NOT contain `getSupabaseEnv`
    - lib/env.ts does NOT contain `SupabaseEnv`
    - lib/env.ts contains `getSquareEnv` (unchanged)
    - tests/supabase.test.ts does NOT contain `getSupabaseEnv`
    - tests/supabase.test.ts contains `describe("getSupabaseClient"` (kept intact)
    - `npm run build` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>place_preorder types fixed to string (UUID). getSupabaseEnv removed from env.ts and its test block removed from supabase.test.ts. Build and all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 1 and Phase 2 planning artifacts</name>
  <files>
    .planning/phases/02-drop-config-storefront/02-VERIFICATION.md,
    .planning/phases/02-drop-config-storefront/02-03-SUMMARY.md,
    .planning/phases/02-drop-config-storefront/02-05-SUMMARY.md,
    .planning/phases/01-foundation/01-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/02-drop-config-storefront/02-VERIFICATION.md (current status: gaps_found — needs update)
    - .planning/phases/02-drop-config-storefront/02-03-SUMMARY.md (missing requirements-completed frontmatter)
    - .planning/phases/02-drop-config-storefront/02-05-SUMMARY.md (missing requirements-completed frontmatter)
    - .planning/phases/01-foundation/01-VALIDATION.md (nyquist_compliant: false — needs update)
  </read_first>
  <action>
**1. Update `02-VERIFICATION.md`:**

Change frontmatter `status: gaps_found` to `status: complete`.
Change frontmatter `score: 2/4 roadmap success criteria verified` to `score: 4/4 roadmap success criteria verified`.
For each gap entry in the `gaps:` frontmatter array, change `status: failed` to `status: resolved` and add `resolved_by: "Phase 2 plans 02-04 and 02-05; remaining capacity enforcement moved to Phase 3"`.

**2. Update `02-03-SUMMARY.md`:**

Add `requirements-completed: [ORD-04]` to the YAML frontmatter block (after the `key-files:` section). ORD-04 is "Checkout validates that the drop is active before accepting orders" — this is what plan 02-03 implemented (checkDropReady).

**3. Update `02-05-SUMMARY.md`:**

Add `requirements-completed: [DATA-05, ORD-04]` to the YAML frontmatter block. DATA-05 is "Drop pickup options stored in Supabase and replace hardcoded config" and ORD-04 is "Checkout validates drop is active" — both were completed by plan 02-05's checkout migration.

**4. Update `01-VALIDATION.md`:**

Change frontmatter `nyquist_compliant: false` to `nyquist_compliant: true`.
Change frontmatter `wave_0_complete: false` to `wave_0_complete: true`.
Change frontmatter `status: draft` to `status: complete`.
In the Per-Task Verification Map, change all task statuses from `pending` to `green` (the underlying tests and files all exist and pass).
In the Validation Sign-Off section, check all checkboxes (change `- [ ]` to `- [x]`).
Change `**Approval:** pending` to `**Approval:** approved`.
  </action>
  <verify>
    <automated>grep -c "status: complete" .planning/phases/02-drop-config-storefront/02-VERIFICATION.md && grep -c "requirements-completed" .planning/phases/02-drop-config-storefront/02-03-SUMMARY.md && grep -c "requirements-completed" .planning/phases/02-drop-config-storefront/02-05-SUMMARY.md && grep -c "nyquist_compliant: true" .planning/phases/01-foundation/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 02-VERIFICATION.md frontmatter contains `status: complete`
    - 02-VERIFICATION.md frontmatter contains `score: 4/4`
    - 02-03-SUMMARY.md frontmatter contains `requirements-completed: [ORD-04]`
    - 02-05-SUMMARY.md frontmatter contains `requirements-completed:`
    - 01-VALIDATION.md frontmatter contains `nyquist_compliant: true`
    - 01-VALIDATION.md frontmatter contains `wave_0_complete: true`
    - 01-VALIDATION.md frontmatter contains `status: complete`
    - 01-VALIDATION.md contains `**Approval:** approved`
  </acceptance_criteria>
  <done>All four planning artifacts updated. Phase 2 verification reflects completion, summaries have requirements-completed frontmatter, Phase 1 validation is marked compliant and approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan modifies types, removes dead code, and updates planning documents — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | lib/database.types.ts | accept | Type file is auto-generated reference; changing number to string aligns with live schema — no runtime behavior change, only type safety improvement |
| T-03-06 | Information Disclosure | lib/env.ts | mitigate | Removing getSupabaseEnv reduces exported surface area — fewer functions that handle credentials are exposed |
</threat_model>

<verification>
1. `npm run build` — TypeScript compilation succeeds with corrected types
2. `npm run test` — all tests pass (getSupabaseClient tests intact, getSupabaseEnv tests removed)
3. `grep "p_drop_id: string" lib/database.types.ts` — confirms type fix
4. `grep -c "getSupabaseEnv" lib/env.ts` — returns 0
5. `grep -c "getSupabaseEnv" tests/supabase.test.ts` — returns 0
6. `grep "requirements-completed" .planning/phases/02-drop-config-storefront/02-03-SUMMARY.md` — present
7. `grep "nyquist_compliant: true" .planning/phases/01-foundation/01-VALIDATION.md` — present
</verification>

<success_criteria>
- place_preorder types corrected from number to string (UUID)
- getSupabaseEnv function and interface removed from lib/env.ts
- getSupabaseEnv test block removed; getSupabaseClient tests unaffected
- Phase 2 VERIFICATION.md status updated to complete
- Phase 2 summaries have requirements-completed frontmatter
- Phase 1 VALIDATION.md marked nyquist_compliant with all tasks green
- Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-capacity-enforcement/03-02-SUMMARY.md`
</output>
