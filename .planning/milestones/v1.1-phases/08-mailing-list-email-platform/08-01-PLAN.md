---
phase: 08-mailing-list-email-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - lib/env.ts
  - lib/unsubscribeToken.ts
  - tests/unsubscribeToken.test.ts
  - app/api/unsubscribe/route.ts
  - app/unsubscribe/page.tsx
autonomous: true
requirements: []
tags:
  - mailing-list
  - email
  - cleanup
  - resend
user_setup:
  - service: resend
    why: "Broadcast targets a Resend Segment; segment ID must be obtained from the Resend dashboard before /api/admin/broadcast can be called."
    env_vars:
      - name: RESEND_SEGMENT_ID
        source: "Resend Dashboard -> Audiences -> Segments. Create a segment that includes all subscribers, copy its ID."
    dashboard_config:
      - task: "Create a segment in the Resend audience that should receive drop broadcasts"
        location: "Resend Dashboard -> Audiences -> (your audience) -> Segments -> New segment"

must_haves:
  truths:
    - "package.json no longer lists jose, sanitize-html, or @types/sanitize-html"
    - "package.json lists @react-email/components and @react-email/render"
    - "lib/unsubscribeToken.ts does not exist"
    - "app/api/unsubscribe/route.ts does not exist"
    - "app/unsubscribe/page.tsx does not exist"
    - "tests/unsubscribeToken.test.ts does not exist"
    - ".env.example contains RESEND_SEGMENT_ID and does not contain UNSUBSCRIBE_SECRET"
    - "lib/env.ts exports getResendEnv() returning {apiKey, segmentId}"
    - "npm test runs (no broken imports for deleted lib/unsubscribeToken.ts)"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency manifest"
      contains: "@react-email/components"
    - path: "package.json"
      provides: "Updated dependency manifest (removals)"
      contains: "@react-email/render"
    - path: "lib/env.ts"
      provides: "getResendEnv() fail-fast helper"
      contains: "export function getResendEnv"
    - path: ".env.example"
      provides: "Updated environment template"
      contains: "RESEND_SEGMENT_ID="
  key_links:
    - from: "package.json"
      to: "node_modules/@react-email/components, node_modules/@react-email/render"
      via: "npm install resolves new dependencies"
      pattern: "@react-email/components.*@react-email/render"
    - from: "lib/env.ts"
      to: "process.env.RESEND_API_KEY, process.env.RESEND_SEGMENT_ID"
      via: "getResendEnv() reads env at call time"
      pattern: "process\\.env\\.RESEND_(API_KEY|SEGMENT_ID)"
---

<objective>
Prepare the codebase for Phase 8's Resend Contacts/Broadcasts migration by:
1. Installing React Email packages (`@react-email/components@^1.0.12`, `@react-email/render@^2.0.8`).
2. Removing the now-unused `jose`, `sanitize-html`, and `@types/sanitize-html` packages.
3. Deleting the custom JWT unsubscribe surface (`lib/unsubscribeToken.ts`, `tests/unsubscribeToken.test.ts`, `app/api/unsubscribe/route.ts`, `app/unsubscribe/page.tsx`) per D-09, D-10, D-11.
4. Adding `getResendEnv()` helper to `lib/env.ts` for fail-fast Resend env validation (mirrors `getSquareEnv()` pattern).
5. Updating `.env.example` to remove `UNSUBSCRIBE_SECRET` and add `RESEND_SEGMENT_ID` per D-04 (renamed from D-04's `RESEND_AUDIENCE_ID` per RESEARCH.md finding — Resend migrated audiences -> segments).

Purpose: Wave 1 cleanup unblocks Wave 2 route migration. Plan 02 creates the email template in parallel; Plan 03 (Wave 2) consumes both this plan's helper + package installs and Plan 02's component.

Output: Clean dependency tree, deleted unsubscribe surface, env helper ready for use, .env.example reflects the new contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-mailing-list-email-platform/08-CONTEXT.md
@.planning/phases/08-mailing-list-email-platform/08-RESEARCH.md
@.planning/phases/08-mailing-list-email-platform/08-PATTERNS.md

<interfaces>
<!-- Existing fail-fast env helper pattern (from lib/env.ts). New getResendEnv must mirror this exactly. -->

From lib/env.ts:
```typescript
export interface SquareEnv {
  host: string;
  accessToken: string;
  locationId: string;
  frozenCategoryId: string;
  sauceVariationId: string;
  environment: "sandbox" | "production";
}

export function getSquareEnv(): SquareEnv {
  // ... reads process.env, throws on missing required vars, returns typed object
}
```

From lib/supabase.ts (alternate inline guard pattern):
```typescript
const url = process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL;
const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
if (!url || !key) {
  throw new Error("Missing Supabase environment variables. Check ...");
}
```

NEW shape to add to lib/env.ts:
```typescript
export interface ResendEnv {
  apiKey: string;
  segmentId: string;
}

export function getResendEnv(): ResendEnv {
  const apiKey = process.env.RESEND_API_KEY;
  const segmentId = process.env.RESEND_SEGMENT_ID;
  if (!apiKey || !segmentId) {
    throw new Error(
      "Missing Resend environment variables. Check RESEND_API_KEY and RESEND_SEGMENT_ID."
    );
  }
  return { apiKey, segmentId };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies — install React Email packages, uninstall jose / sanitize-html</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (verify current dependency versions before changes)
    - .planning/phases/08-mailing-list-email-platform/08-RESEARCH.md (sections "Standard Stack" and "Packages to Remove" — verifies version pins)
  </read_first>
  <action>
    Run these npm commands in order from the repo root (`/Users/matt/Development/BigMattsBbq`):

    1. `npm install @react-email/components@^1.0.12 @react-email/render@^2.0.8`
    2. `npm uninstall jose sanitize-html`
    3. `npm uninstall --save-dev @types/sanitize-html`

    After each command, allow npm to update both `package.json` and `package-lock.json`.

    Verify package.json afterward:
    - `dependencies` contains `"@react-email/components": "^1.0.12"` and `"@react-email/render": "^2.0.8"`
    - `dependencies` does NOT contain `jose` or `sanitize-html`
    - `devDependencies` does NOT contain `@types/sanitize-html`
    - `dependencies.resend` is unchanged (still `^6.12.2`)

    Do NOT manually edit package.json — let npm do it. If npm reports peer-dependency warnings for React 18 from `@react-email/*`, that is acceptable and expected (React Email 1.x supports React 18.3.x).
  </action>
  <verify>
    <automated>node -e "const p=require('/Users/matt/Development/BigMattsBbq/package.json'); const ok = p.dependencies['@react-email/components'] && p.dependencies['@react-email/render'] && !p.dependencies.jose && !p.dependencies['sanitize-html'] && !(p.devDependencies||{})['@types/sanitize-html']; if(!ok){console.error('package.json deps incorrect',{addReact:!!p.dependencies['@react-email/components'],addRender:!!p.dependencies['@react-email/render'],keepResend:p.dependencies.resend,removeJose:!p.dependencies.jose,removeSanitize:!p.dependencies['sanitize-html'],removeSanitizeTypes:!(p.devDependencies||{})['@types/sanitize-html']});process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@react-email/components"' package.json` exits 0
    - `grep -q '"@react-email/render"' package.json` exits 0
    - `grep -q '"jose"' package.json` exits 1 (string absent)
    - `grep -q '"sanitize-html"' package.json` exits 1 (string absent)
    - `grep -q '"@types/sanitize-html"' package.json` exits 1 (string absent)
    - `grep -q '"resend"' package.json` exits 0 (still present)
    - `test -d node_modules/@react-email/components` exits 0
    - `test -d node_modules/@react-email/render` exits 0
    - `test ! -d node_modules/jose` exits 0 (directory removed)
    - `test ! -d node_modules/sanitize-html` exits 0 (directory removed)
  </acceptance_criteria>
  <done>
    React Email packages installed and resolved in node_modules; jose, sanitize-html, and @types/sanitize-html removed from manifest and node_modules; package-lock.json updated by npm; resend dependency untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getResendEnv() helper to lib/env.ts</name>
  <files>lib/env.ts</files>
  <read_first>
    - lib/env.ts (current file — see exact getSquareEnv pattern to mirror)
    - lib/supabase.ts (alternate fail-fast guard reference)
    - .planning/phases/08-mailing-list-email-platform/08-PATTERNS.md (section "lib/env.ts (utility/config)")
  </read_first>
  <action>
    Append (do NOT overwrite the existing `SquareEnv` / `getSquareEnv` exports) the following to `lib/env.ts`, after the existing `getSquareEnv` function and before the trailing newline:

    ```typescript

    export interface ResendEnv {
      apiKey: string;
      segmentId: string;
    }

    export function getResendEnv(): ResendEnv {
      const apiKey = process.env.RESEND_API_KEY;
      const segmentId = process.env.RESEND_SEGMENT_ID;

      if (!apiKey || !segmentId) {
        throw new Error(
          "Missing Resend environment variables. Check RESEND_API_KEY and RESEND_SEGMENT_ID."
        );
      }

      return { apiKey, segmentId };
    }
    ```

    Conventions to follow exactly:
    - Named export only (no default export) — matches every other lib/ file
    - PascalCase interface name with `Env` suffix (`ResendEnv`)
    - No JSDoc comments (matches existing file style)
    - Throw `Error` (not a custom class) with descriptive message naming both env vars
    - 2-space indentation, trailing newline (per `.editorconfig`)

    Do NOT modify the existing `SquareEnv` interface or `getSquareEnv` function.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const src=fs.readFileSync('/Users/matt/Development/BigMattsBbq/lib/env.ts','utf8'); const checks={hasInterface:/export interface ResendEnv\s*\{[^}]*apiKey:\s*string;[^}]*segmentId:\s*string;[^}]*\}/.test(src),hasFunction:/export function getResendEnv\(\):\s*ResendEnv/.test(src),readsApiKey:/process\.env\.RESEND_API_KEY/.test(src),readsSegmentId:/process\.env\.RESEND_SEGMENT_ID/.test(src),throwsOnMissing:/throw new Error\([^)]*Missing Resend/.test(src),keepsSquareEnv:/export function getSquareEnv\(\):\s*SquareEnv/.test(src)}; const ok=Object.values(checks).every(v=>v); if(!ok){console.error(checks);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export interface ResendEnv" lib/env.ts` exits 0
    - `grep -q "export function getResendEnv" lib/env.ts` exits 0
    - `grep -q "process.env.RESEND_API_KEY" lib/env.ts` exits 0
    - `grep -q "process.env.RESEND_SEGMENT_ID" lib/env.ts` exits 0
    - `grep -q "Missing Resend environment variables" lib/env.ts` exits 0
    - `grep -q "export function getSquareEnv" lib/env.ts` exits 0 (existing function preserved)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>
    `lib/env.ts` exports both `getSquareEnv` (unchanged) and a new `getResendEnv()` that throws when `RESEND_API_KEY` or `RESEND_SEGMENT_ID` is unset, returning `{ apiKey, segmentId }` on success. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete custom JWT unsubscribe surface (4 files) and update .env.example</name>
  <files>lib/unsubscribeToken.ts, tests/unsubscribeToken.test.ts, app/api/unsubscribe/route.ts, app/unsubscribe/page.tsx, .env.example</files>
  <read_first>
    - .env.example (current file — see exact "Phase 5" comment block to replace)
    - .planning/phases/08-mailing-list-email-platform/08-PATTERNS.md (section ".env.example Changes" — has the exact replacement block)
    - .planning/phases/08-mailing-list-email-platform/08-CONTEXT.md (D-09, D-10, D-11, D-12)
  </read_first>
  <action>
    **Step 1 — Delete files** (use shell `rm`; the four files exist and have been verified):

    ```bash
    rm /Users/matt/Development/BigMattsBbq/lib/unsubscribeToken.ts
    rm /Users/matt/Development/BigMattsBbq/tests/unsubscribeToken.test.ts
    rm /Users/matt/Development/BigMattsBbq/app/api/unsubscribe/route.ts
    rm /Users/matt/Development/BigMattsBbq/app/unsubscribe/page.tsx
    ```

    Then remove the now-empty directories:
    ```bash
    rmdir /Users/matt/Development/BigMattsBbq/app/api/unsubscribe
    rmdir /Users/matt/Development/BigMattsBbq/app/unsubscribe
    ```

    **Step 2 — Replace `.env.example` Phase 5 comment block** (lines 19-28 of current file):

    Replace this exact block:
    ```
    # Phase 5 — broadcast + unsubscribe
    # These two secrets MUST be independent values (each 32+ chars).
    # Rotation semantics differ:
    #   - BROADCAST_SECRET gates the admin broadcast API; rotate freely.
    #   - UNSUBSCRIBE_SECRET signs unsubscribe JWTs with a 30-day expiry;
    #     rotating it invalidates every outstanding unsubscribe link.
    # Do NOT reuse one for the other.
    BROADCAST_SECRET=
    UNSUBSCRIBE_SECRET=
    ```

    With this exact block (per D-12, plus D-04 with `RESEND_SEGMENT_ID` per RESEARCH.md naming correction):
    ```
    # Phase 5 — broadcast
    # BROADCAST_SECRET gates the admin broadcast API. Rotate freely. Min 16 chars.
    BROADCAST_SECRET=

    # Phase 8 — Resend Contacts + Broadcasts
    # RESEND_SEGMENT_ID: obtain from Resend dashboard -> Audiences -> Segments.
    # Required for POST /api/admin/broadcast. Not needed for subscriber signups
    # (resend.contacts.create() is global, no segment ID required).
    # Unsubscribes are handled natively by Resend (List-Unsubscribe header) —
    # no UNSUBSCRIBE_SECRET is needed.
    RESEND_SEGMENT_ID=
    ```

    Do NOT modify any other lines of `.env.example` (Supabase, Resend API key, Square sections must remain unchanged).

    **Note on test file deletion (RESEARCH.md Pitfall 6):** `tests/unsubscribeToken.test.ts` imports `../lib/unsubscribeToken`. Deleting the source file without deleting the test file would break `npm test` with `ERR_MODULE_NOT_FOUND`. Both deletions are mandatory in this same task.

    **Note on no other consumers:** Verified via grep that no other source file imports `jose`, `sanitize-html`, `lib/unsubscribeToken`, `app/api/unsubscribe`, or `app/unsubscribe`. The broadcast route DOES still import `signUnsubscribeToken` and `sanitize-html` — those imports are removed in Plan 03 (Wave 2). For this plan, the broadcast route is not yet running through tests in this state — Plan 03 immediately follows in Wave 2 before any test run validates the full graph.
  </action>
  <verify>
    <automated>bash -c 'set -e; for f in lib/unsubscribeToken.ts tests/unsubscribeToken.test.ts app/api/unsubscribe/route.ts app/unsubscribe/page.tsx; do test ! -e "/Users/matt/Development/BigMattsBbq/$f" || { echo "still exists: $f"; exit 1; }; done; grep -q "RESEND_SEGMENT_ID=" /Users/matt/Development/BigMattsBbq/.env.example; grep -q "UNSUBSCRIBE_SECRET" /Users/matt/Development/BigMattsBbq/.env.example && { echo "UNSUBSCRIBE_SECRET still in .env.example"; exit 1; } || true; grep -q "BROADCAST_SECRET=" /Users/matt/Development/BigMattsBbq/.env.example; grep -q "RESEND_API_KEY=" /Users/matt/Development/BigMattsBbq/.env.example; echo OK'</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e lib/unsubscribeToken.ts` exits 0
    - `test ! -e tests/unsubscribeToken.test.ts` exits 0
    - `test ! -e app/api/unsubscribe/route.ts` exits 0
    - `test ! -e app/unsubscribe/page.tsx` exits 0
    - `test ! -d app/api/unsubscribe` exits 0
    - `test ! -d app/unsubscribe` exits 0
    - `grep -q "RESEND_SEGMENT_ID=" .env.example` exits 0
    - `grep -c "UNSUBSCRIBE_SECRET" .env.example` returns 0 (string entirely absent)
    - `grep -q "BROADCAST_SECRET=" .env.example` exits 0 (preserved)
    - `grep -q "RESEND_API_KEY=" .env.example` exits 0 (preserved)
    - `grep -q "NEXT_PUBLIC_SUPABASE_URL=" .env.example` exits 0 (preserved)
    - `grep -rn "from \"jose\"\|from 'jose'" /Users/matt/Development/BigMattsBbq/app /Users/matt/Development/BigMattsBbq/lib /Users/matt/Development/BigMattsBbq/components /Users/matt/Development/BigMattsBbq/tests` exits 1 (no remaining imports — D-11 satisfied at filesystem level; broadcast route still imports `signUnsubscribeToken` from a now-deleted module, fixed in Plan 03)
  </acceptance_criteria>
  <done>
    All four custom-unsubscribe files deleted; `tests/unsubscribeToken.test.ts` removed alongside `lib/unsubscribeToken.ts` (preventing the orphan-test breakage from RESEARCH.md Pitfall 6); `.env.example` updated to remove `UNSUBSCRIBE_SECRET` and add `RESEND_SEGMENT_ID` with explanatory comment; all unrelated env vars preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> repo | Deletions and dependency changes alter what code runs in production |
| process.env -> lib/env.ts | New `getResendEnv()` reads RESEND_API_KEY (high-value secret) and RESEND_SEGMENT_ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `lib/env.ts::getResendEnv()` returning RESEND_API_KEY | mitigate | Helper is server-side only (no `"use client"` directive in lib/); never imported into client components. Same pattern as existing `getSquareEnv()` which holds `SQUARE_ACCESS_TOKEN`. |
| T-08-02 | Tampering | `package.json` dependency removal could leave dangling import | mitigate | Acceptance criteria grep for any remaining `from "jose"` or `from "sanitize-html"` imports in app/, lib/, components/, tests/. Plan 03 (Wave 2) removes the broadcast route's residual `sanitize-html` and `signUnsubscribeToken` imports immediately. |
| T-08-03 | Denial of Service | Missing `RESEND_API_KEY` or `RESEND_SEGMENT_ID` causes broadcast 500 | accept | Fail-fast `getResendEnv()` throws a clear error; ops fix by setting env. Standard pattern across all our env helpers. |
| T-08-04 | Repudiation | Deleted unsubscribe routes — recipients who try old links get 404 | accept | Old custom JWT links predate Phase 8; Resend's native List-Unsubscribe replaces them. Per D-09, this is the explicit design. New broadcasts (post-deploy) use Resend's headers. |
| T-08-05 | Information Disclosure | `.env.example` is committed; ensure it stays placeholder-only | mitigate | Replacement block uses empty `=` values only — no secrets baked in. Existing convention preserved. |
</threat_model>

<verification>
After all three tasks complete:

1. `grep -rn "from \"jose\"\|from 'jose'" app/ lib/ components/ tests/` → exits 1 (no matches; broadcast route's `signUnsubscribeToken` import is broken-on-purpose until Plan 03 fixes it in the same wave).
2. `npx tsc --noEmit` MAY report errors in `app/api/admin/broadcast/route.ts` (missing `sanitize-html`, missing `lib/unsubscribeToken`). This is EXPECTED — Plan 03 in Wave 2 fixes those imports. Do NOT attempt to fix them in this plan.
3. `npm test` MAY skip / fail on `broadcast.test.ts` due to the broken route imports. This is EXPECTED. The test file is rewritten in Plan 03.
4. `mailingList.test.ts` should still PASS (route still imports Supabase — also fixed in Plan 03 — but its current imports are intact).
5. `.env.example` diff shows exactly the Phase 5/Phase 8 comment block swap; no other lines changed.
</verification>

<success_criteria>
- `package.json` lists `@react-email/components ^1.0.12` and `@react-email/render ^2.0.8`
- `package.json` does NOT list `jose`, `sanitize-html`, `@types/sanitize-html`
- `node_modules` reflects the install/uninstall (`@react-email/*` present, `jose`/`sanitize-html` absent)
- `lib/env.ts` exports `getResendEnv(): ResendEnv` alongside the unchanged `getSquareEnv`
- All four custom unsubscribe files deleted (lib + test + route + page); empty parent dirs removed
- `.env.example` removes `UNSUBSCRIBE_SECRET`, adds `RESEND_SEGMENT_ID`, preserves all other vars unchanged
- TypeScript compiles `lib/env.ts` cleanly (`getResendEnv` is well-formed)
- No remaining `from "jose"` or `from 'jose'` imports anywhere in `app/`, `lib/`, `components/`, `tests/`

Wave 2 (Plan 03) immediately follows and fixes the intentionally-broken broadcast route imports.
</success_criteria>

<output>
After completion, create `.planning/phases/08-mailing-list-email-platform/08-01-SUMMARY.md` per the standard summary template, including:
- Confirmation of dependency changes (npm view of new deps, removed deps)
- Confirmation of file deletions (4 files + 2 dirs)
- Diff snapshot of the .env.example change
- Note that broadcast route imports are intentionally broken until Plan 03
</output>
