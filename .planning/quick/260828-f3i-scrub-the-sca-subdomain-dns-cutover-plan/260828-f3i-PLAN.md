---
phase: quick-260828-f3i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sca-subdomain-deployment.md
  - README.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [QUICK-260828-f3i]

must_haves:
  truths:
    - "docs/sca-subdomain-deployment.md no longer exists"
    - "README.md describes the SCA Tracker without pending-cutover language and without a link to the deleted doc"
    - "PROJECT.md no longer lists DNS cutover as a next-milestone goal or an active requirement"
    - "PROJECT.md records that the subdomain was decided against and /sca is the canonical URL"
    - "STATE.md carries a dated decision entry explaining the scrub and why the shipped routing code stays"
    - "proxy.ts, lib/sca/routing.ts, and .env.example are byte-identical to HEAD"
  artifacts:
    - path: "README.md"
      provides: "SCA Tracker section describing /sca as the canonical path"
      contains: "app/sca"
    - path: ".planning/PROJECT.md"
      provides: "Next Milestone Goals, Active requirements, and Context with the DNS cutover item removed"
      contains: "Key Decisions"
    - path: ".planning/STATE.md"
      provides: "Appended decision entry recording the scrub"
      contains: "### Decisions"
  key_links:
    - from: "README.md"
      to: "proxy.ts / lib/sca/routing.ts"
      via: "prose describing host-based routing as dormant-but-shipped"
      pattern: "proxy\\.ts"
    - from: ".planning/PROJECT.md"
      to: "Key Decisions table"
      via: "new row recording the decision against the subdomain"
      pattern: "subdomain"
---

<objective>
Scrub the SCA subdomain DNS cutover plan from the repo's documentation and planning
artifacts. The subdomain was decided against — `/sca` is sufficient and is the canonical
URL for the SCA Tracker.

Purpose: The docs currently present `sca.bigmattsbbq.com` as an imminent, merely-pending
manual step. That is now false and will mislead a future reader (or a future planning pass)
into treating a dead item as outstanding work.

Output: `docs/sca-subdomain-deployment.md` deleted; `README.md`, `.planning/PROJECT.md`,
and `.planning/STATE.md` updated to reflect the decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@README.md
@docs/sca-subdomain-deployment.md

**Scope boundary — read this before editing anything.**

The *routing code* stays. `proxy.ts`, `lib/sca/routing.ts`, and the `SCA_HOSTNAME` env var
are shipped, tested Phase 9 infrastructure. They are inert when no `sca.*` host is bound
(`resolveScaRouting` simply does not match), so they cost nothing and are harmless to leave
in place. Only the DNS/deployment *planning and docs* are being scrubbed.

Do NOT modify:
- `proxy.ts`
- `lib/sca/routing.ts`
- `.env.example` (the `SCA_HOSTNAME` block stays — the env var still exists)
- any file under `tests/`
- any file under `.planning/phases/09-*/` (historical phase records are immutable; their
  references to the deleted doc are an accurate account of what Phase 9 shipped)
- `.planning/MILESTONES.md` (archived v2.0 milestone record — also immutable history)

**Known reference sites (from grep):**

| File | Lines | Nature |
|------|-------|--------|
| `README.md` | 40, 64-70 | env var doc (keep) + subdomain section (rewrite) |
| `.planning/PROJECT.md` | 9, 13, 38, 41, 93, 115 | live/current statements (edit) |
| `.planning/PROJECT.md` | 21, 29, 78 | archived `<details>` block + shipped-requirement record (leave) |
| `.planning/STATE.md` | 98, 100, 108 | historical decision log entries (leave; append new entry instead) |
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the deployment doc and rewrite the README SCA section</name>
  <files>docs/sca-subdomain-deployment.md, README.md</files>
  <action>
Delete `docs/sca-subdomain-deployment.md` with `git rm docs/sca-subdomain-deployment.md`.
This leaves `docs/` empty; that is fine — git does not track empty directories, so no
further cleanup is needed. Do not create a replacement doc.

In `README.md`, leave line 40 (the `SCA_HOSTNAME` bullet under "Required env vars") exactly
as-is — the env var still exists and is still optional.

Replace the "## SCA Tracker subdomain" section (currently lines 64-70) with a section titled
"## SCA Tracker". Its prose must:
- State that the SCA Tracker lives under `app/sca` and is served at `/sca` — the canonical URL.
- Note that host-based routing exists in `proxy.ts` at the repo root (Next.js 16 renamed
  `middleware.ts` to `proxy.ts`) and would rewrite an `sca.*` host into `/sca`, but that no
  such subdomain is bound and none is planned — the `/sca` path is the intended access route.
- Contain no "not yet performed", "pending", "remaining manual steps", "cutover", or
  "Hostinger" language, and no markdown link to `docs/sca-subdomain-deployment.md`.

Keep the section short (3-4 lines of prose). Match the surrounding README voice: plain
declarative sentences, no headings beyond the `##`, ~100 char soft wrap like the neighbours.
  </action>
  <verify>
    <automated>test ! -f docs/sca-subdomain-deployment.md && ! grep -niE "sca-subdomain-deployment|cutover|not yet performed|Hostinger|remaining manual" README.md && grep -q "app/sca" README.md && grep -q "proxy.ts" README.md && echo PASS</automated>
  </verify>
  <done>The doc file is gone from the working tree and the git index; README.md has an "## SCA Tracker" section that mentions `app/sca` and `proxy.ts` but contains none of the pending-cutover terms and no link to the deleted file.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the DNS cutover item from PROJECT.md and record the decision</name>
  <files>.planning/PROJECT.md</files>
  <action>
Make these edits to `.planning/PROJECT.md`. Line numbers are from the current file; locate
by content, not by offset, since earlier edits shift later lines.

1. **Line 9** (Current State, v2.0 bullet): change "competition tracker at
   `sca.bigmattsbbq.com`" to "competition tracker at `/sca`".

2. **Line 13** (### Next Milestone Goals): delete the entire bullet beginning
   "DNS cutover for `sca.bigmattsbbq.com` at Hostinger". Do not replace it — the remaining
   three candidate bullets stand on their own.

3. **Line 38** (## What This Is): change "SCA steak cookoff competition tracker at
   `sca.bigmattsbbq.com` sharing the same repo" to "SCA steak cookoff competition tracker at
   `/sca` sharing the same repo".

4. **Line 41**: change "SCA Tracker is live at `/sca` (pending DNS cutover to
   `sca.bigmattsbbq.com`), fully database-driven" to "SCA Tracker is live at `/sca`, fully
   database-driven" — just drop the parenthetical.

5. **Line 93** (### Active): delete the entire "[ ] DNS cutover for `sca.bigmattsbbq.com` at
   Hostinger (manual step, checklist in `docs/sca-subdomain-deployment.md`)" bullet.

6. **Line 115** (## Context, SCA Tracker routing bullet): replace it with a bullet that
   states `/sca` is the canonical SCA Tracker URL; that host-based routing via `proxy.ts`
   (Next.js 16's renamed `middleware.ts`) ships and would support an `sca.*` subdomain, but
   the subdomain was decided against as unnecessary; and that the routing code is left in
   place as inert, already-tested infrastructure. Do not reference the deleted doc.

7. **## Key Decisions table**: append one new row after the existing
   "Human-verify checkpoints..." row:
   | `sca.bigmattsbbq.com` subdomain dropped; `/sca` path is canonical | The path already
   serves the tracker identically on every environment; a dedicated subdomain added a DNS
   dependency and a dangling-CNAME takeover surface for zero user-visible benefit | ✓ Good |

8. **Line 78** (### Validated, INFRA-03) stays as written — it is an accurate record that the
   routing code shipped, and the code is still shipped.

9. The archived `<details>` block (lines 18-34) stays untouched — it is explicitly labeled
   "as planned, for historical reference".

10. Update the footer line to: `*Last updated: 2026-08-28 — SCA subdomain dropped; /sca is the canonical SCA Tracker URL*`
  </action>
  <verify>
    <automated>! grep -nE "sca-subdomain-deployment|DNS cutover" .planning/PROJECT.md && ! grep -n "pending DNS cutover" .planning/PROJECT.md && grep -q "decided against" .planning/PROJECT.md && grep -c "^| \`sca.bigmattsbbq.com\` subdomain dropped" .planning/PROJECT.md && echo PASS</automated>
  </verify>
  <done>PROJECT.md contains zero occurrences of "DNS cutover" or "sca-subdomain-deployment"; the Next Milestone Goals list and the Active requirements list no longer mention the subdomain; the Context bullet and a new Key Decisions row record the decision and state that the routing code intentionally remains.</done>
</task>

<task type="auto">
  <name>Task 3: Append the decision to STATE.md and confirm no code changed</name>
  <files>.planning/STATE.md</files>
  <action>
Append a single entry to the end of the `### Decisions` list in `.planning/STATE.md` (after
the final `[Phase 11]` bullet on line 126, still inside "Recent decisions affecting current
work"). Do not edit or delete the existing Phase 9 bullets on lines 98, 100, or 108 — those
are an accurate record of what was decided at the time.

The new bullet must be prefixed `[Quick 260828-f3i]:` and must convey:
- The `sca.bigmattsbbq.com` subdomain and its DNS cutover were decided against; `/sca` is the
  canonical SCA Tracker URL and is sufficient.
- `docs/sca-subdomain-deployment.md` was deleted; README.md and PROJECT.md were updated to
  drop the pending-cutover framing.
- The shipped routing code (`proxy.ts`, `lib/sca/routing.ts`, `SCA_HOSTNAME`) was
  deliberately left in place — it is inert when no `sca.*` host is bound and is already
  tested, so removing it would be churn with regression risk and no benefit.
- No CNAME was ever created at Hostinger and no Vercel domain binding was ever added, so the
  dangling-subdomain-takeover concern documented in the deleted rollback section does not
  apply — there is nothing to unwind.

Also update the frontmatter `last_updated` to the current ISO timestamp and `last_activity`
to `2026-08-28 — SCA subdomain DNS cutover plan scrubbed; /sca is canonical`. Leave
`status`, `stopped_at`, and the `progress` block unchanged — this quick task does not alter
milestone state.

Finally, prove no application code was touched: run
`git status --porcelain` and confirm no entry matches `proxy.ts`, `lib/sca/`, `.env.example`,
`app/`, `components/`, or `tests/`. The pre-existing modifications to `.planning/config.json`
and `supabase/.temp/cli-latest` are unrelated and expected — leave them alone.
  </action>
  <verify>
    <automated>grep -q "\[Quick 260828-f3i\]" .planning/STATE.md && ! git status --porcelain | grep -E "proxy\.ts|lib/sca/|\.env\.example|^.[ MD] (app|components|tests)/" && npm run test 2>&1 | tail -5</automated>
  </verify>
  <done>STATE.md's Decisions list ends with a `[Quick 260828-f3i]` entry covering the decision, the deletion, the deliberate retention of routing code, and the no-dangling-CNAME note; frontmatter timestamps updated; `git status` shows no modification to proxy.ts, lib/sca/, .env.example, app/, components/, or tests/; the full test suite still passes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none introduced | Documentation-only change; no code, config, dependency, or runtime surface is modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-f3i-01 | Information Disclosure | Deleted rollback guidance in `docs/sca-subdomain-deployment.md` | accept | The deleted doc warned about dangling-CNAME subdomain takeover. No CNAME was ever created at Hostinger and no Vercel domain binding was ever added, so there is no dangling record to leave behind. Task 3 records this explicitly in STATE.md so the reasoning survives the deletion. |
| T-f3i-02 | Tampering | `proxy.ts` / `lib/sca/routing.ts` | accept | Routing code is retained unmodified and remains inert: `resolveScaRouting` only matches an exact configured hostname or an `sca.`-prefixed host, neither of which resolves to this deployment. Task 3's `git status` gate proves these files were not touched. |
| T-f3i-SC | Tampering | package installs | n/a | No `npm install` in this plan; no dependency changes. |
</threat_model>

<verification>
1. `test ! -f docs/sca-subdomain-deployment.md` — doc is gone.
2. `grep -rn "sca-subdomain-deployment" README.md .planning/PROJECT.md` returns nothing.
3. `grep -rn "DNS cutover" .planning/PROJECT.md` returns nothing.
4. `git diff --stat HEAD -- proxy.ts lib/sca/ .env.example app/ components/ tests/` is empty.
5. `npm run test` — 247 tests still pass (docs-only change must not move this number).
6. `npm run build` succeeds (guards against a malformed markdown edit breaking nothing, and
   confirms the untouched routing code still compiles).
</verification>

<success_criteria>
- `docs/sca-subdomain-deployment.md` deleted from the working tree and the git index.
- `README.md` has an "## SCA Tracker" section naming `/sca` as canonical, mentioning
  `app/sca` and `proxy.ts`, with no cutover/pending/Hostinger language and no dead link.
- `README.md` still documents the `SCA_HOSTNAME` env var under "Required env vars".
- `.planning/PROJECT.md` has zero occurrences of "DNS cutover" or "sca-subdomain-deployment";
  Next Milestone Goals is down to three candidates; the Active list no longer carries the
  cutover requirement; Context and Key Decisions both record the decision.
- `.planning/STATE.md` has a new `[Quick 260828-f3i]` decision entry and refreshed timestamps,
  with all prior decision entries intact.
- `git status` shows no modification to `proxy.ts`, `lib/sca/routing.ts`, `.env.example`, or
  anything under `app/`, `components/`, or `tests/`.
- `npm run test` passes with the same 247-test count as before.
</success_criteria>

<output>
Create `.planning/quick/260828-f3i-scrub-the-sca-subdomain-dns-cutover-plan/260828-f3i-SUMMARY.md` when done
</output>
