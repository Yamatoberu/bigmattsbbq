---
phase: 260502-ex3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/catering/page.tsx
  - public/UI_review.md
autonomous: true
requirements:
  - UI-REVIEW-12
must_haves:
  truths:
    - "Catering page content container is wider than max-w-4xl on desktop viewports (uses max-w-5xl)"
    - "UI_review.md Issue 12 heading shows strikethrough and DONE status"
    - "UI_review.md Summary table row 12 uses strikethrough on the issue text"
  artifacts:
    - path: "app/catering/page.tsx"
      provides: "Wider catering layout container"
      contains: "max-w-5xl"
    - path: "public/UI_review.md"
      provides: "Issue 12 marked as DONE"
      contains: "Issue 12 — ~~Catering Page Content Too Narrow on Wide Viewports~~ - DONE"
  key_links:
    - from: "app/catering/page.tsx"
      to: "Tailwind responsive layout"
      via: "max-w-5xl mx-auto on root <section>"
      pattern: "max-w-5xl"
    - from: "public/UI_review.md Summary table"
      to: "Issue 12 entry"
      via: "strikethrough syntax"
      pattern: "~~Catering content too narrow on desktop~~"
---

<objective>
Fix catering page max-width so content fills more of the desktop viewport (Issue 12 from public/UI_review.md), and mark Issue 12 as DONE in UI_review.md using the same strikethrough/DONE pattern previously used for Issues 9 and 10.

Purpose: On a 1280px viewport, the catering page content currently sits in a narrow column with large empty side margins, making pricing tiers harder to compare at a glance. The current container is `max-w-4xl` (896px); we are widening to `max-w-5xl` (1024px) to give the three-column pricing tier grids more breathing room while still preserving readable line lengths for the prose.

Output:
- `app/catering/page.tsx` root `<section>` uses `max-w-5xl` instead of `max-w-4xl`.
- `public/UI_review.md` Issue 12 heading and summary table row both show strikethrough + DONE styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@app/catering/page.tsx
@public/UI_review.md

<interfaces>
<!-- Catering page root container (current state) -->
<!-- File: app/catering/page.tsx, line 168 -->

```tsx
export default function CateringPage() {
  return (
    <section className="section-spacing mx-auto max-w-4xl">
      ...
    </section>
  );
}
```

<!-- UI_review.md current state for Issue 12 -->
<!-- File: public/UI_review.md, line 324 (heading) and line 353 (summary row) -->

Heading (line 324):
`## Issue 12 — Catering Page Content Too Narrow on Wide Viewports - TODO`

Summary table row (line 353):
`| 12 | Catering content too narrow on desktop | 🟢 Low | Catering |`

<!-- Reference: how Issue 10 was marked DONE in commit 3b427e7 -->

Heading (after):
`## Issue 10 — ~~About Page Bio Paragraph Has No Heading or Section Wrapper~~ - DONE`

Summary table row (after):
`| 10 | ~~About page bio missing heading/section~~ | 🟢 Low | About |`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen catering page container to max-w-5xl</name>
  <files>app/catering/page.tsx</files>
  <action>
In `app/catering/page.tsx`, on the root `<section>` element returned by `CateringPage()` (currently around line 168), change the className from `"section-spacing mx-auto max-w-4xl"` to `"section-spacing mx-auto max-w-5xl"`. This is a single-character class change — `max-w-4xl` becomes `max-w-5xl`. Do not modify any other classes (`section-spacing` and `mx-auto` must remain so vertical rhythm and centering are preserved). Do not touch any of the inner cards, grids, or `glass-card` blocks; their internal `md:grid-cols-3` and `sm:grid-cols-2` rules already handle responsive columns and will simply have more horizontal space to expand into.

Do not migrate to a two-column prose layout (Issue 12 fix step 2) — that is out of scope for this quick task. Only the max-width bump is needed.
  </action>
  <verify>
    <automated>grep -n 'max-w-5xl' app/catering/page.tsx | grep -c 'section-spacing mx-auto max-w-5xl'</automated>
  </verify>
  <done>
    `app/catering/page.tsx` root `<section>` uses `className="section-spacing mx-auto max-w-5xl"`. No other classes changed. `npm run build` (or `npm run lint`) still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark Issue 12 DONE in UI_review.md</name>
  <files>public/UI_review.md</files>
  <action>
Make two edits to `public/UI_review.md`, matching the exact pattern previously used for Issues 9 and 10 (see commit 3b427e7 for reference):

1. **Issue 12 heading** (currently line 324):
   - Before: `## Issue 12 — Catering Page Content Too Narrow on Wide Viewports - TODO`
   - After: `## Issue 12 — ~~Catering Page Content Too Narrow on Wide Viewports~~ - DONE`
   - The strikethrough (`~~...~~`) wraps only the descriptive title, not the `## Issue 12 — ` prefix or the ` - DONE` suffix.

2. **Summary table row 12** (currently line 353):
   - Before: `| 12 | Catering content too narrow on desktop | 🟢 Low | Catering |`
   - After: `| 12 | ~~Catering content too narrow on desktop~~ | 🟢 Low | Catering |`
   - Only the issue text is wrapped in `~~...~~`; the row number, severity, and pages columns are unchanged.

Do not modify any other Issue headings, table rows, or body content. Do not change the `### Problem` or `### Fix` subsections under Issue 12 — leave those subsections unchanged so the original problem description is preserved for historical reference.
  </action>
  <verify>
    <automated>grep -c '## Issue 12 — ~~Catering Page Content Too Narrow on Wide Viewports~~ - DONE' public/UI_review.md && grep -c '| 12 | ~~Catering content too narrow on desktop~~ | 🟢 Low | Catering |' public/UI_review.md</automated>
  </verify>
  <done>
    Both grep counts return `1`. Issue 12 heading shows strikethrough + DONE. Summary table row 12 shows strikethrough on the issue description. No other lines in `public/UI_review.md` were modified.
  </done>
</task>

</tasks>

<verification>
1. `app/catering/page.tsx` root section uses `max-w-5xl` (verify: `grep -c 'section-spacing mx-auto max-w-5xl' app/catering/page.tsx` returns `1`).
2. `app/catering/page.tsx` no longer uses `max-w-4xl` on the root section (verify: `grep -c 'section-spacing mx-auto max-w-4xl' app/catering/page.tsx` returns `0`).
3. `public/UI_review.md` Issue 12 heading is updated (verify: `grep -c '## Issue 12 — ~~Catering Page Content Too Narrow on Wide Viewports~~ - DONE' public/UI_review.md` returns `1`).
4. `public/UI_review.md` Summary table row 12 is updated (verify: `grep -c '| 12 | ~~Catering content too narrow on desktop~~ | 🟢 Low | Catering |' public/UI_review.md` returns `1`).
5. `npm run build` succeeds (no Tailwind/TypeScript regressions).
</verification>

<success_criteria>
- Catering page content container widened from `max-w-4xl` to `max-w-5xl`, giving the three-column pricing tier grids more breathing room on desktop viewports while leaving mobile/tablet layouts unchanged (responsive grid breakpoints are unaffected).
- UI_review.md Issue 12 visually marked as resolved using the established strikethrough + DONE convention, consistent with Issues 9 and 10.
- No other catering page styling, no other UI_review.md issues, and no other files modified.
- Build passes.
</success_criteria>

<output>
After completion, create `.planning/quick/260502-ex3-fix-catering-page-max-width-for-desktop-/260502-ex3-SUMMARY.md` summarizing:
- Files changed: `app/catering/page.tsx` (max-w-4xl → max-w-5xl), `public/UI_review.md` (Issue 12 marked DONE).
- Why: Issue 12 from public/UI_review.md — catering content felt mobile-shaped on 1280px viewports.
- Verification: grep checks above all pass; `npm run build` succeeds.
- Follow-ups (deferred, not in this task): Issue 12 fix step 2 (two-column prose layout) was intentionally not implemented — current three-column tier grid already fills the wider container adequately.
</output>
