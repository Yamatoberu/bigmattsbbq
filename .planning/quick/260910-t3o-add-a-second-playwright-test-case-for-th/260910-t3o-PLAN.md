---
phase: quick-260910-t3o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/reviewRedirect.spec.ts
autonomous: true
requirements: [ISSUE-17-FOLLOWUP]
must_haves:
  truths:
    - "A real browser navigating to /review ends up with https://g.page/r/CeLcycUsx16aEAI/review in its address bar"
    - "The browser-level test never issues a real network request to Google's servers"
    - "The test makes no assertion about Google's page content, DOM, or UI"
    - "The existing request-level 307/location test still exists and still passes unchanged"
  artifacts:
    - path: "e2e/reviewRedirect.spec.ts"
      provides: "Two Playwright cases in one describe block: raw-HTTP 307 check + real browser navigation check"
      contains: "page.route"
  key_links:
    - from: "e2e/reviewRedirect.spec.ts"
      to: "https://g.page/r/CeLcycUsx16aEAI/review"
      via: "page.route() interception fulfilled with a self-authored stub HTML body"
      pattern: "page\\.route\\("
    - from: "e2e/reviewRedirect.spec.ts"
      to: "/review"
      via: "page.goto navigation followed by a page.url() equality assertion"
      pattern: "page\\.goto\\(\"/review\"\\)"
---

<objective>
Add a second Playwright case to `e2e/reviewRedirect.spec.ts` that proves a real browser navigation to `/review` actually lands at the Google review URL — closing the gap left by the existing raw-HTTP test.

Purpose: The existing test proves the *server* emits `307` + the right `location` header. It does not prove the *browser* follows that redirect chain to the correct final URL, which is the actual customer flow (scan printed QR card → browser opens `/review` → lands on the review form). A redirect can be correct at the header level and still fail in a browser (e.g. a proxy or middleware layer swallowing it), and nothing currently catches that.
Output: One additional `test(...)` inside the existing `test.describe("review redirect", ...)` block. No other file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@e2e/reviewRedirect.spec.ts
@e2e/support/stubs.ts
@playwright.config.ts
@next.config.js

<interfaces>
<!-- Current full contents of the file being extended (do not rewrite; append a second test): -->

```ts
import { test, expect } from "@playwright/test";

test.describe("review redirect", () => {
  test("/review redirects to the Google review form", async ({ request }) => {
    const response = await request.get("/review", { maxRedirects: 0 });

    expect(response.status()).toBe(307);
    expect(response.headers()["location"]).toBe("https://g.page/r/CeLcycUsx16aEAI/review");
  });
});
```

The redirect under test (`next.config.js`, unchanged by this plan):
`source: "/review"` → `destination: "https://g.page/r/CeLcycUsx16aEAI/review"`, `permanent: false` (307).

`page.route(...)` + `route.fulfill(...)` conventions already used in this repo (`e2e/support/stubs.ts`):

```ts
await page.route("**/api/frozen-items", (route) =>
  route.fulfill({
    status: 200,
    contentType: "application/json",
    body: JSON.stringify(items)
  })
);
```

Playwright config facts (`playwright.config.ts`):
- `baseURL` = `PLAYWRIGHT_BASE_URL` or `http://localhost:3000`, so `page.goto("/review")` resolves against the dev server
- Single `chromium` project; `webServer` runs `npm run dev` and reuses an existing server outside CI
- `fullyParallel: true` — the two cases in this file may run concurrently, so the new case must not mutate shared state (a `page.route` registration is scoped to its own `page`, so this is safe)

Behavioral facts that make this test work:
- `page.goto()` follows redirects automatically and resolves at the *final* URL, so `page.url()` after it resolves is the post-redirect address-bar URL
- `page.route()` intercepts navigation requests, including cross-origin ones, so the hop to `g.page` can be fulfilled locally without any real network call
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a browser-navigation case to e2e/reviewRedirect.spec.ts</name>
  <files>e2e/reviewRedirect.spec.ts</files>
  <action>
    Append a second `test(...)` inside the existing `test.describe("review redirect", ...)` block. Leave the existing first test byte-for-byte unchanged — it covers the raw-HTTP contract and is not being replaced. Keep the existing single `import { test, expect } from "@playwright/test";` line; add no new imports.

    Name the new case so the distinction from the first is obvious at the reporter level — the first is a header-level check, this one is a browser-level check. Something like "a browser navigating to /review lands on the Google review URL".

    The new case takes the `{ page }` fixture (not `{ request }`). Before navigating, register `page.route()` against the exact destination string `"https://g.page/r/CeLcycUsx16aEAI/review"` and fulfill it with a self-authored stub: `status: 200`, `contentType: "text/html"`, and a minimal HTML body we write ourselves (e.g. a document containing a recognizable marker string such as `stubbed google review page`). Use the exact absolute URL as the route pattern rather than a `**/` glob — it is the precise thing being asserted, and a loose pattern would let a wrong-but-similar destination slip through.

    The interception is load-bearing, not decorative: it is what keeps the test hermetic. Without it, `page.goto` would make a real request to Google's servers, making the suite network-dependent, slow, and subject to Google rate-limiting or consent-interstitial redirects that would rewrite `page.url()` and break the assertion for reasons unrelated to our code.

    Then `await page.goto("/review")` and assert:
    - `expect(page.url()).toBe("https://g.page/r/CeLcycUsx16aEAI/review")` — this is the primary assertion; it proves the browser's address bar actually followed the redirect chain to the correct destination.
    - A secondary assertion that the self-authored stub body rendered (e.g. `await expect(page.locator("body")).toContainText("stubbed google review page")`). This guards against a vacuous pass: it proves the navigation actually completed at the intercepted destination rather than the URL merely being set by an in-flight or aborted navigation.

    Assert ONLY on our own stub content. Do NOT assert on Google's real page title, DOM, headings, or any UI — that page is outside our control and asserting on it would make this test flaky, which is the same constraint the first test and issue #17 already operate under.

    Do NOT touch `next.config.js`, `playwright.config.ts`, or any other file — this is a test-only addition. Do NOT add a helper to `e2e/support/stubs.ts`: the existing helpers there exist because multiple specs reuse them, and this interception is used by exactly one case in one file, so a single inline `page.route()` call is the simpler and more readable choice. Do NOT add a `test.skip` guard or any fixture setup — `/review` touches no Square, Supabase, or Resend dependency.
  </action>
  <verify>
    <automated>npx playwright test e2e/reviewRedirect.spec.ts --reporter=list && grep -c "page.goto(\"/review\")" e2e/reviewRedirect.spec.ts && grep -c "maxRedirects" e2e/reviewRedirect.spec.ts</automated>
  </verify>
  <done>`npx playwright test e2e/reviewRedirect.spec.ts` reports 2 passing tests. The file still contains the original `maxRedirects: 0` request-level case unchanged, plus a new `{ page }` case using `page.route()` and `page.goto("/review")` that asserts `page.url()` equals `https://g.page/r/CeLcycUsx16aEAI/review`. No imports were added, no other files were modified, and no assertion references Google-authored content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner -> third-party (g.page) | Outbound navigation the browser would make when following the 307; intercepted locally in this test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-t3o-01 | Denial of service | `e2e/reviewRedirect.spec.ts` | mitigate | `page.route()` fulfills the `g.page` hop locally, so the suite issues zero requests to Google and cannot be rate-limited, blocked, or slowed by an external outage |
| T-t3o-02 | Tampering | `/review` destination | mitigate | The route pattern and the `page.url()` assertion both use the exact absolute destination literal — a changed or attacker-substituted destination fails the test rather than being silently intercepted by a loose glob |
| T-t3o-03 | Information disclosure | test execution | accept | The spec reads no credentials, cookies, session, Square, or Supabase data; it navigates one anonymous static route |
| T-t3o-SC | Tampering | package installs | accept | No new npm/pip/cargo dependencies are added by this plan — the package legitimacy gate does not apply |
</threat_model>

<verification>
1. `npx playwright test e2e/reviewRedirect.spec.ts --reporter=list` reports 2 passed, 0 failed.
2. `npx tsc --noEmit` is clean (the spec is TypeScript).
3. `npm run test` still passes 304/304 — this change touches no Vitest-covered module.
4. `git status` shows exactly one modified file: `e2e/reviewRedirect.spec.ts`.
5. Hermeticity spot-check: the new case completes in well under the 60s timeout and passes with the machine offline (or with `g.page` otherwise unreachable), confirming the interception prevents any real Google request.

Note: `npm run lint` (`next lint`) currently fails repo-wide on Next.js 16.1.6 with no ESLint config present. This is pre-existing and logged as a deferred item in STATE.md — do not attempt to fix it here. `npx tsc --noEmit` is the safety net. `npm run build` is not required: no application code changes.
</verification>

<success_criteria>
- `e2e/reviewRedirect.spec.ts` contains two tests in one `test.describe("review redirect", ...)` block
- Test 1 (unchanged): `request.get("/review", { maxRedirects: 0 })` asserts 307 + `location` header
- Test 2 (new): `page.goto("/review")` and `page.url()` equals `https://g.page/r/CeLcycUsx16aEAI/review`
- Test 2 intercepts the `g.page` request with `page.route()` and fulfills it with self-authored HTML — no real network call to Google
- No assertion anywhere in the file references Google-authored page content
- `e2e/support/stubs.ts`, `next.config.js`, and all other files are untouched
</success_criteria>

<source_audit>
| Source | Item | Covered By |
|--------|------|------------|
| REQ | ISSUE-17-FOLLOWUP: browser-level coverage of the /review redirect | Task 1 |
| CONTEXT | Second case in the SAME file, existing test untouched | Task 1 action (append-only instruction) |
| CONTEXT | `page.goto` + `page.url()` assertion, not the request fixture | Task 1 action (primary assertion) |
| CONTEXT | Hermetic via inline `page.route()`; no real Google call, no Google content assertions | Task 1 action + T-t3o-01 |
| CONTEXT | Test-only change; no `next.config.js`, no `e2e/support/stubs.ts` addition | Task 1 action (explicit prohibitions) + verification step 4 |

No unplanned items.
</source_audit>

<follow_up>
**Still outstanding from issue #17 (not this plan's scope):** the deployed-Vercel manual check — confirming `https://bigmattsbbq.com/review` lands on the real Google review form in a live mobile browser, ideally via a scanned QR code. No local test can satisfy that, and this browser-level test deliberately does not attempt to (it stubs Google out). Perform it after deploy.
</follow_up>

<output>
Create `.planning/quick/260910-t3o-add-a-second-playwright-test-case-for-th/260910-t3o-SUMMARY.md` when done
</output>
